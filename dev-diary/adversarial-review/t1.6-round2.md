# T1.6 round 2

## Verdict

APPROVE

| Severity | Count |
| --- | ---: |
| C | 0 |
| H | 0 |
| M | 0 |
| L | 0 |

Round 1 recorded H1. The remediation leaves zero residue from H1. The completed
seeded run now uses `answered_extracted`, and its stored structured result passes
the shared T1.5 parser intact.

## Independent pins

| Claim | Pin | Observed result |
| --- | --- | --- |
| The seed resets from empty migrations without the linked project. | `supabase db reset --local --workdir /tmp/orma-t1-6-r2.0ZoVwF` on an isolated local stack using ports 55620 through 55629. | All four migrations applied, followed by `supabase/seed.sql`. The stack has no linked project. |
| The completed seeded run satisfies its finalisation contract. | Direct PostgreSQL query of the isolated `call_runs` and `results` rows. | The run is `completed`, its disposition is `answered_extracted`, its mood is `energised`, and the result is valid. |
| The stored result satisfies T1.5 rather than only its own `valid` flag. | A Deno evaluation passed the stored `results.structured` value to `parseStructuredResult` from `supabase/functions/_shared/result.ts`. | The parser returned a non-null result with one commitment for the seeded item, `slot_change_requested=no`, and `mood=energised`. |
| Sign-in and RLS browsing remain usable. | Password grant for `local@example.com`, followed by authenticated PostgREST reads. | Profiles 1, consents 1, slots 1, items 4, item mentions 3, commitments 1, call runs 1, events 2, transcripts 1, and results 1. |
