# T6.2 remediation round 1

This remediation addresses T6.2-1 from `t6.2-round1.md`. It does not change the
round-one verdict or the T6.2 status.

| Finding | Remediation | Pin | Mutation covered |
| --- | --- | --- | --- |
| M T6.2-1, retired item metadata | The retired card now calls `itemMeta(item)`, the same helper used by open cards. The helper counts that item's `item_mentions` rows and combines `mentionLabel(count)` with the profile-zone age. A retired item with two mention rows therefore renders `2 mentions` and its age. | The rendered retired metadata line is `{item.retired_reason || 'Retired'} · {itemMeta(item)}`. `itemMeta` reads only the `counts` map built one row at a time from `rows.mentions`. Svelte diagnostics report 0 errors and 0 warnings. | Replacing `itemMeta(item)` with the former age-only expression removes the measured count from retired cards while open cards retain theirs. |

## Verification

- `PATH="$PWD/node_modules/.bin:$PATH" node scripts/with-public-env.mjs svelte-kit sync`
  and `svelte-check --tsconfig ./tsconfig.json` passed with 0 errors and 0 warnings.
- `git diff --check` passed.
- `npm run check` could not complete because another active task has temporarily
  removed `web/src/routes/app/settings/+page.svelte`, which its unrelated
  auth-header check reads. This remediation did not touch that path.

## Owns note

Only `web/src/routes/app/items/+page.svelte` and this remediation record changed
for T6.2. No verdict was changed and no T6.2 status line was edited.
