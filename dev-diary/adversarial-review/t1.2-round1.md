# T1.2 round 1 review

## Verdict

APPROVE

| C | H | M | L |
| --- | --- | --- | --- |
| 0 | 0 | 0 | 0 |

## Independent checks

I applied the core and RLS migrations to an isolated local Supabase stack. I
seeded every public table with one row for each of two authenticated users.

For both ownership directions, the non-owner read, no-op update, and delete
measurements returned zero rows on all 13 tables. This included join-protected
`item_mentions`, `call_events`, `transcripts`, and `results`.

After clearing the JWT subject under the `anon` role, `count(*)` returned zero
for every public table. The database rolled back every policy probe before the
temporary stack stopped.
