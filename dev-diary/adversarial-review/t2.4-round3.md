# T2.4 review, round 3

**Verdict:** APPROVE

**Counts:** C 0, H 0, M 0, L 0

## Zero-residue check

- **Round-one C1, dispatch wiring:** `tick` builds CALL-E dependencies and
  dispatches every atomically claimed run. The configured-tick check observes
  the claim RPC and a fixture-only `POST /v1/calls`.
- **Round-one C2, webhook configuration:** `calle.ts` derives the receiver URL
  only from `ORMA_API_URL` and `ORMA_WEBHOOK_SECRET`. No unsupported
  `ORMA_WEBHOOK_URL` reference remains in the production paths.
- **Round-one H1, fixture and request contract:** the dispatcher loads the
  committed completed-call fixture and pins both its digest and the complete
  serialized request body.
- **Round-two C1, inactive tick scheduler:**
  `20260912170000_schedule_tick.sql` registers `tick-runs` at every UTC minute.
  It posts through the Orma API only when Vault holds a non-empty named key.
- **Scheduler safety:** the migration supplies the Vault value only as
  `apikey`. Tick alone disables gateway JWT validation, and
  `withSupabase({ auth: "secret:materialise" })` rejects an unauthenticated
  request before its handler performs a database read.
- **T2.9 transitions owned by this seam:** tick appends `claimed` after the
  SQL claim. Dispatch appends `dispatched` only after its provider result is
  durably stored.

## Independent checks

`deno test --allow-env --allow-net --allow-read
supabase/functions/_shared/calle.ts supabase/functions/tick/index.ts
supabase/functions/_shared/events.ts` passed 30 checks.

`git diff --check` passed.

The scheduler source independently shows the named `tick-runs` cron job, the
every-minute expression, the Vault lookup, the non-empty predicate, and the
authenticated tick endpoint. No CALL-E request was made during this review.
