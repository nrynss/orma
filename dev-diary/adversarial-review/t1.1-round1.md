# T1.1 round 1 review

## Verdict

REMEDIATE

| C | H | M | L |
| --- | --- | --- | --- |
| 0 | 1 | 0 | 0 |

The migration defines every table and database constraint named by `spec.md` §2
and T1.1. It has no executable acceptance test. The local Supabase runtime is
also absent, so this review could not measure the migration against PostgreSQL.

| Where | What | Pin | Mutation |
| --- | --- | --- | --- |
| `supabase/migrations/20260911000000_core_schema.sql` | T1.1 has no executable database test for its required clean reset, duplicate `idempotency_key` rejection, invalid `phone_e164` rejection, or tick query plan. `supabase status` reports that `supabase_db_orma` does not exist, so the required `supabase db reset` measurement cannot run locally. | Add a runnable migration test. It must reset a local database, attempt duplicate idempotency and invalid phone inserts, then inspect `EXPLAIN` for the tick predicate. It fails today because no test or local runtime exists. | Removing the unique constraint, phone check, or scheduler index would still leave the repository green. A later migration failure would also have no automated detection. |
