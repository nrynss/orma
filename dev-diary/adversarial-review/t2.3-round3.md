# T2.3 review, round 3

**Verdict:** APPROVE

**Counts:** C 0, H 0, M 0, L 0

## Prior-finding residue

| Prior finding | Independent pin | Result |
| --- | --- | --- |
| C1. The scheduler could claim a run before T2.4 could dispatch it. | `20260912161000_tick.sql` contains no `cron.schedule` invocation. `deno test --allow-env --allow-net supabase/functions/tick/index.ts` passed the no-dispatch check, which records no claim RPC request. | Resolved. The deployed seam cannot claim a scheduled row without a dispatcher. |
| H1, round 1. The concurrency check mocked PostgreSQL. | `bash supabase/migrations/test-tick-claims.sh` started an isolated local database, applied migrations through `20260912161000_tick.sql`, and used two independent `psql` sessions as `service_role`. The sessions returned one claim and zero claims, then the database contained one claimed row. | Resolved. The acceptance test exercises the RPC, grant, transaction lock and persisted state. |
| H1, round 2. The concurrency timeout used whole seconds and could accept a blocking query. | The same isolated acceptance run completed session two within the runner's 750 ms Python `time.monotonic()` timeout while session one held the row lock for three seconds. | Resolved. A blocking `FOR UPDATE` cannot return after the first transaction commits and still pass the sub-second deadline. |

## Independent checks

`deno test --allow-env --allow-net supabase/functions/tick/index.ts` reported 3 passed and 0 failed.

`bash supabase/migrations/test-tick-claims.sh` passed against a temporary local Supabase database. It applied the T2.3 migration and verified one atomic claim across two real PostgreSQL sessions.

`git diff --check` passed.

The migration limits the batch to 20, keeps the candidate lock and state update in one statement, and grants the RPC only to `service_role`. The tick defers claiming until T2.4 supplies dispatch, preventing a committed claim from being stranded before that dependency exists.
