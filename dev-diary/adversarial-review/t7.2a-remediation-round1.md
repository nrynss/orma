# T7.2a remediation, round 1

Round 1 commit `878b91a` returned REMEDIATE with C0 H0 M2 L0 against the
implementation at `b28fd3b`. This round fixes both findings. The
`telegram/voice.ts` seam edit was authorized by the orchestrator and is
recorded in `t7.2a-contract-change.md`.

## Findings

| where | what | pin | mutation |
| --- | --- | --- | --- |
| M `telegram/voice.ts` lines 113 to 122 beside `_shared/vertex.ts` lines 20 to 29 | The duplicate byte-identical URL builder is deleted. `telegram/voice.ts` now re-exports `vertexGenerateContentUrl` from `../_shared/vertex.ts` in one line. The name and call signature stay the same. | A repo grep finds `aiplatform.googleapis.com` only in `_shared/vertex.ts` lines 26 to 27. `telegram/voice.ts` line 113 holds only the re-export. The combined suite passes 38 tests with `voice_tests_b.ts`, `voice_tests_b2.ts` and `voice_tests_d.ts` still importing the name from `telegram/voice.ts`. | Reintroducing a local body would recreate a second exported definition. The grep pin then reports two hits and fails the review rule that only the shared module may build the URL. |
| M `_shared/vertex_tests.ts` token stubs against `_shared/vertex.ts` lines 84 and 86 | Two injected-fetch tests join the suite. One returns a 500 token response. One returns a token 200 without `access_token`. Both assert the `google token exchange failed` message. | Each test records every fetch call and asserts exactly one request after the throw. `generateContent` can never run on a failed token exchange. Suite grows from 7 to 9 shared tests, all older tests unchanged. | Both failure throws in `googleAccessToken` deleted, replaced by a cast fall-through returning an empty token. The suite reported `FAILED | 7 passed | 2 failed`. Both new pins failed on their message assertions while every older test stayed green. |

## Proof

1. `deno test --allow-read --allow-env supabase/functions/telegram/voice.ts
   supabase/functions/_shared/vertex.ts supabase/functions/_shared/vertex_tests.ts`
   printed `ok | 38 passed | 0 failed (1s)`. The two new shared tests ran in the
   combined suite.
2. `deno test --allow-read supabase/functions/_shared/vertex.ts
   supabase/functions/_shared/vertex_tests.ts` printed
   `ok | 9 passed | 0 failed (791ms)`.
3. `deno check supabase/functions/telegram/index.ts` exited 0.
4. The mutation run was a temporary working-tree edit on `_shared/vertex.ts`
   with both failure throws removed. `vertex.ts` was restored by copy right
   after the run. Its checksum returned to
   `10bc9a3f46dfa2d5afe21889554e61d6ac9c864fd2089c741cd0a899cd357b5e`, the
   pre-mutation value, and `git diff` reports it clean.
5. The grep in the first pin row was run after restoration. No
   `--allow-net` flag and no real credential appear in any new test.

## Notes for the next round

Both new tests share the recorded-call stub style of the bearer-token test.
The `calls.length` assertion is what turns a message mismatch into a proof
that generateContent never ran.
