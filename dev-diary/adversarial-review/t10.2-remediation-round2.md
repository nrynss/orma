# T10.2 remediation, round 2

Round 2 remains REMEDIATE with C0 H1 M0 L0. This remediation fixes H1 only.
It changes no verdict and does not mark the task complete.

## Findings

| where | what | pin | mutation |
| --- | --- | --- | --- |
| H1 `supabase/functions/calle-webhook/index.ts`, `a confirmation webhook repairs a lost post-dispatch record write` | The authoritative confirmation webhook now claims by `phone_confirmation_id` without requiring a pre-existing `calle_call_id`. It writes the returned provider id and maps a completed call to `dialled`, which restores verification eligibility after the earlier dispatch patch failed. | `deno test --allow-env --allow-net --allow-read supabase/functions/calle-webhook/index.ts` passed 38 tests. The new pin drives a terminal authoritative `call_terminal` response with a confirmation id into a row that has no stored provider id. It independently captures the database patch and asserts one id-only claim with `calle_call_id = call_terminal` and `state = dialled`. | Restore the prior `calle_call_id` filter or omit the provider id from the patch. The pin finds either no matching repair or a row that remains unbound and not verification eligible. |

## Proof

1. `deno check supabase/functions/calle-webhook/index.ts` exited 0.
2. `deno test --allow-env --allow-net --allow-read supabase/functions/calle-webhook/index.ts` passed 38 tests.
3. `git diff --check` exited 0.
