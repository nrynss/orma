# T8.1 remediation, round 3

Round 3 review returned REMEDIATE with C0 H0 M1 L1 against `scripts/seed.ts`.
This round remediates both findings in `scripts/seed.ts`.

## Findings

| where | what | pin | mutation |
| --- | --- | --- | --- |
| M `verifyCounts`, RPC call to `assemble_briefing` | `verifyCounts` called `assemble_briefing` without `p_now`. Verification evaluated ages against today instead of the requested demo date. | Running with `--demo-date 2026-09-20` printed "✗ Lead line does not include age" because the SQL function defaulted `p_now` to current database time. | `verifyCounts` now passes `p_now: dateTimeInTz(plan.demoDate, 8, 0, plan.profile.timezone)`. Verification checks the lead line against the demo date under any `--demo-date`. Reverting parameter restores the false failure. |
| L `removeSeed`, unused variable declaration | `removeSeed` declared `const userId = profile.id` without reading it. | Running `deno lint scripts/seed.ts` failed with exit code 1 naming unused `userId`. | The unused `const userId = profile.id` declaration was removed from `removeSeed`. `deno lint scripts/seed.ts` now exits 0 with zero errors. Restoring line fails lint. |

## Proof

1. `deno lint scripts/seed.ts` passes with exit code 0.
2. `scripts/p6-local-stack.sh up` verified against `firstrun@example.com` (America/New_York) with `--demo-date 2026-09-20`.
3. `assemble_briefing` at `2026-09-20 08:00 EDT` reports lead line "You've mentioned the dentist 3 times. It's been 34 days."
4. `verifyCounts` passes all checks: 3 times and 34 days verified.
5. `--remove` removes all seeded rows cleanly for the account.
6. Local stack torn down cleanly via `scripts/p6-local-stack.sh down`. Zero containers remain.
