# T2.5 remediation, round 1

This file answers `t2.5-round1.md` (verdict REMEDIATE, C 0, H 1, M 1, L 1). The verdict stays with the reviewer. This round fixed the three findings and nothing else.

Edited files: `supabase/functions/tick/index.ts`, `supabase/functions/_shared/poll.ts` and `supabase/functions/calle-webhook/index.ts`. The T2.8 dry-run edits in `tick/index.ts` stay intact. The `unavailable("finalisation")` stub stays in place, because T2.7a owns the real finaliser.

## Findings

| Finding | Remediation | Pin | Mutation covered |
| --- | --- | --- | --- |
| H. `tick/index.ts` finalise fan-out is not isolated, so the throwing stub turns every tick into a 500 once a poll-terminalised run exists. | `tick()` now wraps each `deps.finalise(run)` in its own `try`/`catch`, mirroring `pollStep`. A failing run logs its id and stays selected for the next tick. | Unit test "a run that cannot finalise costs neither the tick nor its siblings". It serves two terminal runs, throws for one, and asserts a 200 plus the sibling finalised. Live pin H below. | Reverting to `Promise.all(terminal.map((run) => deps.finalise(run)))` fails the test with "a finalise failure must not turn the tick red, saw 500". |
| M. `_shared/poll.ts` give-up deadline falls back to `poll_after` when `dispatched_at` is null, so the deadline slides on every pass. | `readRun` now selects `created_at`. The window runs from `dispatched_at ?? created_at`. `created_at` is `not null` with a `now()` default and no writer moves it. | Unit test "a run without dispatched_at gives up on its creation time, not its sliding poll_after". It asserts the read selects `created_at`, the PATCH writes `failed` with `poll_timeout`, and no re-fetch happens. Live pin M below. | Restoring the `poll_after` fallback fails the test with "a run with no dispatched_at must still give up once its window has passed". |
| L. `calle-webhook/index.ts` ignores the result of `terminaliseFromRefetch`, so a `refetched` row carries a superseded state when the poll won. | `terminaliseFromRefetch` now returns `applied`, `already_resolved` or `not_terminal`. The receiver records that value as `outcome` in the `refetched` detail. | Unit test "the refetched row marks whether the webhook's write won or the poll had already resolved the run". A PATCH matching one row must record `applied`, and a PATCH matching zero rows must record `already_resolved`. Live pin L below. | Dropping `outcome` from the detail fails the test with "a PATCH matching 1 rows must record outcome applied". |

## Static checks, observed output

- `deno check` passed on all three files.
- `deno test --allow-env --allow-read --allow-net _shared/poll.ts`: `ok | 14 passed | 0 failed`.
- `deno test --allow-env --allow-read --allow-net tick/index.ts`: `ok | 44 passed | 0 failed`. This count includes tests registered by imported modules.
- `deno test --allow-env --allow-read --allow-net calle-webhook/index.ts`: `ok | 28 passed | 0 failed`. This count includes the poll.ts tests it imports.

## Revert checks

Each revert ran in a scratch copy of `supabase/functions` under `/tmp/orma-p2/t25-revert-{H,M,L}`. The shared working tree never held a reverted fix. Each filtered run reported `FAILED | 0 passed | 1 failed`, with the messages quoted in the table.

## Live pins

The driver is `/tmp/orma-p2/rem-t25-pins.ts`. It imports the working-tree `createTickHandler` and tick `depsFromEnv` from `tick/index.ts`, the webhook handler, and `pollRun`. A stub serves `api.call-e.test`, and the driver refuses any other outbound host. Every observation below is a SQL read, taken with `docker exec supabase_db_stack psql`. The host port 57022 refused connections from this shell, although the container reported healthy.

Baseline before the pins: 0 runs in `scheduled` or `claimed`, 0 due polls, and 0 terminal runs with a null `completed_at`.

**H.** The driver created a `completed` run with `completed_at` null and a due `awaiting_result` sibling. One real tick answered 200. SQL then read the sibling at `state = completed`, `completed_at` null, `poll_after` null, with exactly one `polled` row carrying `completed`. The terminal run read unchanged, with no `polled` row.

**M.** The driver created an `awaiting_result` run with `dispatched_at` null and `created_at` twelve minutes old. The window closed three minutes later. Ten passes of the real `pollRun` advanced a simulated clock one minute each. SQL read three `polled` rows in order: `pending`, `pending`, then `failed` with `poll_timeout`. The run read `state = failed`, `disposition = not_answered`, `calle_failure.failure_code = poll_timeout` and `completed_at` null. The last seven passes wrote nothing.

**L.** The poll moved a run to `completed` first. The real webhook then delivered with a re-fetch of `failed` and answered 200. SQL read the run at `completed`, then three timeline rows: `polled` (`completed`, `terminal`), `webhook_received`, and `refetched` (`failed`, `already_resolved`).

**Cleanup.** The driver's run rows, their cascaded events and its webhook event were deleted. A SQL count then read 0 rows for every marker the driver used.

## Not closed here

- The live T2.5 done condition still needs a real CALL-E key. This round placed no real call.
- The `refetched` row still carries the observed `state` alongside `outcome`. T2.9 must read `outcome` before it treats that state as authoritative.
- The stack's containers had restarted about four minutes before these pins ran. Nothing here depends on rows from an earlier run.
