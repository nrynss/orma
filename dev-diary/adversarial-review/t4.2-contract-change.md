# T4.2 contract change request

T4.2 owns only `supabase/functions/mcp/tools/`.
Transport and JWT auth stay in T4.1 `index.ts`.

Without index wiring the new handlers never run on the live MCP
endpoint. Raising this contract is correct for the owns seam.

## Ask
In `supabase/functions/mcp/index.ts`, owned by T4.1 or a follow-up:

1. Import tools:
   `import { registerOrmaTools } from "./tools/mod.ts"`
2. Change `createOrmaMcpServer` to take the authenticated user:
   `createOrmaMcpServer(client, user)`
3. Replace the stub `registerTool` loop with one call:
   `registerOrmaTools(server, client, user)`
4. Pass `user` from `createMcpHandler` into `createOrmaMcpServer`.

Keep `TOOL_NAMES` export in index if other pins need it, or re-export
from `./tools/mod.ts`.

## Why not edited here
Task owns `tools/` only. Editing `index.ts` would cross the T4.1 seam.
Orchestrator or a tiny follow-up can land the four lines above.

## Residual
Proxy `/mcp` rewrite and `[functions.mcp] verify_jwt = false` remain
from `t4.1-contract-change.md`. Unchanged by T4.2.
