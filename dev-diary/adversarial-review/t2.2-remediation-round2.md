# T2.2 remediation, round 2

This remediation addresses H1 from `t2.2-round2.md`. It does not change the
review verdict or task status.

| Finding | Remediation | Pin | Mutation |
| --- | --- | --- | --- |
| H1 | Added an independent, fully substituted expected CALL-E task in `briefing.ts`. The golden test compares `renderCallTask(fixture)` to it byte for byte. The expected output follows the reviewed task in `docs/calle-call.md`. | `deno test --allow-read supabase/functions/_shared/briefing.ts` passes `golden briefing renders the reviewed CALL-E task byte for byte`. | Changing a CALL-E instruction, ordering, safety rule, or substituted output while leaving the expected contract intact fails the byte comparison. |
