# T6.2 round 1 review

## Verdict

**REMEDIATE**

Reviewer: Luna (`gpt-5.6-luna`)

The owner item page loads and mutates items through the browser client. Open items
show mention counts, but retired items omit them. This violates the item metadata
requirement for every item.

| Severity | Count |
| --- | --- |
| C | 0 |
| H | 0 |
| M | 1 |
| L | 0 |

## Scope and evidence

I read `AGENTS.md`, the T6.2 specification, the T6.1 handoff, the T6.2 handoff,
the item page and its load module, the item and RLS migrations, and the shared age
helpers. I inspected the working tree directly. I did not modify implementation
files.

`npm run check` in `web/` passed with zero Svelte diagnostics. The page load reads
`item_mentions` independently, and the RLS policy restricts those rows through
the owning item. The open list filters retired rows, and retire and restore preserve
the mention rows. The date update is owner-scoped by RLS and feeds the shared age
helper in the profile timezone.

## Findings

| ID | Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- | --- |
| T6.2-1 | M | `web/src/routes/app/items/+page.svelte`, retired item metadata | Retired items render only the retirement reason and age. They do not render the mention count, although the task requires mention count and age on each item. | Render a fixture retired item with two rows in `item_mentions`, then independently count those rows and inspect the retired card. The card must expose `2 mentions` alongside its age. | Remove the count expression from the retired metadata after remediation. The retired card loses the measured count while open cards still pass, proving this fix covers the missing branch. |

## Cross-task check

T6.7 adds owner-action RPC types and a migration. It does not overlap the T6.2
owned directory, and no integration defect was found between the two changes.

## Re-review requirement

Round 2 must show the retired branch with a measured mention count, preserve the
open-list count, and re-run the page checks. It must also claim zero residue from
T6.2-1.
