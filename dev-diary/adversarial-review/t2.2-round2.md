# T2.2 review, round 2

**Verdict:** REMEDIATE

**Counts:** C 0, H 1, M 0, L 0

`deno test --allow-read supabase/functions/_shared/briefing.ts` passed 3 checks.
`bash supabase/migrations/test-briefing.sh` passed against an isolated PostgreSQL
instance. The acceptance runner independently proves the local-date boundary,
the durable provider summary, the four-mention 34-day lead, the two-fact cap,
and the service-role grant.

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| H | `supabase/functions/_shared/briefing.ts:184-195` | The renderer check proves only that every variable is substituted and that the Hello line survives. It does not compare the rendered task with the reviewed template in `docs/calle-call.md`, byte for byte. The round-one H3 pin explicitly required that comparison. A changed step, ordering, safety instruction, or closing line can therefore ship while all three tests pass. | Add a checked-in expected rendered task derived from the reviewed template and compare `renderCallTask(fixture)` byte-for-byte. The fixture must substitute every variable, so an instruction or ordering mutation fails independently of the module's own template literal. | Replacing step 6, removing the no-number rule, or changing the walk-before-capture order leaves the present renderer checks green. |

There is no residue from round-one H1, H2, or M1. H3's database-backed SQL
coverage is now present and passes. Its required renderer byte-for-byte pin
remains absent.
