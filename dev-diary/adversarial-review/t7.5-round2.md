# T7.5 round 2 review

## Verdict

**APPROVE**

The remediation closes the round1 finding with a one-shot generic fallback in the
receipt wrapper. Every brief probe passes on my own runs. Zero findings at every
severity, and the round1 residue is proven zero by mutation.

| C | H | M | L |
| --- | --- | --- | --- |
| 0 | 0 | 0 | 0 |

## Scope

Remediation commit `c93cbff` on `phase-7`, reviewed as the diff `03e454d..c93cbff`.
One product file changes, `supabase/functions/_shared/finalise.ts`. The tick, calle,
receipt, deliver-telegram and ingest files are byte-identical across the diff. The
remediation diary is the only other change. I implemented nothing and remediated
nothing. My only lasting write is this file.

I read `AGENTS.md` in full, the T7.5 block in `dev-diary/PHASE-7-analysis.md`, the
round1 review, the remediation diary, `receipt.ts`, `deliver-telegram.ts`, and the
full receipt path with its suite in `finalise.ts`.

## Independent evidence

Every proof below is my own run at `c93cbff` on this machine. Deno 2.9.6 is on PATH.

- `deno test --allow-read --allow-env supabase/functions/_shared/finalise.ts`
  passed 88 of 88.
- `deno test --allow-read --allow-env supabase/functions/tick/index.ts` passed
  101 of 101.
- `deno test --allow-read supabase/functions/_shared/receipt.ts
  supabase/functions/_shared/receipt_tests.ts` passed 5 of 5.
- `deno test --allow-read supabase/functions/_shared/deliver-telegram.ts
  supabase/functions/_shared/deliver-telegram_tests.ts` passed 10 of 10.
- `deno check supabase/functions/tick/index.ts` exited 0.

### The zero-residue mutation, run by me

Round1 found one H. One CTA marker in arbitrary user or transcript text killed the
whole receipt. I proved the fix is load-bearing.

1. I recorded the sha256 of `finalise.ts` at `c93cbff`:
   `b1d809db48c3c9da3d54417d3d4cb4629afe3f35314f46ea9c8a4a7cf4cbc5f0`.
2. I mutated the wrapper. The fallback try block became a bare rethrow of the
   caught error, so the refusal escaped and no generic receipt could go out.
3. The suite printed `FAILED | 87 passed | 1 failed`. The only red test was
   `unsafe user text still sends one generic post_call receipt`.
4. I restored the file and re-checked the checksum. It matches the recorded hash
   exactly, and `git status` shows no product file modified.
5. I re-ran all five commands on the restored file. All green at the counts above.

A blunter first mutation rethrew before the non-CTA branch and left the fallback
unreachable. Deno rejected that at type-check instead of failing the pin, so I
restored, re-verified the checksum, and used the clean shape. The proof stands on
the clean mutation only.

### The brief pins, each one checked

- **Safe text still names captures, retirements and commitments.** The pin
  `a successful finalise sends one post_call receipt naming resolved texts`
  resolves item-1 and item-2 and asserts all three resolved strings in the send.
  Green.
- **Unsafe text falls back once to the honest generic receipt.** The new pin
  drives a captured question and a retired `please` text. Exactly one Telegram
  send, body exactly `Call summary\nCaptured: none\nRetired: none`. The fallback
  is one call inside the catch. No loop exists in the wrapper.
- **The unsafe strings reach neither Telegram nor the payload.** The pin rejects
  both unsafe strings in the send text and requires the row payload text to equal
  the generic summary. The fallback passes `structured: null`, so the payload
  arrays are empty by construction.
- **Exactly one delivery row exists on the fallback.** The pin counts one
  deliveries POST and one PATCH. The guard throws at `deliver-telegram.ts`
  line 319, before the profile read and before `insertDelivery`, so the refused
  attempt leaves no row that the retry could duplicate.
- **The fallback reveals no failure and invents no content.** The generic body
  carries no error text and no invented names. A fallback failure logs the run id
  and the message and nothing else.
- **A replay still sends nothing.** The replay pin asserts zero items, profiles,
  deliveries and Telegram requests. Unchanged and green.
- **Other receipt failures still only log and leave finalisation intact.** The
  profiles-503 pin forbids any deliveries row and keeps the `finalised` row. The
  Telegram-500 pin keeps the ingest disposition and writes the error text on the
  row. Both unchanged and green, and both use non-CTA errors, so neither routes
  through the new fallback.
- **Production bot-token threading and named-env failure hold.** `receiptDeps`
  threads the token into both the named attempt and the fallback. `depsFromEnv`
  requires `TELEGRAM_BOT_TOKEN` by name. Both pins stay green.
- **No old test was weakened.** The diff deletes seven lines, three header
  comment lines and four catch lines. Every test change is an addition. The 87
  prior finalise tests pass unmodified beside the new one.
- **No secret and no project host.** A scan of the full diff finds no URL, no
  host, and no key-shaped value. The only token literal is the pre-existing
  `fixture-bot-token` fixture constant.

## Findings

None at any severity, so the four-column table has no rows.

## Zero residue against round1

Round1 carried C0 H1 M0 L0. The single H was the all-or-nothing CTA throw in
`sendPostCallReceipt`. The remediation catches exactly that refusal and retries
once with no named texts. My mutation proof shows the pin fails when the fix is
removed, so the fix is load-bearing and the finding is closed. Zero residue on
round1. The production-caller item from `p7-e2e-round1.md` stays closed, and the
untouched T7.4 and T7.3 seams pass at 5 and 10.

## False positives I rejected

- **The refusal is detected by message equality across two files.**
  `CTA_REFUSAL_MESSAGE` in `finalise.ts` mirrors the seam's throw at
  `deliver-telegram.ts` line 146. The new pin fails if either side drifts, so the
  coupling is guarded. The seam sits outside this task's owns line, so a typed
  error was not available without a contract change.
- **The generic receipt prints none for a run that captured and retired.** The
  round2 brief names this exact honest generic receipt as the prescribed
  behaviour. It invents nothing and reveals nothing. Deliberate design.
- **The fallback could mask a guard that refuses all text.** The safe-naming pin
  would fail first, so a guard regression cannot hide behind the fallback.
- **A failed or canceled call also gets a receipt when it finalises.** That
  wiring predates this diff, matches the T7.4 sentence that a run with no result
  still sends a receipt, and round1 walked the same path without scoring it.
- **The wrapper ignores the `DeliverResult` status on a named send failure.**
  True at `03e454d` as well. The row carries the error text inside the seam.
  Nothing in this diff changes it.
- **Tick reports 101 tests against the 100 quoted in round1.** The tick file is
  byte-identical across the diff. The remediation diary already flags the earlier
  quote as a miscount. No code question remains.

## Done-when for the orchestrator

Zero findings at every severity and a proven zero-residue claim. The verdict is
APPROVE and T7.5 can land.
