# T2.10 phase e2e remediation, round 3

Round 3 found the same class as round 2 on the two writers round 2 did not name.
Both are closed here. No verdict changes. Nothing nobody found was fixed.

## Findings

| Sev | Finding | Remediation | Pin | Mutation |
| --- | --- | --- | --- | --- |
| M1 | `dispatchOne`'s two terminal failure writes, the 409 idempotency conflict and any non-2xx response, wrote `state = 'failed'` and `completed_at` and never a disposition. `terminalRuns()` only selects runs with a null `completed_at`, so no later writer reaches them. `billable` read false only from the column default. | `_shared/calle.ts` now derives that pair once, in `failedDispatchFields`, and both writes plus the recovery use it. It returns `state = 'failed'`, `disposition = 'not_answered'` and `billable = false`, because `spec.md` section 3 maps a failed run to `not_answered` and never bills that disposition. One definition, so a fourth dispatcher failure cannot invent its own. | `deno test` on `_shared/calle.ts`. The 409 case asserts the terminal PATCH body carries `"disposition":"not_answered"` and `"billable":false`, and the rejected-dispatch case asserts the same on its parsed patch. Driver PHASE A1 and A2 read `failed \| not_answered \| false \| completed=true \| writer=null \| id=null`, and PHASE H counts zero terminal runs at rest with a null disposition, against two before. | Reverting the two writes fails both Deno cases on the PATCH body, and the instrument reads `failed \| null` again with PHASE H back at two. |
| M2 | `abandon_call_run` wrote `disposition = 'not_answered'` without setting `state`, so a call the give-up ended kept whatever state it held. Two real paths arrive holding another state, a webhook-terminalised `completed` run whose re-fetch then failed three times, and a rehearsal whose ingestion call failed once. Both ended `completed / not_answered`, which `spec.md` section 3 never produces. The rehearsal also sent the finaliser after a `fixture:` id CALL-E cannot have issued. | The give-up now writes `state = 'failed'` in the same guarded UPDATE that sets `completed_at`, `disposition = 'not_answered'` and `billable = false`, so it writes the whole pair the spec gives a failed run whatever the run held. `finaliseStep` also recognises a synthetic call id and ends that run at once, through the same boundary and with its own reason, instead of spending three re-fetches on an id CALL-E never issued. A rehearsal that ingested is already finished, so only a stranded one reaches that line. | `bash supabase/migrations/test-ingest.sh` case seventeen now starts its first run in the state the webhook leaves, `completed` with the disposition unwritten, and reads `failed not_answered false true the call re-fetch failed after 3 attempts`, so dropping the state half fails the case. `deno test` on `_shared/finalise.ts` asserts a `fixture:` run reaches no CALL-E host, spends no attempt, and posts the synthetic-id reason to `abandon_call_run`. Driver PHASE C3 reads `failed \| not_answered \| false \| ... \| id=provider`, and PHASE I reads `failed \| not_answered \| false \| ... \| id=fixture:synthetic` with zero attempts and the timeline `claimed,dispatched,finalised`. | Reverting the state fails case seventeen with `the abandoned run read 'completed not_answered false true ...'`. Reverting the synthetic-id guard fails the finalise case, because the module asks CALL-E about `fixture:call_recorded` again. |

## Every writer that ends a run, after this round

| Writer | State | Disposition | Billed |
| --- | --- | --- | --- |
| `ingest_call_result` | from the payload | from the payload | the disposition, and a real call id |
| the 409 conflict | `failed` | `not_answered` | never |
| the non-2xx response | `failed` | `not_answered` | never |
| the dispatch recovery | `failed` | `not_answered` | never |
| the dispatch refusal | `canceled` | `canceled` | never |
| the poll's two give-ups | `failed` | `not_answered` | never |
| `abandon_call_run` | `failed` | `not_answered` | never |

Every write that ends a run stores the pair itself. The three dispatcher
failures derive it from one definition, and each SQL boundary writes it in the
statement that ends the run.

## Contract changes

None this round. Every path this round touched is on the T2.10 `owns` line,
which the orchestrator widened before the work started. `dev-diary/spec.md`
still needs no edit, because both fixed pairs are rows its table already
carries.

## Evidence

The reviewer's instrument ran unchanged against the shared stack before and
after the fix, with `/tmp/r3e2e/seed.sql` loaded and `/tmp/r3e2e/cleanup.sql`
afterwards.

| Reading | Before | After |
| --- | --- | --- |
| PHASE A1 pair | `failed \| null \| false \| completed=true \| writer=null \| id=null` | `failed \| not_answered \| false \| completed=true \| writer=null \| id=null` |
| PHASE A2 pair | `failed \| null \| false \| completed=true \| writer=null \| id=null` | `failed \| not_answered \| false \| completed=true \| writer=null \| id=null` |
| PHASE C3 pair | `completed \| not_answered \| false \| completed=true \| writer=null \| id=provider` | `failed \| not_answered \| false \| completed=true \| writer=null \| id=provider` |
| PHASE I pair at rest | `completed \| not_answered \| false \| completed=true \| writer=dry_run \| id=fixture:synthetic` | `failed \| not_answered \| false \| completed=true \| writer=dry_run \| id=fixture:synthetic` |
| PHASE I attempts | 3, after `CALL-E re-fetch returned HTTP 404` | 0 |
| PHASE I timeline | `claimed,dispatched,finalised` over two ticks | `claimed,dispatched,finalised` in one tick |
| PHASE H terminal runs at rest with a null disposition | 2 | 0 |
| PHASE H rows where billable disagrees with the disposition rule | 0 | 0 |
| Instrument verdict | 58 passed, 0 failed | 57 passed, 1 failed unchanged, and 59 passed, 0 failed in the copy below |

The unchanged run's one failure is its PHASE C3 check, which asserts the pair
`completed / not_answered` and therefore characterises the defect. The copy at
`/tmp/t210/r3-driver-after.ts` changes that one expectation to the pair the spec
produces and adds a PHASE I check for the zero attempts, so the same instrument
passes 59 of 59 on the fixed tree. The instrument at `/tmp/r3e2e/driver.ts` is
the reviewer's file and it was not edited.

```
deno check <the nine files below>        -> exit 0
deno test --allow-env --allow-read --allow-net <the same nine>
                                          -> ok | 283 passed | 0 failed
bash supabase/migrations/test-ingest.sh   -> exit 0, "T2.7 ingestion acceptance passed."
bash /tmp/t210/fresh-migrations.sh        -> exit 0, all thirteen migrations applied in order
```

The nine files are `_shared/briefing.ts`, `_shared/ingest.ts`, `_shared/poll.ts`,
`_shared/finalise.ts`, `_shared/dispatch-mode.ts`, `_shared/fixtures.ts`,
`_shared/calle.ts`, `calle-webhook/index.ts` and `tick/index.ts`.

The fresh database and the shared stack matched on all six `md5(prosrc)` values,
with `abandon_call_run = 1aa1124a4ea9a88dcfbbd6b60ddef630` on both, on 109
`public` columns with no diff, and on all five RPC ACLs.

## Reverting one fix at a time

| Tree | Fix reverted | Pin run in the tree | Observed |
| --- | --- | --- | --- |
| `/tmp/t210/trees/revert-m1` | the two dispatcher failure writes, back to state, failure and completion time alone | `deno test` on `_shared/calle.ts` | `FAILED \| 48 passed \| 2 failed`. `the 409 write must carry the pair for a failed run, saw {"state":"failed","calle_failure":...}` and the same for the rejected dispatch. |
| `/tmp/t210/trees/revert-m2a` | `state = 'failed'` from `abandon_call_run` | `bash supabase/migrations/test-ingest.sh` | exit 1, `the abandoned run read 'completed not_answered false true the call re-fetch failed after 3 attempts'`. |
| `/tmp/t210/trees/revert-m2b` | the synthetic call id guard in `finaliseStep` | `deno test` on `_shared/finalise.ts` | `FAILED \| 50 passed \| 1 failed`, `CALL-E re-fetch returned call_finalise_1 for fixture:call_recorded`, which is the module asking the provider about an id it never issued. |

Each tree holds a copy of `supabase/` and `testdata/`, and a `diff -rq` against
the repository confirms it differs only in the file its finding names.

## Cleanup

Every row this round created is gone. `/tmp/r3e2e/cleanup.sql` deletes the three
scratch profiles, their cascade and the `evt_r3%` rows after each instrument run.
The stack reads its baseline, which is the reading round 2 closed on.

```
profiles=1 auth_users=1 slots=1 call_runs=1 call_events=2 items=4 mentions=3
consents=1 webhook_events=0 transcripts=1 results=1
runs_ids=40000000-0000-4000-8000-000000000001
```

`test-ingest.sh` and the fresh migration runner build and destroy their own
throwaway Supabase projects. The scratch drivers, seeds and revert trees live
under `/tmp/t210`, which is scratch space.

No call was placed. `CALLE_API_BASE` pointed at the stub host throughout. No
secret was echoed and every phone number in this file is masked. No commit was
made.
