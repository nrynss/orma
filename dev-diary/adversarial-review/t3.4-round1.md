# T3.4 round 1 review

## Verdict

**APPROVE**

| C | H | M | L |
| --- | --- | --- | --- |
| 0 | 0 | 0 | 0 |

## Findings

None.

## Independent checks

`deno` was on PATH as Deno 2.9.6.

`deno test --allow-read supabase/functions/_shared/deliver-telegram.ts`
reported 9 passed, 0 failed.

A profile with `telegram_receipts=false` and a live chat id returned
`skipped` with reason `receipts_disabled`. Telegram call count was zero.
`deliveries` length was zero. No POST to `/rest/v1/deliveries`.

A profile with receipts on and `telegram_chat_id=null` returned
`skipped` with reason `no_chat_id`. Again zero Telegram calls and zero rows.

An enabled profile with chat `424242` took a post-call with captures
`Continental` and retirement `book the dentist`. Status was `sent`.
One `deliveries` row had `kind=post_call`, `channel=telegram`,
`call_run_id` set, `sent_at=2026-09-12T12:00:00.000Z`, and `error=null`.
The Telegram body named both texts and contained no `?`.

A Telegram 500 after insert left one row with `sent_at=null` and a non-empty
`error` containing `telegram`. Status was `failed`.

Fixture-shaped `structured_result` from `call-completed.json` yielded
captured text `Continental` and zero retired ids. The formatted summary
included `Continental` and `Retired: none`. Delivery status was `sent`.

Pattern delivery with prose about the dentist set `kind=pattern` and
`call_run_id=null`. The Telegram text carried that prose.

REST doubles posted to `https://orma-api.nryn.dev/rest/v1/deliveries`.
The URL did not contain `supabase.co`. The service role value was absent
from the JSON body.

A successful Telegram send followed by a failing `sent_at` PATCH still
returned `sent`. The row `error` stayed null. That pins send outcome
apart from bookkeeping.

Insert order on the failure path was profile GET, deliveries POST, then
error PATCH. The Telegram call ran after the insert.

## Accepted for this round

The contract note is honest. T3.4 owns only `deliver-telegram.ts`.
T7.4 and T7.3 must call the helpers. Done-when on a live ingested run
waits on those consumers.

Skip with no row for disabled receipts matches the task wording
that the user gets nothing.

Retirement texts are caller-supplied. Structured results only carry
`item_id`. That split is named in the contract file.

Tests live in `deliver-telegram.ts` because that is the T3.4 owns seam.

T3.4 status remains `claimed:orma`. This review does not mark the task done.
