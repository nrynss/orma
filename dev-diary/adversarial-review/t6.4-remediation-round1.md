# T6.4 remediation round 1

| # | What changed | Where | New pin the re-reviewer checks |
|---|--------------|-------|-------------------------------|
| 1 | Restored the live-run briefing update deleted by the T6.4 harness edit, placed back in its original spot between the `item_mentions` insert and the Patterns mood-runs block, keeping the T6.4 fixture block intact: `-- The dispatcher stores the briefing before it places a call. The live run carries one from the real SQL.` plus `update public.call_runs set briefing = public.assemble_briefing('00000000-0000-4000-8000-0000000000c1', '08:00', now()) where id = '4f000000-0000-4000-8000-0000000000c1';` (4 lines, byte-identical to HEAD). | `scripts/p6-local-stack.sh`, `insert_harness_rows`, lines 211-214 | `grep -A3 "dispatcher stores the briefing" scripts/p6-local-stack.sh` shows the update; `bash -n scripts/p6-local-stack.sh` passes; run the stack and count `briefing is not null` true for run `4f000000-0000-4000-8000-0000000000c1`, so T6.1 Today reads the briefing for live@example.com again. |
