# Adversarial review: T5.1, round 1

- Reviewer: RevT51R1
- Date: 2026-09-13
- Subject: uncommitted diff on `supabase/config.toml`, branch `phase-4`. One file, 33 insertions, zero deletions.
- Specification: `dev-diary/PHASE-5-auth-shell.md` task T5.1 and its done-when.
- Verdict: **REMEDIATE**

## Counts

| Severity | Count |
|---|---|
| C | 0 |
| H | 1 |
| M | 1 |
| L | 2 |

## Pin results

Every pin below is a fresh measurement by this reviewer against the live project through `https://orma-api.nryn.dev`. Nothing is quoted from the implementer.

1. **Diff scope.** `git diff --stat` names one file. The diff is a pure addition appended after `[functions.mcp]`. It contains zero deleted lines, so every `[functions.*]` block stays byte identical. Only the `[auth]` area and comments changed.
2. **Schema and reproducibility.** `supabase config diff` (CLI 2.117.0, read-only, project `pyuubklpkhjngiqqwypf`) reports no `different` and no `local_only` entries. Every value the repo declares matches live. It also reports ten `remote_only` entries: live settings the repo never declares. Four sit inside the auth seam and six sit outside it. The claim that the repo file plus named env vars reproduces production therefore fails in ten places. Finding F1 and F3.
3. **Secret hygiene.** `pass = "env(RESEND_API_KEY)"` and `admin_email = "env(RESEND_FROM)"`. A scan of the added lines finds zero `re`-prefixed key shapes and no quoted secret-looking string. The only quoted values are env references, `"Orma"`, the two redirect origins, `"1s"`, `"smtp.resend.com"` and `"resend"`. The sender address resolves to `@send.nryn.dev`, an Orma subdomain on the verified Resend domain.
4. **Done-when, live.**
   - a. `GET /auth/v1/settings` with the anon key answers 200. `external.email` is the only provider on. `mailer_autoconfirm` is false. Anonymous sign-in is off.
   - b. `POST /auth/v1/otp` for the probe address answers 200 in 3.12s. GoTrue fails the request when SMTP fails, so a 200 at this latency evidences a real SMTP submission to Resend. This pin cannot prove delivery, the From header in the delivered mail, or the content. The Resend key is send-only and cannot list messages. The probe address has no MX record by design.
   - c. The admin API minted a magiclink action link with the legacy `SUPABASE_SERVICE_ROLE_KEY` JWT. The link, followed through the front door, redirects once and lands on `https://orma.nryn.dev`. A second link with `redirect_to` swapped to `https://evil.example/callback` is refused and lands on the same site URL. The new-format `SUPABASE_SECRET_KEY` gets 401 on the admin path, as the brief states.
   - d. Rate run A: ten sends at 6.8s spacing, ten 200s, zero 429, 64.2s wall. Rate run B, tightened to fit inside one minute: seven 200s, then 429 on sends 8 to 10 with `over_email_send_rate_limit`. See finding F2.
5. **Redirect list.** Two entries, `https://orma.nryn.dev` and `https://orma.nryn.dev/**`. App origin only, no localhost, no wildcard host, no bare `**`. Glob semantics follow Supabase convention. The bare-origin entry is required because the glob needs the path slash, so it is not redundant. Nothing is broader than the app needs. Live enforcement is measured in pin 4c.
6. **Signup posture.** Both `enable_signup` flags on is read as the declared judge-facing decision. Live values are sane: `enable_confirmations` true, `double_confirm_changes` at the hosted default true, `max_frequency` 1s, `mailer_autoconfirm` false. Magic link therefore acts as the confirmation step and the session is minted at verify.
7. **Probe hygiene.** One probe address, `rev-t51r1@probe.nryn.dev`, a label under a domain with no MX. Eighteen emails were accepted by GoTrue, all to that address. The one probe user was deleted through the admin API and the readback answers 404. `profiles`, `telegram_link_tokens` and `item_mentions` each hold zero rows. The users list held exactly one user before deletion and zero after. No residue.
8. **Doc style of added comments.** Zero semicolons, zero em or en dashes, longest sentence 15 words, active voice throughout.

## Findings

| # | Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|---|
| F2 | H | `supabase/config.toml`, `[auth.rate_limit]` `email_sent = 30`. | The done-when rate pin is hour-state-dependent on this shared project. Thirty emails an hour exhaust across the mandated implement, review and re-review rounds. Once the bucket trips, every auth email is refused until the rolling hour slides, including a real judge magic link. | Run B at 16:51Z: sends 1 to 7 answered 200, sends 8 to 10 answered 429 `over_email_send_rate_limit`. A probe at 17:01Z still answered 429, so the window is rolling, not clock aligned. Eighteen reviewer sends plus about twelve implementer sends filled one rolling hour. | Revert the remediation and the second ten-in-a-minute run in any shared hour re-fails exactly as measured. The block then extends to judge signups for the rest of the hour. |
| F1 | M | `supabase/config.toml`, `[auth]` area, missing declarations. | The repo file does not pin four live auth settings: `auth.email.enable_confirmations` (live true), `auth.email.otp_length` (live 8), `auth.mfa.totp.enroll_enabled` and `auth.mfa.totp.verify_enabled` (live true). A rebuild from this repo flips `mailer_autoconfirm` to true, shrinks the OTP to six digits and drops TOTP MFA. | `supabase config diff` lists these four as `remote_only` with `declared=false`. | Revert the remediation and `supabase config diff` keeps reporting the drift, so any clean-checkout rebuild or fresh push silently changes the auth behaviour this review pinned. |
| F3 | L | Ownership map, six live settings outside every `owns` line. | `db.pooler.default_pool_size`, `db.pooler.max_client_conn`, `storage.analytics.enabled`, `storage.analytics.max_namespaces`, `storage.image_transformation.enabled` and `auth.sms.twilio.enabled` live on the project but no task can declare them. The repo-wide reproduction claim stays false outside the auth seam. | `supabase config diff` lists all six as `remote_only`. They predate this task and no task's `owns` line covers them. | Without an owning task, a rebuild cannot restore those six values and no review round can see the drift close. The contract gap survives every round. |
| F4 | L | `dev-diary/PHASE-5-auth-shell.md`, task T5.1 prose. | The task says "over its HTTP API". The artifact uses Resend's SMTP interface from Supabase auth, which is the only transport GoTrue accepts. The diff comment documents the reasoning correctly. The prose and the artifact disagree. | Task prose line against `[auth.email.smtp]` `host = "smtp.resend.com"` in the diff. | With the prose unchanged, the next implementer reading the task tries to wire an HTTP sender into GoTrue and cannot. |

The orchestrator may close F4 directly under the documentation shortcut. It lives in markdown under `dev-diary/`, touches no code path or configuration value, and confirming the fix needs only reading. The orchestrator may not close F2, F1 or F3 that way.

## Notes, not findings

- The front door answers Cloudflare error 1010 to `python-urllib` client signatures before the request reaches Supabase. Reviewers must pin through curl or a browser client. This is sound bot hygiene and not a defect.
- `/auth/v1/settings` reports `sms_provider: "twilio"` while phone login is off. That is a cosmetic hosted default and matches the undeclared remote flag named in F3.
- The status line `claimed:orma-impl-51` was left untouched per the brief. This review changed no code, no config and no status line.

## Orchestrator dispositions

F4 is closed directly under the documentation shortcut, on 2026-09-13. The
T5.1 task prose in `dev-diary/PHASE-5-auth-shell.md` now names Supabase custom
SMTP speaking Resend's SMTP interface, and notes the port block binds Edge
Functions only. No code path, migration, policy, configuration value or schema
changed. Closed by the orchestrator in the T5.1 landing commit.

F3 is recorded as accepted drift in the T5.1 landing handoff entry, naming all
six remote-only settings. They predate this task and no current task owns them.
Handing them a new `owns` line is a separate contract decision, not a T5.1
remediation.
