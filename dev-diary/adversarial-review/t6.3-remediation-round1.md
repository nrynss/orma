# T6.3 remediation round 1

Verdict in t6.3-round1.md stands. One L finding, fixed below.

| # | Where | What changed | Re-reviewer pin |
|---|---|---|---|
| 1 (L) | `web/src/routes/app/history/+page.ts` lines 9 to 25, 58 to 78, 80 to 89 | Removed the standalone `items(id, text)` fetch and its `HistoryRows` type field. The view never read `rows.items`. Retirement and commitment lines already receive item text through the embedded joins in the `item_mentions` and `commitments` selects, so the loader now fetches exactly what the view reads. | Grep `rows.items`, `itemsRes`, and `from("items")` across `web/src/routes/app/history/`. Expect zero hits outside the `items(...)` embedded join fragments in the two remaining select strings. Confirm the `Promise.all` destructure holds four entries and the returned `rows` object holds no `items` key. |
