# T2.6 remediation, round 2

This remediation addresses the H1 replay-guard residue. It does not deploy the
function or place a call.

| Finding | Remediation | Pin | Mutation covered |
| --- | --- | --- | --- |
| H1 | New events store `capture_pending`. Successful masked logging changes that marker to `captured`. If logging fails, delete remains best-effort. A failed delete leaves `capture_pending`, so a provider replay is allowed to retry capture. A captured event remains a no-op. | `deno test --allow-env supabase/functions/calle-webhook/index.ts` forces capture logging and its DELETE cleanup to fail. It then replays the same event and fails unless the second request captures and returns 200. | Treating every conflict as a completed capture makes a cleanup failure permanently discard evidence. |

Validation also ran `deno check supabase/functions/calle-webhook/index.ts` and
`git diff --check`. Both passed.
