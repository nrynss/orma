# T3.3 remediation round 1

This remediation addresses H from `t3.3-round1.md`. It does not
change the verdict. T3.3 stays `claimed:orma` until round 2 APPROVE lands.

| Finding | Remediation | Pin | Mutation covered |
| --- | --- | --- | --- |
| H | After a successful `insertVoiceItem`, success `editMessage` runs in its own try. On edit failure, `sendMessage` posts the recorded transcript plus Confirm. If that also fails, the helper still returns `captured` and leaves the ack. The outer catch never rewrites to `VOICE_FAIL_TEXT` after an insert. Fail-path `editMessage` is best-effort too. | `deno test --allow-read supabase/functions/telegram/voice.ts`. New test makes the success edit throw. Status is `captured`. Log has no `VOICE_FAIL_TEXT`. Log has a follow-up `send:` of `recordedVoiceReply` with `send-markup:voice_ok:`. `listItemsByUser` still counts one row. Independent probe against the fixed helper matches. | Restoring the single try around insert and edit makes a flaky `editMessageText` look like a failed transcription while the item remains. |
