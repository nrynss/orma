# T8.1 round 4 review

- Reviewer: RevT81R4 (independent reviewer, did not implement or remediate)
- Date: 2026-09-14
- Subject: `scripts/seed.ts` after remediation round 3
- Specification: T8.1 block in `dev-diary/PHASE-8-ship.md`
- Prior rounds: `t8.1-round1.md`, `t8.1-remediation-round1.md`, `t8.1-round2.md`, `t8.1-remediation-round2.md`, `t8.1-round3.md`, `t8.1-remediation-round3.md`

## Verdict

**APPROVE**

| C | H | M | L |
| --- | --- | --- | --- |
| 0 | 0 | 0 | 0 |

Remediation round 3 resolved both remaining findings (F13 and F14). All 14 findings across rounds 1, 2, and 3 now show zero residue. The implementation meets every requirement in the specification.

## Scope

I read `AGENTS.md`, the T8.1 specification, and all prior review and remediation files. I examined the full diff of `scripts/seed.ts` against `main`. I inspected the implementation of `dateTimeInTz`, `buildPlan`, `applyPlan`, `removeSeed`, and `verifyCounts`. I conducted independent testing against a fresh local stack initialized via `scripts/p6-local-stack.sh up`. I modified no application code.

## Independent evidence

Every measurement below reflects my own run against `scripts/p6-local-stack.sh` on port 58321. I tested both `firstrun@example.com` (America/New_York) and `local@example.com` (Asia/Kolkata). The service role key remained in a restricted local file. The stack was torn down cleanly afterward.

1. **Linter.** `deno lint scripts/seed.ts` exits with code 0. It reports zero lint errors.
2. **Baseline.** The fresh database contains 3 profiles, 8 items (4 seeded), 7 runs (0 seed-key), 5 mentions, 2 events, 1 transcript, and 1 result. Slots (3), consents (3), scheduled runs (1), and pattern reports (1) match baseline expectations.
3. **Plan run.** Running `scripts/seed.ts` in plan mode exits with code 0. An independent database query confirmed that row counts across all 15 tracked tables remained unchanged. The plan run writes exactly zero rows.
4. **First apply on `firstrun@example.com`.** The command seeds 3 items, 3 runs, 5 mentions, 9 events, 3 transcripts, 3 results, and 4 commitments. All other tables remain unaffected.
5. **Idempotence.** Running `--apply` a second time leaves identical row counts across all tables.
6. **Apply with non-today demo date (F13 check).** Running `--apply` with `--demo-date 2026-09-20` succeeds cleanly. The verification RPC evaluates at the demo date timestamp. The lead line includes both "3 times" and "34 days".
7. **Apply on `local@example.com`.** Seeding the local account creates 3 items and 3 runs with timestamps tailored to Asia/Kolkata.
8. **Rollback on `local@example.com`.** Running `--remove` on `local@example.com` leaves zero seeded items and zero seeded runs. All 4 original items from `supabase/seed.sql` remain completely intact.
9. **Briefing assembly.** An independent SQL call to `assemble_briefing` for `firstrun@example.com` returns `lead_line` "You've mentioned the dentist 3 times. It's been 34 days."
10. **Pattern computation.** `compute_pattern_facts` over the 7-day window ending yesterday (`2026-09-07` to `2026-09-13`) returns `observed_days = 3` and `sufficient_history = true`.
11. **Timezone independence.** I tested `dateTimeInTz` under 7 host timezones (Asia/Kolkata, UTC, America/New_York, Pacific/Auckland, Europe/London, Pacific/Honolulu, Asia/Tokyo). New York 08:00 consistently evaluated to `2026-09-11T12:00:00.000Z`.
12. **Briefing history accuracy (F8 check).** Seeded runs store lead lines "Everything open is still current", "It's been 30 days", and "It's been 31 days". Each lead line reflects rows and age present before dispatch.
13. **Dispatched event request body (F9 check).** Each `dispatched` event contains the full reviewed CALL-E task template with "Hello. This is Orma." and all 7 steps. Phone numbers are masked (`+155555XXXXX`), and the webhook URL dynamically uses `ORMA_API_URL` with a redacted path.
14. **Captured item timestamp (F10 check).** Weekend groceries has `created_at` matching run 3 `completed_at` (`2026-09-11 12:02:00+00`). The comparison `created_at <= completed_at` holds true.
15. **Ingested mention count alignment (F11 check).** Ingested and finalised event mention counts match `results.structured` entry counts (1, 2, 2) and actual mention rows (1, 2, 2) across all runs.
16. **Disclosure active voice (F12 check).** The disclosure paragraph uses active voice throughout. It contains zero passive clauses. Every sentence has 30 words or fewer.
17. **Non-local safety guard.** Invoking `--apply` against `https://orma-api.nryn.dev` without `--i-confirmed-dry-run` terminates with exit code 1. Passing `--i-confirmed-dry-run` successfully bypasses the guard.
18. **User pagination (F5 check).** I inserted 55 dummy users so that target accounts sat on page 2. `lookupProfile` successfully paginated to page 2 and resolved the profile.
19. **Item retirement survival (F1 check).** Retiring the dentist in SQL and adding a null-run mention survived a subsequent `--apply`. The item preserved `retired` status, `retired_reason`, and all 4 mentions.
20. **Final rollback on `firstrun@example.com`.** Running `--remove` deleted all seeded rows. The account's 2 non-seeded items survived. Table counts returned to exact baseline.
21. **Clean teardown.** Executing `scripts/p6-local-stack.sh down` stopped all services. Zero `orma-p6-local` containers remain.

## Residue against prior rounds

| ID | Prior claim | Residue | Evidence |
| --- | --- | --- | --- |
| F1 | Rerun preserves item status and non-seed mentions | Zero | Evidence 19 |
| F2 | Runs at demo-6, -4, -3 yield 3 observed days | Zero | Evidence 10 |
| F3 | Transcript mention offsets match spoken turns | Zero | Verified in SQL |
| F4 | Stored briefing and dry-run dispatcher request body | Zero | Evidence 12, 13 |
| F5 | User lookup paginates `auth.admin.listUsers` | Zero | Evidence 18 |
| F6 | Groceries source is `call` with matching timestamps | Zero | Evidence 14 |
| F7 | `dateTimeInTz` is invariant to host machine timezone | Zero | Evidence 11 |
| F8 | Briefing lead lines backed by prior rows at dispatch | Zero | Evidence 12 |
| F9 | Full CALL-E task template and dynamic webhook URL | Zero | Evidence 13 |
| F10 | Captured item `created_at <= completed_at` | Zero | Evidence 14 |
| F11 | Ingested mention count matches structured entries | Zero | Evidence 15 |
| F12 | Disclosure paragraph written in active voice | Zero | Evidence 16 |
| F13 | `verifyCounts` evaluates `assemble_briefing` at demo date | Zero | Evidence 6 |
| F14 | `removeSeed` contains zero unused variables | Zero | Evidence 1 |

## Findings

Zero findings across all severities.

| ID | Sev | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- | --- |
| - | - | - | Zero findings. | - | - |

## Notes

- The stack was torn down cleanly. Zero containers remain.
- All sentences in this document contain 30 words or fewer. I used no semicolons and no em dashes.
