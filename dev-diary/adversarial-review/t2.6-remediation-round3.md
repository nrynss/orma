# T2.6 remediation, round 3

This remediation addresses C1 and H1 from round 3. It does not deploy the
function or place a call.

| Finding | Remediation | Pin | Mutation covered |
| --- | --- | --- | --- |
| C1 | Object keys are discarded. Each object becomes a fixed redaction label containing its masked values, so only nesting and arity remain. | `deno test --allow-env supabase/functions/calle-webhook/index.ts` supplies phone and email strings as JSON keys, then fails if either remains in capture evidence. | Restoring provider keys exposes unknown caller data in logs. |
| H1 | Every delivery atomically patches `capture_pending` to `capture_claimed`. Only the patch returning one row may write capture evidence. Failed logging returns the claim to pending. | The same test command runs two synchronized duplicate requests. It fails unless both return 200 and exactly one invokes capture. | Restoring a read before capture allows concurrent retries to log duplicate evidence. |

Validation ran `deno test --allow-env supabase/functions/calle-webhook/index.ts`,
`deno check supabase/functions/calle-webhook/index.ts`, and `git diff --check`.
All passed.
