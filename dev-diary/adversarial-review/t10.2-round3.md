# T10.2 review, round 3

**Verdict:** REMEDIATE

**Counts:** C 0, H 1, M 0, L 0

## Scope and evidence

I reviewed the T10.2 specification, both earlier review rounds, both
remediations, and every owned implementation path. `deno check` passed for
`confirm-phone` and `calle-webhook`. The shared CALL-E and webhook tests passed
89 checks. `git diff --check` passed.

I independently mutated the post-call persistence recovery. I used a completed
authoritative callback after the profile changes from A to B and back to A.
The callback updates the expired A confirmation to `dialled`, which makes its
pre-change code eligible again.

## Findings

| Where | What | Pin | Mutation |
| --- | --- | --- | --- |
| `supabase/functions/calle-webhook/index.ts:272-285` | **H1: The post-dispatch recovery webhook revives a confirmation that a phone change expired.** The repair PATCH filters only by confirmation id. Start confirmation for A, change the profile A to B, then B to A. The T10.1 trigger correctly marks the row expired. A delayed completed callback from the original CALL-E call then writes `state = dialled` without checking its prior state or number generation. `verify_phone_code` selects that live A row and confirms a code issued before the change. This reopens round-one H1. | On the local stack, create a requested A confirmation and retain its valid code. Update the profile A to B and B to A through the authenticated route. Deliver the authoritative completed callback carrying its `phone_confirmation_id`. Query the row and call `verify_phone_code` with the original code. It must remain `expired`, with `confirmed_at` and `profiles.phone_confirmed_at` null. The current PATCH changes it to `dialled`, so the old code confirms. | Remove the recovery PATCH state predicate or its profile-generation check. The A to B to A delayed-webhook pin revives the stale confirmation and confirms its original code. |

## Zero residue

Round-one M1 has zero residue. The dry-run record masks the code, destination,
and webhook secret before it is stored.

Round-two H1 has zero residue for the narrow lost persistence mutation. A
completed authoritative callback can bind the provider call id and restore a
row whose first post-dispatch patch failed.

Round-one H1 has residue. The round-two recovery can overwrite the trigger's
`expired` state after the A to B to A invalidation. H1 above is the same
changed-number defect on the delayed callback path, not a new defect.
