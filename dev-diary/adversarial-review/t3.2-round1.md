# T3.2 round 1 review

## Verdict

**APPROVE**

| C | H | M | L |
| --- | --- | --- | --- |
| 0 | 0 | 0 | 0 |

## Findings

None.

## Independent checks

`deno` was not on PATH. Deno 2.9.6 was installed only for this review.

`deno test --allow-read supabase/functions/telegram/capture.ts` reported 18 passed, 0 failed.

Eleven of those tests register because `capture.ts` imports `link.ts`. Seven tests live in `capture.ts`.

A memory store with chat `1001` bound to user A and chat `2002` bound to user B took one plain message on chat `1001`.

`listItemsByUser` for user A returned one row. The row `userId` was user A. `source` was `telegram`. User B still had zero rows.

The inbound text had padding. The stored `text` was the trimmed value. The reply equalled `recordedCaptureReply` of that stored value, not the padded inbound string.

An unlinked chat with a thought returned status `unlinked`. The reply equalled `START_LINK_PROMPT` from `link.ts`. `listItemsByUser` counted zero rows.

An unlinked chat with whitespace-only text still returned `START_LINK_PROMPT`. A linked `/start` returned status `ignored` and inserted zero rows.

A REST double posted `https://orma-api.nryn.dev/rest/v1/items`. The URL did not contain `supabase.co`. The JSON body set `source` to `telegram` and `user_id` to the linked profile. `Authorization` was `Bearer` plus the service role value. That value was absent from the body. `Prefer` was `return=representation`.

The REST GET was `https://orma-api.nryn.dev/rest/v1/profiles?telegram_chat_id=eq.1001&select=id,telegram_chat_id`.

`rg captureTextMessage supabase/functions/telegram/index.ts` matched nothing.

`git show --name-only aacc6e6` listed `supabase/functions/telegram/capture.ts` and `dev-diary/adversarial-review/t3.2-contract-change.md`. `index.ts` was not in the commit.

T1.2 `items_owner` uses `auth.uid() = user_id`. The webhook has no user JWT. Spec §7 says functions use the service role and bypass RLS. The rest store sends that key.

## Accepted for this round

The contract note is honest. T3.2 owns only `capture.ts`. T3.1 still must call `captureTextMessage` and load `SUPABASE_SERVICE_ROLE_KEY`.

Done-when insert, quoted reply, and unlinked prompt hold on the helper. They do not hold on a live text update until T3.1 calls it. That gap is the named contract change, not a silent owns breach.

A REST double that returns another user's profile still inserts under that id. The GET already filters `telegram_chat_id=eq.<chat>`. PostgREST honoring that filter is the pin, not a lying mock.

If `insertItem` returns different `text` than the write, `captureTextMessage` throws after the row exists. `items.text` has no mutating trigger.

A linked thought that starts with `/` is `ignored`. The task treats leading slash as a command.

`fromRestItem` throws when `listItemsByUser` sees a non-telegram `source`. `captureTextMessage` does not call that list on the capture path.

Tests live in `capture.ts` because that is the T3.2 `owns` seam.

T3.2 status remains `claimed:orma`. This review does not mark the task done.
