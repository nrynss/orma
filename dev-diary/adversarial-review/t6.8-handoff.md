# T6.8 handoff: Landing page

Implementation role. This work is ready for independent review.

## What exists now

`/` stays server rendered with no dependency on sign-in state. It renders identical content for signed-out and signed-in visitors. It keeps the thesis lede, the beat exchange, and the two sections under the existing local style block. It adds no token import and no static asset.

I removed the closed sign-ups line. The footer now invites sign-up through the login page and links `/login`. A Get started section above the footer links `/login` too, so the page itself carries the way in.

A Synthetic demo panel sits between the thesis sections and the way in. It carries the subline "No real person, no real call. Not production evidence." It replays the dentist exchange as a list and offers Play synthetic audio plus Stop. Play speaks the three lines in turn through SpeechSynthesis. Orma lines use the default voice and the You line uses a higher pitch. The active line highlights while it speaks and the state resets at the end. SSR and browsers without SpeechSynthesis render a disabled button with a calm note instead. Teardown cancels any queued speech.

No phone number appears on the page or in this note. No live call went out.

## Validation

Static only. This batch ran no npm command, no vite build, no docker container, and no local stack.

- `git diff --check` passed.
- `git status --porcelain` shows only `M web/src/routes/+page.svelte` plus this handoff. It shows no new file under `web/static`.
- A case-insensitive grep for `ssr`, `auth`, `session`, and `Not yet open` across the page returns zero matches.
- A read of lines 1 through 65 confirms tabs, Svelte 5 runes, and zero semicolons in the TypeScript block.

## Reviewer focus

Curl `/` and pin three strings in the response. The thesis lede "A daily two-minute accountability call" must appear. The label "Synthetic demo" must appear with its subline. The closed text "Not yet open for sign-ups" must not appear.

Pin the no-asset constraint with `git status`. It must show no new file under `web/static`.

Pin the SSR guard by reading the script block. `window` appears only inside event handlers and lifecycle hooks. The first render uses `supported = false`, so SSR emits the disabled button and the calm note.
