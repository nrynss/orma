# T2.3 remediation, round 2

This remediation addresses H1 from `t2.3-round2.md`. It does not alter the
round-two verdict or the task status.

| Finding | Remediation | Validation | Mutation covered |
| --- | --- | --- | --- |
| H1 | The runner invokes the second independent `psql` session through Python. `time.monotonic()` enforces a 750 ms deadline. The whole-second wall-clock subtraction is removed. | `bash supabase/migrations/test-tick-claims.sh` passed against a fresh PostgreSQL-only stack. It returned zero before the deadline. It then counted one claimed row. | I changed `FOR UPDATE SKIP LOCKED` to `FOR UPDATE`. The runner failed at 0.753 s. I restored `SKIP LOCKED`. |
