# T9.2 round 2 adversarial review: the shell and navigation

Fresh reviewer. I implemented nothing in T9.2, I reviewed nothing in round 1 and I fixed nothing. The
round 1 review, the remediation and the handoff come from other agents. Every number below comes
from the running app, never from the remediation text.

Tree reviewed: HEAD `e7b596f`, with the T9.2 delta uncommitted on top. The remediation delta is two
files, `web/src/lib/shell/TabBar.svelte` and `web/src/lib/shell/AccountMenu.svelte`. This round
verifies that delta and runs a compact sweep over the surfaces the fix could disturb. It does not
repeat the round 1 sweep.

The central gate is taken as given: `npm run check` at 343 files with 0 errors and 0 warnings, and
`npm run build` at exit 0. I ran neither.

The shared `orma-p6-local` stack started under another agent just before I began, restarted while I
worked, and was gone by the time I finished. So I ran an isolated copy the way the remediation did.
The copy uses the same script, the same 17 migrations, the same `supabase/seed.sql` and the same
harness rows. It differs in two locked values, project id `orma-p6-t92r2` and ports 58350 to 58359.
The dev server ran on port 5217 and I signed in as `local@example.com`. I tore my stack down and
confirmed zero `orma-p6-t92r2` containers.

## Verdict

**APPROVE.**

| Severity | Count | Meaning |
|---|---|---|
| C | 0 |  |
| H | 0 |  |
| M | 0 |  |
| L | 0 |  |

Zero findings against T9.2. Both prior findings are closed by measurement. One real defect
reproduces on a page T9.2 does not own, and I record it below outside these counts.

## What I measured, and how

| Claim | Measurement |
|---|---|
| F1 closed | All six app pages at 320, 360 and 412 in light, reading every `.o-tab-label` `clientWidth` against `scrollWidth`, plus the cell width, the bar height and the tab height |
| The Timeline label at 320 | The label's integer box, its fractional rendered box, the tab content box and the label weight |
| The current cue | Exactly one rendered `aria-current="page"` per page, the `::before` computed content on the current tab and on the other five |
| The rule | The `::before` computed height, width, top, position and background, against the bar's computed background |
| The cue without colour | A 3x screenshot of the bar at 320 with `filter: grayscale(1)` on the root element |
| Both schemes | `/app/timeline` at 320, 360 and 412 under `prefers-color-scheme: dark`, and colour screenshots of the bar in each scheme |
| F2 closed | `DOMDebugger.getEventListeners` over CDP on the document node, counting `pointerdown` at each step of an open, press and close sequence |
| The outside press | A real `Input.dispatchMouseEvent` press at (8, 120), with `elementFromPoint` naming the element under it |
| The unmount | A client side navigation to `/app/onboarding` while the panel was open, proved by a window mark that survived |
| The crossover | Rendered `nav` boxes, rendered `aria-current` nodes and shell target heights at 899 and 900 |
| Horizontal scroll | Document `scrollWidth` against `clientWidth` on the six pages at 320 and 360, and the tab bar's own pair |
| The wordmark | Each image `src`, `naturalWidth`, computed `display` and rendered box at `/app` 320, 360, 412 and `/` 320 in both schemes, then the data URL decoded and compared with both files |

## Findings

No finding is open against T9.2.

| Where | What | Pin | Mutation |
|---|---|---|---|
| None |  |  |  |

## Carried defect, outside T9.2

One row, with the same four columns. I do not count it against T9.2 and I do not raise it as a new
T9.2 finding. The file belongs to T9.7. Round 1 recorded the row as unresolved for want of pattern
rows, and the remediation reproduced it and also declined a finding. My stack holds the harness
pattern rows, so the reproduction is now complete.

| Where | What | Pin | Mutation |
|---|---|---|---|
| `web/src/routes/app/patterns/+page.svelte` (severity M if it is counted, owner T9.7) | At 320 and at 360 the Patterns page scrolls sideways once the mood trend draws | Document `scrollWidth` **366** against `clientWidth` **320** at 320, and **366** against **360** at 360. Hiding the page's 342 pixel trend SVG returns `scrollWidth` to the client width. The tab bar's own `scrollWidth` equals its `clientWidth` at 320 | Leave it and a phone at 360 scrolls the Patterns page sideways |

The remediation delta cannot cause it. Both changes live in `TabBar.svelte` and `AccountMenu.svelte`.
One drops a font weight, which narrows the bar. The other adds an absolutely positioned 3 pixel rule
inside a grid cell, which adds no width. The overflowing node sits in `section.chart` inside the page
itself.

## M1 closed: the tab label fits and the cue is not colour alone

I swept all six app pages at 320, 360 and 412 in light. That is 18 cases. Every visible
`.o-tab-label` reported `scrollWidth` equal to `clientWidth` and no label clipped.

| Width | Cell | Timeline label cw / sw | Widest label | Clipped | Rule | Bar height | Tab height |
|---|---|---|---|---|---|---|---|
| 320 | 53.33 | 49 / 49 | 49 | 0 | 3 by 53.33 | 57 | 56 |
| 360 | 60 | 49 / 49 | 49 | 0 | 3 by 60 | 57 | 56 |
| 412 | 68.66 | 49 / 49 | 49 | 0 | 3 by 68.67 | 57 | 56 |

On `/app/timeline` at 320 the current label reads `clientWidth` **49** against `scrollWidth` **49**
at weight **500**. The rendered label box is **49.14** pixels inside a **51.33** pixel content box,
which leaves 2.19 pixels of slack. The tab keeps 1 pixel of side padding inside a 53.33 pixel cell.

The current cue is a set, and only one part of it is colour. The tab carries `aria-current="page"`,
the brand label colour and a 3 pixel rule above its icon. Exactly one rendered tab per page carries
the attribute. Exactly one carries the rule. The other five report `::before` content `none` in all
18 cases.

The rule renders through the whole cell width and 3 pixels tall, positioned at the top edge. The
computed `inset` at 320 reads `0px 0px 53px`, so the rule adds no layout width. The bar measures 57
pixels in every case, which matches the pre-fix height.

Contrast of the rule against the bar surface is **8.65 to 1** in light, `rgb(111, 43, 127)` on
`rgb(250, 250, 252)`, and **8.92 to 1** in dark, `rgb(217, 166, 234)` on `rgb(25, 23, 35)`. Both
clear the 3 to 1 floor for a non-text control.

The greyscale screenshot at 320 shows the rule as a plain dark bar over the Timeline cell, and the
Timeline label and icon read darker than the rest. The colour screenshots show `Timeline` whole in
both schemes with the rule above its icon.

Under `prefers-color-scheme: dark` the same three widths report `clientWidth` 49 against
`scrollWidth` 49, weight 500, no clipped label and the same rule geometry.

## L1 closed: the panel closes outside and the listener count returns to zero

I counted `pointerdown` listeners on the document node with `DOMDebugger.getEventListeners` over
CDP. The count comes from the browser, so no page code reports on itself. The document carries
`click` and `visibilitychange` before the panel ever opens.

| Step | `aria-expanded` | Panel rendered | Document `pointerdown` listeners |
|---|---|---|---|
| Closed baseline | false | no | **0** |
| Opened by the trigger | true | yes | **1** |
| Press at (8, 120), outside the block | false | no | **0** |
| Press inside the panel | true | yes | **1** |
| Closed by the trigger | false | no | **0** |
| Escape | false | no | **0** |
| Tab click, path becomes `/app/items` | false | no | **0** |
| Client side navigation to `/app/onboarding` | no trigger | no | **0** |

The element under (8, 120) is `main.timeline`. It is neither a link nor part of the account block,
so the press proves the new listener rather than a navigation.

The inside press keeps the panel open, which is the half that protects the trigger's own toggle and
the Sign out action. The last row is the unmount path. A window mark set before the move survived
it, so the component unmounted inside one document and the effect cleanup removed the listener.

## Compact regression sweep

| Surface | Measurement | Result |
|---|---|---|
| Bar height | `.o-tabbar` box at 320, 360 and 412 on all six pages | 57 in all 18 cases, unchanged |
| Tab target height | `.o-tab` box, same cases | 56 in all 18 cases |
| One navigation at 899 | Rendered `nav` boxes on `/app` | 1 of 2, shell `display: flex`, tab bar `grid` |
| One navigation at 900 | Rendered `nav` boxes on `/app` | 1 of 2, shell `display: grid`, sidebar 248, tab bar `none` |
| The current page at both widths | Rendered `aria-current="page"` nodes | 1 at 899 and 1 at 900, both reading Today |
| Shell targets at both widths | Rendered nav and account targets | 8 targets, 44 to 56 at 899, all 44 at 900 |
| Horizontal scroll at 320 | Document `scrollWidth` against `clientWidth` | Equal on five of six pages, see the carried defect |
| The wordmark in both schemes | `src`, `naturalWidth` and `display` at `/app` 320, 360, 412 and `/` 320 | Exactly one image shows per scheme, and it is the file that scheme owns |

The wordmark check decodes the displayed data URL and compares it with both repo files, collapsing
whitespace and normalising quotes, which is all the dev server changes. The shown image equals the
light file in light and the dark file in dark. Each image reports a 480 by 200 natural size and a 77
by 32 rendered box. Both `src` values stay the same in both schemes, so nothing recolours them.

The 20 pixel targets I met on `/app` at 899 are page content links such as `Read the transcript`.
The shell's own smallest target is 44. That row is already recorded under T9.5 to T9.8.

## Residue
Zero residue remains against round 1. Both of its findings are closed, and every other residue row
it recorded still holds. Row 3 is the one that changed, because this round reproduced it.


1. **M1, the clipped tab label at 320. Closed.** Timeline at 320 reads 49 against 49 in light and in
   dark, no label clips on any of the six pages at 320, 360 or 412, and the rule carries the cue at
   8.65 to 1 in light and 8.92 to 1 in dark.
2. **L1, the panel that ignored an outside press. Closed.** The outside press closes the panel, and
   the document `pointerdown` count returns to 0 after the outside press, after the trigger, after
   Escape, after a tab click and after an unmount.
3. **The Patterns overflow.** Carried, not T9.2, owner T9.7. Reproduced with a pin this round.
4. **The operator's phone walk stays owed.** Contract change row 5 records it and I ran no phone.
5. **The desk content column.** T9.5 to T9.8 own the 608 pixel max width inside the 1032 pixel area.
6. **The landing and login pages still print Orma as text.** T9.3 owns both files.
7. **Page content links measure 20 to 22 pixels.** Outside the shell, owned by T9.5 to T9.8.
8. **The retired `var(--o-` references.** The page files own the rest, per contract change row 3.
9. **The harness note.** My stack had `orma-p6-local` cycling under me, so I left it alone and ran
   my own copy. That stack held no containers at all when I finished.

## What this small round did not re-verify

These surfaces did not change, so the round 1 measurements still stand for them.

- The artwork bytes and their hashes. Round 1 compared them with the operator's originals. I checked
  only which file each image resolves.
- The accessibility tree, the accessible name of the brand link, and JavaScript disabled.
- The safe area inset, the bar's padding under it, and the bottom clearance under page content.
- Focus visibility and the focus ring on every nav target.
- Signed out routing for the five `/app` paths.
- Layout above 900 pixels beyond the single nav count at 900.
- The built worker and the built chunks.
- Every page level surface, including the wide content column, the landing hero, the prose and the
  `/dev/ui` gallery.
- The before columns in the remediation text. I measured the fixed tree alone, and I changed no
  implementation to reproduce a pre-fix state.

## Done when

**T9.2 done when:** every `/app` page shows exactly one navigation with the current page marked, the
tab bar works at 360 pixels and on the operator's phone, no content hides behind the tab bar,
keyboard focus is visible on every nav target, and signed-out routing is unchanged.

Re-verified this round: one navigation with exactly one marked current page at 899 and 900, six
whole tab labels with no clipping at 320, 360 and 412, tab targets at 56, and shell targets at 44 or
more at 899 and 900. Standing on round 1: the bottom clearance, the focus rings, the signed-out
routing and the accessibility tree.
Owed to the operator: the phone walk. One phase level must-not-change row fails on the Patterns page.
T9.7 owns that page and the row above records it.
