# T2.2 remediation, round 1

| Finding | Remediation | Pin | Mutation |
| --- | --- | --- | --- |
| H1 | `assemble_briefing` now converts fallback `items.created_at` into `profiles.timezone` before taking its date. User-provided `since_date` remains a local date. | `bash supabase/migrations/test-briefing.sh` creates an `Asia/Kolkata` item at `2026-08-13 20:00:00+00` and queries at `2026-09-12 00:00:00+00`. The returned lead remains `Everything open is still current.`, proving the age is 29 days. | Restoring a UTC date for `created_at` promotes items across a local midnight boundary at the 30-day threshold. |
| H2 | The prior summary now comes only from `transcripts.raw ->> 'summary'` on the latest completed call. The contract records that T2.7 must preserve the completed provider response there. | The isolated runner inserts a completed run and its provider summary into `transcripts.raw`. It asserts that `last_call_summary` returns the stored provider text. | Returning to confidence metadata or prior briefing input yields an empty or stale summary. |
| H3 | Added `supabase/migrations/test-briefing.sh`, an isolated database acceptance runner. It starts PostgreSQL only, applies the core and briefing migrations, and calls the RPC against fixture rows. | The runner asserts the local-date boundary, the verbatim four-mention and 34-day lead, the durable summary, `open_count`, two-fact cap, and `service_role` grant. `deno test --allow-read supabase/functions/_shared/briefing.ts` also passes all renderer and storage checks. | Removing SQL ordering, age conversion, mention counting, summary retrieval, cap, or grant makes the database runner fail while a fetch stub cannot observe it. |
| M1 | The lead now expands qualifying items into ordered facts and limits the facts themselves to two. | The isolated runner creates two items that are overdue, repeated, and aged. It asserts the lead is exactly `first item is overdue. You've mentioned first item 3 times.` | Reverting to item-only limiting emits all six facts for two qualifying items. |
