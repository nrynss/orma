# T10.1 handoff: Server-owned phone confirmation

## What landed

The onboarding request no longer writes `phone_confirmed_at`. The database trigger clears browser-set confirmation values and clears confirmation when the phone number changes.

Service-role updates remain able to write the field. T10.2 must set `orma.phone_confirmation_write` locally before its security-definer verifier confirms a number.

## Validation

- The onboarding capture check proves the profile request omits the field.
- A rollback-only local database probe proves authenticated inserts and updates cannot self-confirm.
- The same probe proves service-role confirmation writes succeed.
- `npm run check` passed in `web/`.
- The CALL-E shared-module tests passed, 50 tests.

## Review

Round 1 approved with zero findings across all severities. See `t10.1-round1.md` for independent pins.
