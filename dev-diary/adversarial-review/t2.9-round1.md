# T2.9 review, round 1

**Verdict:** REMEDIATE

**Counts:** C 1, H 0, M 1, L 0

`deno test --allow-env --allow-net supabase/functions/_shared/events.ts` reports
two passing mocked-request tests. `git diff --check` passes. Neither check
observes the timeline after a call-run transition.

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| C | `supabase/functions/_shared/events.ts:56-76`, `supabase/functions/materialise/index.ts:202-224`, `supabase/functions/tick/index.ts:123-145`, `supabase/functions/_shared/calle.ts:216-287` | `recordCallEvent` has no production caller. Materialisation, claiming, and the current dispatch seam change `call_runs` without inserting their required `materialised`, `claimed`, or `dispatched` rows. A completed run therefore has no ordered story, so the task's demo-critical done condition is absent. T2.9 owns only the helper, while every transition owner must import it. Record that cross-seam contract before integrating it. | In an isolated database, materialise one due slot, atomically claim its run, then dispatch it with the CALL-E completed fixture. Query `call_events` by `call_run_id, at, id`. The result must contain `materialised`, `claimed`, and `dispatched` in that order. It returns zero rows today because no production module imports `recordCallEvent`. | Reverting any transition integration leaves that stage invisible, so an operator cannot reconstruct a completed call or locate a failure. |
| M | `supabase/functions/_shared/events.ts:56-75` | The helper accepts `{}` for every event kind. Its comment asks callers to supply a provider identifier, state, failure reason, or durable observation, but the executable contract does not require one. A failed run can record `finalised` with no state or failure reason, which does not name where it stopped as required. | Call `recordCallEvent("run-1", "finalised", {}, deps)` against a recording fetch implementation and inspect the POST body. It succeeds and stores `"detail":{}` today. Add a kind-specific detail contract and a test that rejects an opaque terminal event. | Reverting payload validation permits opaque terminal rows that force an operator back to invocation logs to diagnose a failed run. |

There is no residue from an earlier T2.9 review. This is round 1.
