# T2.3 review, round 1

**Verdict:** REMEDIATE

**Counts:** C 1, H 1, M 0, L 0

`deno test --allow-env --allow-net supabase/functions/tick/index.ts` passes its
three unit tests. The tests do not exercise the PostgreSQL function or scheduler.

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| C | `supabase/migrations/20260912161000_tick.sql:38-56`, `supabase/functions/tick/index.ts:47-50,123-145` | The migration enables the minute job now. Every claimed run reaches the injected `dispatchClaimed` placeholder and fails. The handler returns 500 after the SQL claim committed. The run stays `claimed`, which the RPC never selects again. This loses the scheduled call before T2.4 exists. Do not schedule tick until dispatch wiring can advance claimed runs. | Apply the migration in an isolated Supabase database. Insert one due scheduled `call_runs` row. Configure the scheduler key, then invoke `fn:tick`. The response is 500. A direct `select state from call_runs where id = <run>` returns `claimed`. A second tick must not claim it. This fails today because `unavailable("dispatch")` throws after `claim_due_call_runs` commits. | Reverting the guard permits the scheduler to strand due calls whenever dispatch is absent or disabled. |
| H | `supabase/functions/tick/index.ts:167-188` | The concurrent-claim test is a fetch stub with its own `available` flag. It proves only that the stub returns one row. It cannot pin the SQL statement, transaction scope, function grants, or RPC execution. | Add an isolated database test. Apply `20260912161000_tick.sql` and insert one due scheduled row. Start two database sessions that call `public.claim_due_call_runs(20)` concurrently. Count claimed rows afterwards. Assert one returned row across both sessions and a single `claimed` row. No database-backed test exists today. | Reverting `FOR UPDATE SKIP LOCKED`, separating selection from update, or breaking the function grant still leaves TypeScript tests green. |

There is no residue from an earlier T2.3 review. This is round 1.
