# T2.1 review, round 2

Verdict: REMEDIATE

| Severity | Count |
| --- | ---: |
| C | 1 |
| H | 0 |
| M | 0 |
| L | 0 |

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| C | `supabase/migrations/20260912150000_schedule_materialise.sql:7-11`, `supabase/config.toml` | The new cron job sends the opaque `sb_publishable_...` API key as `Authorization: Bearer ...`, while `materialise` has no function-specific `verify_jwt = false` configuration and no handler-level API-key authentication. `verify_jwt` is therefore enabled. The scheduled POST fails platform JWT verification before `createMaterialiseHandler` runs, so production does not materialise runs. Disabling verification alone would make a service-role-writing endpoint public, so remediation must authenticate the scheduler request in the handler with a dedicated secret or the supported API-key mechanism. | Read `supabase/config.toml`. It configures only `functions.telegram`, so Materialise retains the default `verify_jwt = true`. The scheduler command sends `Authorization: Bearer sb_publishable_...`. The current Supabase authorization-header documentation states that a publishable key is not a JWT and that API-key service calls need `verify_jwt = false` plus handler authorization. A scheduled `pg_net` POST should return 401 before the handler. | Restore the bearer publishable key or remove the protected handler authentication. The nightly job again fails or an unauthenticated internet caller can materialise and delete future scheduled runs with the function's service role. |

## Independent checks

- `deno test --allow-env --allow-net supabase/functions/materialise/index.ts` passed all five tests. They independently cover the Asia/Kolkata instant, repeated and skipped DST times, durable-key duplicate suppression, and the inactive-slot deletion predicate.
- `git diff --check` passed.
- The cron expression is `10 0 * * *`, which is 00:10 UTC. Its named `cron.schedule` call is repeat-safe with installed `pg_cron` 1.6.4, which updates an existing named job rather than adding a second job.
- The inactive predicate remains `slot_id`, `state=scheduled`, and strictly future `scheduled_for`. It does not delete claimed, dispatched, terminal, or already-due rows.

## Prior-finding residue

Round-one C1 is resolved at the configuration-text level. The tracked named cron job now has the required UTC schedule and endpoint. It has not resolved the end-to-end scheduling requirement because the new request's authentication cannot reach the handler.
