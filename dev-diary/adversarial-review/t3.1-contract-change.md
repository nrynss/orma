# T3.1 contract change request

T3.1 owns `supabase/functions/telegram/index.ts` and
`supabase/config.toml` `[functions.telegram]`. That block sets
`verify_jwt = false` so Telegram POSTs reach the path secret.

T5.1 still owns only the auth block of `config.toml`. Magic-link work
must not change `[functions.telegram]`.

The handler reads `TELEGRAM_BOT_TOKEN`, `TELEGRAM_WEBHOOK_SECRET`, and
`ORMA_API_URL` from the function environment at runtime. Those names match
GitHub secrets and `scripts/bootstrap-env.sh`. This task does not commit values.

Boot and GET register `setWebhook` using `ORMA_API_URL`. They never use a
`*.supabase.co` host.

| Where | What | Pin | Mutation |
| --- | --- | --- | --- |
| `supabase/config.toml` `[functions.telegram]`, owned by T3.1 | Telegram posts webhooks with no JWT. A CLI deploy that still verifies JWTs never reaches the path-secret check. | `test -f supabase/config.toml` exits 0. The file sets `[functions.telegram] verify_jwt = false`. A gateway 401 JSON body on `POST /functions/v1/telegram` with no Authorization header, instead of this function's empty 401, means JWT is still on. | `@orma_tele_bot` cannot deliver updates. Live secret rejection cannot be pinned. |
