# T6.8 round-1 review: Landing page

Reviewer role. I implemented nothing. I fixed nothing. I measured the page as a signed-out visitor would meet it.

Verdict: APPROVE.

Counts: C 0, H 0, M 0, L 0.

## Pins

All pins come from the live dev server on port 5202 and from reads of the file. I started the server with the exact form `cd web && env $(../scripts/p6-local-stack.sh env) npm run dev -- --port 5202`. Curl through `localhost` returned HTTP 200 with 10415 bytes. Curl through `127.0.0.1` refused the connection while `localhost` answered, so Vite bound the loopback by name in this environment. That quirk sits outside the page. No cookies went out, so the request read as signed out.

Response body, saved and counted:

| String | Count | Result |
|---|---|---|
| `A daily two-minute accountability call` | 2 (lede plus meta description) | present |
| `href="/login"` | 3 (shell Sign in, Get started, footer) | clear way in |
| `Not yet open` case-insensitive | 0 | closed line gone |
| `Synthetic demo` | 1 | label present |
| `No real person, no real call` | 1 | subline present |
| `Not production evidence` | 1 | subline present |
| `<h1 class="svelte-1uha8ag">Orma</h1>` | 1 | SSR heading |
| `Why a phone call`, `Quitting is easy`, `Get started` | 1 each | sections intact |
| `spinner`, `skeleton`, `data-loading` case-insensitive | 0 | no spinner shell |
| Disabled `Play synthetic audio` button plus `speech synthesis. This browser` note | 1 each | SSR fallback rendered |

The `h1` carries a Svelte scope class, so a bare `<h1>Orma</h1>` match reads 0. The scoped match above reads 1. The demo panel arrives in the SSR HTML with its label, its subline, its three lines, and the else branch. The disabled button and the fallback paragraph are literal in the bytes. First render uses `supported = false`, so this is the expected server output.

| Token | Count |
|---|---|
| `ssr` | 0 |
| `auth`, `session`, `supabase`, `fetch(`, `goto`, `export const`, `$app` imports | 0 each |
| `static`, `assets`, `var(--`, `@import`, `token` | 0 each |
| `Not yet open` | 0 |
| `speechSynthesis` | 7 |
| `window` | 6 |
| `login` | 2 |

`web/static/` holds only the pre-existing `robots.txt`. `git status --porcelain -- web/static/` prints nothing. No new asset file exists.

## SpeechSynthesis path, end to end

| Line | Behaviour |
|---|---|
| 10 | `supported = $state(false)`, so SSR and first paint show the disabled button and the calm note. |
| 56 to 58 | `onMount` sets `supported` only when `speechSynthesis` and `SpeechSynthesisUtterance` both exist in `window`. This is the sole unbounded `window` read and it runs client side only. |
| 127 to 134 | Browsers without the API keep the disabled button and read `Synthetic audio needs speech synthesis. This browser does not offer it.` |
| 38 to 46 | `play` returns when unsupported or already playing, cancels any queued speech, then speaks from line 0 with `playing = true` and `active = 0`. |
| 19 to 36 | `speakLine` builds one utterance per line, raises pitch for the `You` line, chains the next line through `onend`, and resets on the last line or on `onerror`. `window.speechSynthesis.speak` runs only inside this handler chain. |
| 119 | `class:active` highlights the speaking line. `active` advances in `onend` at line 26. |
| 24 to 34 | `onend` on the final line and `onerror` anywhere call `reset`, which clears `playing` and `active`. `stop` cancels and resets. A cancelled utterance resolves through `onerror` or a guarded `onend`, and both land on `reset`, so state cannot stick on playing. |
| 60 to 64 | `onDestroy` cancels queued speech. The `typeof window` guard is redundant inside a destroy hook but harmless. |

The demo cannot imply a real person or production evidence. The label reads `Synthetic demo`. The subline reads `No real person, no real call. Not production evidence.` The button reads `Play synthetic audio`. Sound comes from the visitor browser TTS engine. No audio file, no recording control, no waveform, no transcript offset, and no phone number appear anywhere on the page or in this note.

## Findings

No findings. Every check above measured the artifact rather than quoting the handoff.

## Done-when assessment

The phase doc says: a signed-out visitor gets server-rendered content with a clear way in, the closed sign-ups line is gone, and one clearly labelled synthetic call demo plays on the page.

| Clause | Pass or fail | Pin |
|---|---|---|
| A signed-out visitor gets server-rendered content with a clear way in | Pass | Cookie-free curl returns 200 with the heading, lede, beat, sections, and demo in 10415 bytes of HTML and zero spinner markers. Three `href="/login"` targets carry the way in. |
| The closed sign-ups line is gone | Pass | Zero `Not yet open` matches in the response and in the file. The footer now invites sign-up through the login page. The diff shows this as the only footer change. |
| One clearly labelled synthetic call demo plays on the page | Pass | Label plus subline pinned in SSR bytes. Play, stop, highlight, end reset, error reset, and teardown read at the lines cited above. The `{:else}` fallback covers SSR and browsers without the API. |

Content authorities check out. Spec section 11 asks for what Orma is, the thesis, and one call recording on `/`. The task block replaces the recording with a labelled synthetic demo, and the page follows the task block. Product sections 1 and 6 survive intact: the lede carries the two-minute call thesis, `Why a phone call` carries the dismissal argument, and `Quitting is easy and unpunished` carries the retire argument. The style block keeps the local idiom and adds `.demo` plus `.controls` in the same voice. No app token import exists.

Scope note: the working tree holds concurrent sibling changes (`PHASE-6-web-app.md`, `p6-local-stack.sh`, other handoffs, new `app/history`, `app/patterns`, `app/timeline` routes). None belongs to T6.8. The T6.8 delta touches only `web/src/routes/+page.svelte` and its handoff, which matches its `owns` line.
