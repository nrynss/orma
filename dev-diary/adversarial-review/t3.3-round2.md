# T3.3 round 2 review

## Verdict

**APPROVE**

| C | H | M | L |
| --- | --- | --- | --- |
| 0 | 0 | 0 | 0 |

## Zero-residue claim

Round one contained H insert-then-edit fail UX. This review found zero residue from it.

| Prior finding | Independent pin | Result |
| --- | --- | --- |
| H. After insert, throwing `editMessage` showed `VOICE_FAIL_TEXT` while an item existed. | Independent Deno probe. `editMessage` threw on `Recorded:`. Result status was `captured`. `editText` equalled `recordedVoiceReply` of the transcript. Log had no `VOICE_FAIL_TEXT`. Log had `send:` of that reply plus `send-markup:voice_ok:`. `listItemsByUser` counted one row. | Resolved. |

## Independent checks

`deno` was already on PATH as Deno 2.9.6.

`deno test --allow-read supabase/functions/telegram/voice.ts` reported 29 passed, 0 failed.

Eleven of those tests register because `voice.ts` imports `link.ts`. Seven register from `capture.ts`. Eleven live in `voice.ts` after the remediation test.

Ack is still the first side effect. A happy-path capture still edits the ack in place with the transcript and Confirm.

A throwing transcribe still edits to `VOICE_FAIL_TEXT`, attaches no confirm, and inserts zero rows.

The REST double still uploads through `ORMA_API_URL` into `item-audio` before the items insert. No `supabase.co` host.

`VoiceChat.sendMessage` is implemented on `createTelegramVoiceChat` as a Bot API `sendMessage` with optional reply markup.

`rg captureVoiceNote supabase/functions/telegram/index.ts` still matched nothing.

Remediation introduced no new defect inside the T3.3 seam that this review records as a finding.

## Accepted for this round

The contract note is still honest. T3.3 cannot wire T3.1, add `items.audio_url`, or create bucket `item-audio`.

Done-when ack, edit, stored audio link, and failed-transcription copy hold on the helper. They are not met on a live voice note until T3.1 calls `captureVoiceNote`.

If both success edit and follow-up send fail, the ack stays on `VOICE_ACK_TEXT` while the item exists. That is preferable to a false failure message. Confirm is missing until the user looks in Orma or retries.

Upload-then-insert can still leave an orphan Storage object when the items POST fails.

Tests live in `voice.ts` because that is the T3.3 `owns` seam.

T3.3 status remains `claimed:orma` until the orchestrator lands this APPROVE and marks the task done.
