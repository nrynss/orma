# T6.3 round-2 review: History

Verdict: APPROVE.

Counts: C 0, H 0, M 0, L 0.

## Residue against round 1

Round 1 returned REMEDIATE with one L finding. Its re-measured state follows.

| Round-1 # | Where | Re-measured state |
|---|---|---|
| 1 (L) | `web/src/routes/app/history/+page.ts:69` stood accused of fetching `items(id, text)` with no reader | Zero residue. Grep for `rows.items`, `itemsRes`, and `from("items")` over `web/src/routes/app/history/` returns zero hits. The loader `Promise.all` destructures four entries (`transcriptsRes`, `resultsRes`, `mentionsRes`, `commitmentsRes`) and the returned `rows` object holds no `items` key. The view reads exactly `profile`, `runs`, `transcripts`, `results`, `mentions`, and `commitments`. Item text still reaches the summary lines through the embedded joins `items(text,status,source,created_at,retired_at)` in the mentions select and `items(text)` in the commitments select. Both joins return live data, verified below. |

## Pins

Seed run `40000000-0000-4000-8000-000000000001` is `completed`, `answered_extracted`, mood `energised`, scheduled 2026-09-13. `transcripts` holds 1 row with 2 turns (`agent` at offset 0, `user` at offset 8). `results` holds 1 row, `valid` true, with empty `retired_items` and one commitment (`30000000-0000-4000-8000-000000000001` at offset 8, due 2026-09-15). `item_mentions` holds 3 rows on the run (offsets 0, 2, 4). `commitments` holds 1 row (evidence offset 8).

Feeding those rows through `lastCallSummary` gives retired 0, committed 1, captured 0, `mentionCount` 3, with one Committed line at offset 8. The capture candidate (`Call Amma`, source `call`, created 2026-09-06) predates the run start (2026-09-13), so the `ms(created_at) >= runStart` guard in `model.ts:482` correctly keeps it out. History calls `lastCallSummary(run, rows.mentions, rows.commitments, result?.structured ?? null, zone, now)` (`+page.svelte:55`) from the same `$lib/today/model` import Today uses, with the same inputs. Counts agree by construction.

PostgREST as `local@example.com` returns `call_runs` newest first by `scheduled_for` (scheduled, canceled, seed, then `0011`, `0012`, `0013`). The page query adds `state neq scheduled` and `state neq canceled` and returns the 4 completed runs. `transcripts` returns 1 row, `results` 1 row, `item_mentions` 3 rows on the seed run, `commitments` 1 row with the embedded `items` payloads intact. `firstrun@example.com` reads `[]` for `call_runs`, `transcripts`, and `item_mentions`. Cross-user isolation holds.

The loader query shape was replayed verbatim against PostgREST. It returns the seed plus `0011` through `0013` in order. Runs `0011` through `0013` carry neither results nor transcript rows, so `hasResult` is false and the honest line at `+page.svelte:145-147` renders for them. The local profile timezone reads `Asia/Kolkata`, matching the view fallback.

Dev server on 5199 (bound to `::1`, reached via `localhost`): anonymous `GET /app/history` returns 303 to `/login`. Anonymous `GET /app/history?run=<seed>&t=8` returns the same 303, so the auth gate covers parametrised links. The page sets `ssr = false`, so param handling is client side. `requestedRun` comes from `page.url.searchParams.get('run')` (line 28) and `requestedT` from `parseTParam` (line 29, `model.ts:57-62`). `expandedId` picks the requested run when it matches, else the newest run (lines 30-34). `isMarked` matches the turn whose `offsetSeconds` equals `requestedT` in the expanded run (lines 95-97). The mark renders as `data-turn-mark="true"` (line 152) and the effect scrolls to it (lines 99-102). Summary lines and turn offsets both link through `historyHref` (lines 137, 154, 188, 207).

Speakers map `agent` and `bot` to Orma and `user`, `callee`, `caller`, and `you` to You (`model.ts:15-21`), matching both fixture turns. Offsets format through the shared `formatOffset`. Clocks use `formatClock` and `formatDayLabel` in `zone`, read from the profile timezone with an `Asia/Kolkata` fallback (lines 25, 60, 78). `MoodMarker` takes only `label` and `none` and exposes no edit path (lines 116, 191, 210). Live states render the on-the-call header with no summary (lines 120-122). The `noConnection` branch (line 59, lines 127-129) has no live fixture row in this round, so it passes on the code path, which is unchanged since round 1.

Note: the round-1 `failed`/`not_answered` drift row (`4f...f901`) is absent from the database in this round. The `call_runs` count is 6, and the loader returns the 4 completed runs. No assertion depends on it.

Scratch rows: I inserted nothing. Database row counts before and after are identical. All database access was `SELECT`, and all HTTP access was `GET`.

Scope: `git status` shows `web/src/routes/app/history/` (three files: `+page.ts`, `+page.svelte`, `model.ts`), the sibling `patterns/` directory, sibling review files, and a modified `scripts/p6-local-stack.sh` owned by the sibling T6.4 fixture row. I touched no implementation, migration, type, or status file. No shared file changed for this task.

One documentation drift, not a finding: the T6.3 handoff still says the loader reads `items` (handoff line 9). The remediation removed that fetch, and this review pins zero hits. The handoff predates the remediation and describes no behaviour any code relies on.

## Findings

| # | Where | What | Pin | Mutation |
|---|---|---|---|---|
| None | | | | |

## Done-when assessment

Phase doc: a fixture run renders its turns in order, clicking a retirement jumps to the offset where it was said, and a run with no result still shows its transcript.

1. Turns in order: pass. `parseTurns` sorts by offset (`model.ts:37-41`). The seed run carries turns at 0 and 8, which PostgREST returns verbatim.
2. Retirement jumps to its offset: pass on the code path, with the same fixture caveat as round 1. No seed `retired_items` row exists, so no retirement link occurs in data. The identical path (summary line link at line 137 through `historyHref` to `isMarked` plus scroll) is exercised by the commitment line at offset 8. The task owns no fixture paths, so this is not a defect.
3. No-result run still shows its transcript: pass on the code path. Lines 145-162 render the honest line and any turns independently of `hasResult`. Runs `0011` through `0013` carry neither results nor transcript rows, so their transcript section is empty by data, not by code.
