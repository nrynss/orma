# T2.5a review, round 1

Verdict: **APPROVE**

Counts: C 0, H 0, M 0, L 0

This is round 1 for T2.5a. I did not implement, remediate or review T2.5a before. I fixed nothing and changed no code, migration, type or status line.

## Scope and method

I read `AGENTS.md` in full, `dev-diary/README.md`, the T2.5 and T2.5a blocks of
`dev-diary/PHASE-2-call-engine.md`, the last row of `t2.5-contract-change.md`,
`t2.5a-contract-change.md`, `t2.5-round3.md` and `t2.5-round4.md`. I then read
the four owned artifacts, in full for the two code files and the migration, and
in structure for both generated type files. I also read the T2.5a-relevant parts
of `tick/index.ts`, `_shared/events.ts` and `_shared/calle.ts`.

I froze the artifacts and their imports in `/tmp/orma-review-t25a-r1` before any
pin. I drove the real webhook handler and the real `pollRun` with a stub CALL-E
transport, exactly as `t2.5-round3.md` did. The stub refuses every host except
the scratch proxy and `https://api.call-e.test`. I placed no call and did not run
`/tmp/orma-p2/up.sh`.

No claim below comes from a program's own summary line. Every claim is a SQL read
scoped to `idempotency_key like 't25ar1-<tag>-%'` or `event_id like 'evt_t25ar1_<tag>_%'`,
taken from the shell.

## Findings

| Where | What | Pin | Mutation |
| --- | --- | --- | --- |
| no finding | Zero findings at every severity. Every pin named in the done condition passes on the frozen artifact. | n/a | n/a |

## Pin F1a, a second event versus a redelivery of the winner

Command, twice, in two shapes:

```text
deno run --allow-env --allow-net --allow-read pins-r1.ts F1a r1a
```

The first shape is the winner's own history. Event `evt_t25ar1_r1a_f1a_one` was
delivered twice. Its first delivery answered 500, because the stub failed the
`call_events` insert after the run's guarded PATCH had already landed. The
redelivery answered 200.

The second shape is a second event for one call. Event `..._two_a` won. Event
`..._two_b` first met a 503 re-fetch and answered 500, then its redelivery
answered 200.

SQL, two rows for two runs:

```sql
select idempotency_key, state, terminal_writer, completed_at
  from public.call_runs where idempotency_key like 't25ar1-r1a-%' order by 1;
```

```text
    idempotency_key       |   state   |         terminal_writer
--------------------------+-----------+----------------------------------
 t25ar1-r1a-f1a-second    | completed | webhook:evt_t25ar1_r1a_f1a_two_a
 t25ar1-r1a-f1a-winner    | completed | webhook:evt_t25ar1_r1a_f1a_one
```

The winner's stored tag is its own event id. The winner's run holds two rows, a
`webhook_received` row and one `refetched` row with `outcome = applied`. So its
redelivery read back its own tag and claimed the move it made.

The second run's stored tag is `..._two_a`, the first event. Its timeline read:

```sql
select r.idempotency_key as run, e.id, e.kind, e.detail->>'outcome' as outcome,
       e.detail->>'state' as state, e.detail->>'event_type' as evtype
  from public.call_events e join public.call_runs r on r.id = e.call_run_id
 where r.idempotency_key like 't25ar1-r1a-%' order by r.idempotency_key, e.id;
```

```text
          run          | id  |       kind       |     outcome      |   state   |            evtype
-----------------------+-----+------------------+------------------+-----------+-------------------------------
 t25ar1-r1a-f1a-second | 177 | webhook_received |                  |           | call.completed
 t25ar1-r1a-f1a-second | 178 | refetched        | applied          | completed | call.completed
 t25ar1-r1a-f1a-second | 179 | webhook_received |                  |           | call.result_validation_failed
 t25ar1-r1a-f1a-second | 180 | refetched        | already_resolved | completed | call.result_validation_failed
 t25ar1-r1a-f1a-winner | 175 | webhook_received |                  |           | call.completed
 t25ar1-r1a-f1a-winner | 176 | refetched        | applied          | completed | call.completed
```

Event `two_b` recorded `already_resolved`. Six timeline rows for two runs.

Which event won is a measurement, not a response code. The winning tag sits on
the run, and the losing event's `refetched` row disagrees with it.

## Pin F1b, the poll wins and loses its timeline row

Command:

```text
deno run --allow-env --allow-net --allow-read pins-r1.ts F1b r1b
```

The webhook's first delivery got a 503 re-fetch and answered 500. The poll then
moved the run, and its `call_events` insert got 503, so `pollRun` threw.

```text
F1b-webhook-first=500
F1b-poll=threw call event insert failed
F1b-redelivery=200
```

```sql
select idempotency_key, state, terminal_writer, completed_at is null as ca_null
  from public.call_runs where idempotency_key like 't25ar1-r1b-%';
```

```text
             idempotency_key             |   state   | terminal_writer | ca_null
-----------------------------------------+-----------+-----------------+---------
 t25ar1-r1b-f1b-poll-row-lost            | completed | poll            | t
```

```text
             run              | id  |       kind       |     outcome      |   state
------------------------------+-----+------------------+------------------+-----------
 t25ar1-r1b-f1b-poll-row-lost | 181 | webhook_received |                  | 
 t25ar1-r1b-f1b-poll-row-lost | 182 | refetched        | already_resolved | completed
```

Two rows. No `polled` row exists, because the insert failed. The redelivery still
recorded `already_resolved`. It read `terminal_writer = poll` from the run, so it
did not claim a move it never made. Round 3 measured this case recording
`applied` before the fix.

## Pin F1c, both race orderings

Command, six raced rounds:

```text
deno run --allow-env --allow-net --allow-read pins-r1.ts F1c r1c
```

The poll and the webhook run concurrently. The loser's CALL-E re-fetch is delayed
250 ms.

```sql
select idempotency_key, state, terminal_writer
  from public.call_runs where idempotency_key like 't25ar1-r1c-%' order by 1;
```

```text
           idempotency_key            |   state   |              terminal_writer
--------------------------------------+-----------+--------------------------------------------
 t25ar1-r1c-f1c-poll-first-1          | completed | poll
 t25ar1-r1c-f1c-poll-first-2          | completed | poll
 t25ar1-r1c-f1c-poll-first-3          | completed | poll
 t25ar1-r1c-f1c-webhook-first-1       | completed | webhook:evt_t25ar1_r1c_f1c_webhook-first_1
 t25ar1-r1c-f1c-webhook-first-2       | completed | webhook:evt_t25ar1_r1c_f1c_webhook-first_2
 t25ar1-r1c-f1c-webhook-first-3       | completed | webhook:evt_t25ar1_r1c_f1c_webhook-first_3
```

```text
              run               | id  |       kind       |     outcome      |   state
--------------------------------+-----+------------------+------------------+-----------
 t25ar1-r1c-f1c-poll-first-1    | 194 | polled           | terminal         | completed
 t25ar1-r1c-f1c-poll-first-1    | 200 | webhook_received |                  |
 t25ar1-r1c-f1c-poll-first-1    | 201 | refetched        | already_resolved | completed
 t25ar1-r1c-f1c-poll-first-2    | 202 | polled           | terminal         | completed
 t25ar1-r1c-f1c-poll-first-2    | 203 | webhook_received |                  |
 t25ar1-r1c-f1c-poll-first-2    | 204 | refetched        | already_resolved | completed
 t25ar1-r1c-f1c-poll-first-3    | 205 | polled           | terminal         | completed
 t25ar1-r1c-f1c-poll-first-3    | 206 | webhook_received |                  |
 t25ar1-r1c-f1c-poll-first-3    | 207 | refetched        | already_resolved | completed
 t25ar1-r1c-f1c-webhook-first-1 | 208 | webhook_received |                  |
 t25ar1-r1c-f1c-webhook-first-1 | 209 | refetched        | applied          | completed
 t25ar1-r1c-f1c-webhook-first-2 | 210 | webhook_received |                  |
 t25ar1-r1c-f1c-webhook-first-2 | 211 | refetched        | applied          | completed
 t25ar1-r1c-f1c-webhook-first-3 | 212 | webhook_received |                  |
 t25ar1-r1c-f1c-webhook-first-3 | 213 | refetched        | applied          | completed
```

Fifteen rows for six runs. In every poll-first round the `polled` row claims the
move and `terminal_writer` reads `poll`. In every webhook-first round one
`refetched` row claims the move, no `polled` row exists, and `terminal_writer`
names that event. Six rounds, one state writer each.

## The named attack list

**The reschedule path must not stamp the writer.** Command
`pins-r1.ts RESCHEDONLY r1R`, with the stub reporting `in_progress`.

```sql
select state, terminal_writer, poll_after from public.call_runs
 where idempotency_key like 't25ar1-r1R-%';
```

```text
        state        | terminal_writer |             poll_after
---------------------+-----------------+----------------------------
 awaiting_result     |                 | 2026-09-13 07:28:38.174+00
```

The run is still pending, the column is null, and one `polled` row carries
`outcome = pending`. Mode `RESCHED r1s` adds the second half. The same run was
rescheduled and then a webhook for its call won.

```text
 t25ar1-r1s-resched | failed | webhook:evt_t25ar1_r1s_resched
 197 polled           pending          in_progress
 198 webhook_received
 199 refetched        applied          failed
```

The reschedule left the column null, and the winning event then stamped its own
tag.

**The give-up path.** Mode `GIVEUP r1u` on a run dispatched 16 minutes ago whose
re-fetch returns 503.

```text
 t25ar1-r1u-giveup | failed | poll | poll_timeout
 192 polled           refetch_failed  failed    poll_timeout
 193 webhook_received
 195 refetched        already_resolved completed
```

The poll named itself. The terminal event that arrived afterwards read `poll` and
recorded `already_resolved`.

**The missing-call-id path.** Mode `NOCALL r1n` on a run with no `calle_call_id`.

```text
 t25ar1-r1n-nocall | failed | poll | poll_unavailable
 196 polled           (no outcome)  failed   run has no CALL-E call id to poll
```

The poll named itself and the timeline names the reason.

**A zero-match PATCH where the column is null.** Mode `NULLTAG r1T`. One SQL
PATCH moved the run to `failed` with `completed_at` set, which mimics an untagged
writer such as `_shared/calle.ts`. A terminal webhook then arrived.

```text
 t25ar1-r1T-nulltag | failed | (null) | consent_missing
 215 webhook_received
 216 refetched        already_resolved completed
```

The event recorded `already_resolved`, not `applied`. No shipped writer produces
this state for a call-bearing run, so the branch is defensive. Its behaviour is
still correct, and the case is pinned.

**A redelivery inside the poll's insert window.** Mode `LAG r1g`, three rounds.
The poll's `call_events` POST is delayed 700 ms and the redelivery's re-fetch
250 ms. So the redelivery reads the run between the poll's PATCH and its insert.
This is round 3 pin F1c in the same shape, with slightly wider delays.
```text
 t25ar1-r1g-lag-1 | completed | poll | ...
 183 webhook_received
 184 refetched        already_resolved  (07:28:07.149)
 185 polled           terminal          (07:28:07.578)
```

Rounds 2 and 3 read the same shape, with the `refetched` row about 430 ms before
the `polled` row. Three of three rounds recorded `already_resolved`. Round 3's
unfixed code recorded `applied` here.

**Does any code still infer a writer from a missing `polled` row?** No.

```text
grep -rn "eq.polled\|kind.*polled" --include=*.ts --include=*.svelte --include=*.sql supabase web proxy scripts
```

Two hits remain, both in `poll.ts` tests. Both assert that the poll writes a
`polled` row. Neither reads the absence of one. `earlierAttemptApplied` and the
`redelivered` flag appear nowhere in the shipped sources.

**The column, the finaliser and `completed_at`.** Mode `FINAL r1F` polled a run to
`completed`. SQL read `completed_at is null` as true and `terminal_writer = poll`.
A SQL read with the same filter as `terminalRuns` returned one row:

```sql
select count(*) from public.call_runs
 where idempotency_key like 't25ar1-r1F-%'
   and state in ('completed','no_result','failed','canceled') and completed_at is null;
```

```text
 finaliser_queue_mirror
------------------------
                      1
```

The poll leaves the run in the finaliser's queue, as its contract requires.

## Mutations, with observed failure text

I reverted seven implementation choices, each in its own scratch copy under
`/tmp/orma-review-t25a-r1/mut<N>`. The live driver ran each with a fresh marker,
then I read SQL. I also ran each mutated module's own pin. The revert that
changes nothing is not a pin, so every row below shows a measured failure.

| Reverted choice | Live pin under the revert | Module pin under the revert |
| --- | --- | --- |
| M1, the webhook PATCH drops `terminal_writer` | `m1-f1a-winner` reads `terminal_writer` null, and its redelivery records `already_resolved`. F1a fails. | `the move must name the event that made it, saw undefined` |
| M2, the zero-match read compares only the `webhook:` prefix | `m2-f1a-second` row 232 records `applied` for event `two_b`, which did not move the run. F1a fails. | `redelivery after a second event moved the run: expected outcome already_resolved, saw ..."outcome":"applied"` |
| M3, the poll's terminal write drops the tag | All three `m3-f1c-poll-first-*` runs read `terminal_writer` null while a `polled` row claims the move. F1c fails. | `the terminal write must name the poll as the writer` |
| M4, the give-up write drops the tag | `m4-giveup` reads `terminal_writer` null beside a `polled` row with `poll_timeout`. F1c fails. | `the give-up write must name the poll as the writer` |
| M5, the missing-call-id write drops the tag | `m5-nocall` reads `terminal_writer` null beside its `polled` row. F1c fails. | `the missing-call-id write must name the poll as the writer` |
| M6, the reschedule stamps the tag | `m6-reschedonly` reads `terminal_writer = poll` on a run still at `awaiting_result`. The column describes a terminal write that never happened. | `a reschedule must not name a terminal writer` |
| M7, the zero-match read goes back to the `state` plus no-`polled`-row inference | `m7-f1b` records `applied` where the frozen tree records `already_resolved`. In the lag variant, `m7b-lag-*` records `applied` in three of three rounds. F1b and F1c fail. | `a lost write must read terminal_writer once`, and `redelivery of this event's own write: expected outcome applied, saw ..."outcome":"already_resolved"` |

The frozen artifact passes all seven. Each revert breaks the pin that defends it.

Module checks on the frozen copy, for completeness:

```text
deno check supabase/functions/_shared/poll.ts supabase/functions/calle-webhook/index.ts supabase/functions/tick/index.ts
Check ... (three lines, exit 0)
deno test --allow-env --allow-read --allow-net <same three files>
ok | 99 passed | 0 failed (291ms)
```

Those 99 tests are the artifact's own pins, so they are not my evidence. My
evidence is the SQL above.

## The two type files and the live schema

Measurement one, byte identity. At the first freeze both files were
`1d88bf30cba5482af46b4bbafd62ed8ceb2c2e73511e9fb21a51e872713dc231`.

Measurement two, the live pair now. Both files are
`a8d84ee2088ae232ecc203463a4b265a7223063b8f3260724d565f93ace8eb1c`.

Between those two measurements a concurrent agent regenerated both files. The
freeze table below therefore shows one of its two copied type files at the newer
hash. The difference between the two hashes is the one sibling line named below.

Measurement three, agreement with the live schema. I read the live schema from
PostgREST, then compared every table.

```text
curl -s http://127.0.0.1:57030/ -o /tmp/orma-review-t25a-r1/openapi.json
```

Fourteen tables, all fourteen identical to the type files, column for column.
`call_runs` carries 22 properties and `terminal_writer` is one of them. Its
OpenAPI description is the migration's `comment on column` text, word for word.

Measurement four, the generator.

```text
supabase gen types typescript --db-url "$SUPABASE_DB_URL" > /tmp/orma-review-t25a-r1/gen-live.ts
diff -u supabase/functions/_shared/database.types.ts /tmp/orma-review-t25a-r1/gen-live.ts
```

No output. Both live type files are byte-identical to a fresh generator run. The
live pair also matches each other byte for byte. The generator's scaffolding
(`graphql_public` first, no `__InternalSupabase` key, the `Omit` alias still
present) is what this CLI version emits, and both copies carry it.

Against HEAD, the change to both files removes the `__InternalSupabase` key,
adds three `terminal_writer` entries, and regenerates the `public` Functions
section. `terminal_writer` also appears in the `claim_due_call_runs` return
shape, which is correct because that function returns `setof call_runs`. The
live diff is 58 insertions and 6 deletions per file, and the sibling's later
`p_skipped` line is one of the 58.

One divergence needs naming. A concurrent agent regenerated both files at
12:57:33 during my review, adding `p_skipped?: Json` for its own migration. My
driving freeze had already re-copied `_shared/database.types.ts` mid-write, so the
frozen pair differs by exactly that one line. The diff is one added line and
nothing else.

```text
diff -u /tmp/orma-review-t25a-r1/web/src/lib/database.types.ts web/src/lib/database.types.ts
+          p_skipped?: Json
```

That line belongs to the concurrent T2.7 work in
`supabase/migrations/20260912180000_ingest.sql`, which is out of scope and which
I do not judge. It changes nothing about T2.5a's artifact. No count above moves
because of it.

## The owner-read decision against what RLS does

`t2.5a-contract-change.md` decides the owner may read the column, and states the
migration changes no policy and no privilege. I checked both halves.

```sql
select policyname, cmd, roles::text, qual from pg_policies
 where schemaname='public' and tablename='call_runs';
```

```text
      policyname      |  cmd   |  roles   |          qual
----------------------+--------+----------+------------------------
 call_runs_owner_read | SELECT | {public} | (auth.uid() = user_id)
```

```sql
select grantee, privilege_type from information_schema.column_privileges
 where table_schema='public' and table_name='call_runs'
   and column_name='terminal_writer' order by grantee;
```

```text
    grantee     | privilege_type
----------------+----------------
 anon           | UPDATE
 anon           | INSERT
 anon           | REFERENCES
 anon           | SELECT
 authenticated  | UPDATE
 authenticated  | SELECT
 authenticated  | REFERENCES
 authenticated  | INSERT
 postgres       | ...
 service_role   | ...
```

I then ran the record's own transaction with my own marker. It inserted one run,
assumed `authenticated` twice with different `sub` claims, and rolled back. Its
observed output matched the record exactly.

```text
 actor | rows |       tag
-------+------+-----------------
 owner |    1 | webhook:evt_r1pin
 actor | rows
-------+------
 other |    0
```

Residue after the rollback: `select count(*) from public.call_runs where
idempotency_key like 't25ar1-rls-pin%'` read 0. The decision matches the policy
and the grant. A row policy grants the whole row, so a privilege revoke would be
the only way to hide one column. The tag is a de-duplication key, and the owner
already reads the run's `call_events` rows through `call_events_owner_read`,
which exposes `refetched` and `polled`. So the column reveals no new class of
data.

## Residue against `t2.5-round3.md` findings 1 and 2

**Finding 1, the F1a misattribution.** Zero residue. The webhook now stamps
`webhook:<event_id>`, and a zero-match read compares the stored tag with its own
full tag. Measured in mode F1a, six timeline rows over two runs.

**Finding 2, the F1b and F1c misattribution.** Zero residue for the attribution.
The poll stamps `poll` before its insert, so a lost insert or a lost race cannot
mislabel the loser. Measured in modes F1b and LAG.

One part of finding 2 is not fixed and T2.5a's done condition does not require
it. A poll whose insert fails leaves the transition with no timeline row. Round 4
recorded that the tag now carries the writer on the run instead. I confirm that
reading. The orphan run is legible through `terminal_writer = poll`, and the
timeline gap is the accepted design rather than a surviving defect.

Finding 3 stayed with T2.7, as round 4 recorded. I did not measure it.

## Commands run, and the counts I saw

Every pin command was `deno run --allow-env --allow-net --allow-read
pins-r1.ts <mode> <tag>` run from `/tmp/orma-review-t25a-r1`, after
`source /tmp/orma-p2/env.sh`. Row counts came from the SQL above.

```text
F1a  r1a  2 runs, 6 call_events rows
F1b  r1b  1 run,  2 call_events rows
F1c  r1c  6 runs, 15 call_events rows
LAG  r1g  3 runs, 9 call_events rows
RESCHEDONLY r1R  1 run, 1 call_events row
RESCHED     r1s  1 run, 3 call_events rows
GIVEUP      r1u  1 run, 3 call_events rows
NOCALL      r1n  1 run, 1 call_events row
NULLTAG     r1T  1 run, 2 call_events rows
FINAL       r1F  1 run, 1 call_events row
mutations   m1..m7b  17 runs, 43 call_events rows
```

Eighteen pinned runs and seventeen mutated runs, so 35 runs. Forty-three
timeline rows in each group, so 86. Both match the cleanup counts below. The
`call_events` id sequence is shared with other writers, so gaps in those ids are
not a scope. Every count above is scoped by `idempotency_key`.

I placed no call. `CALLE_API_BASE` was `https://api.call-e.test` and the driver
refuses any other host. I did not run `/tmp/orma-p2/up.sh`.

## Freeze and tree identity

| File | Frozen sha256 | Live sha256 | Same |
| --- | --- | --- | --- |
| `supabase/functions/_shared/poll.ts` | `7d33db7b…d7bc60` | `7d33db7b…d7bc60` | yes |
| `supabase/functions/calle-webhook/index.ts` | `c08e17d9…a39dda` | `c08e17d9…a39dda` | yes |
| `supabase/functions/tick/index.ts` | `393879de…4bb66` | `393879de…4bb66` | yes |
| `supabase/migrations/20260913120000_terminal_writer.sql` | `918c2e11…7cbeb3` | `918c2e11…7cbeb3` | yes |
| `supabase/functions/_shared/database.types.ts` | `a8d84ee2…eb1c` | `a8d84ee2…eb1c` | yes |
| `web/src/lib/database.types.ts` | `1d88bf30…dc231` | `a8d84ee2…eb1c` | no, one sibling line |

`diff -r --brief supabase/functions /tmp/orma-review-t25a-r1/supabase/functions`
prints nothing for the code. The one file that moved is the web copy of the
generated types, and it moved under a concurrent agent's regeneration at
12:57:33. I report that plainly rather than claim a clean end state. The frozen
code I drove is byte-identical to live.

`git status --short` also shows the concurrent agent's `_shared/ingest.ts`,
`20260912180000_ingest.sql` and `test-ingest.sh`, and its new
`t2.7-remediation-round2.md`. I did not read or judge those. I wrote only this
file.

## Concurrency note, disclosed

The parent agent told me mid-review not to drive `tick`, because it claims every
due run in the shared database. Before that instruction arrived I had driven
`tick()` once, in mode `FINAL`. It wrote nothing. My deps omitted
`dispatchClaimed`, so `claimDueRuns` and its `FOR UPDATE SKIP LOCKED` RPC never
ran, and no `claimed` event was written. `pollDue` was a no-op and `finalise`
only recorded run ids. The result was `{"claimed":0,"polled":0,"finalised":16}`.
So the only database calls were two SELECTs. I did not drive `tick` again. The
finaliser-queue claim above rests on the SQL read with the same filter, and the
one `FINAL` result is extra evidence beside it.

## Residual risk

- **Live timing proof stays open.** T2.5's done condition needs two real CALL-E
  runs. T2.5a needs none, and its pins are fixture-ok. No round has placed a call.
- **The poll's timeline gap survives.** A failed insert leaves one terminal run
  with no `polled` row. The tag records the writer on the run. Round 4 accepted
  this shape and T2.5a's done condition does not ask for a retry.
- **The null-tag branch has no shipped producer.** No writer today moves a
  call-bearing run to a terminal state without a tag. T2.7a's `ingest_call_result`
  validates its `p_state` argument but not the run's state, so a future caller
  could. The branch answers `already_resolved`, which stays truthful.
- **Overlapping ticks.** `duePolls` takes no claim, so two ticks can poll one run
  twice. The state guard still yields one winner. T2.5a does not change this.
- **Two terminal events per call is still unobserved.** F1a and F1b assume CALL-E
  can send two terminal event ids for one call. A live call has not shown it.
- **The status board is stale.** `dev-diary/README.md` line 161 still says T2.5a
  is not started. T2.5a's implementation may not mark its own task done, so the
  landing step owns that line. I did not touch it.
- **The T2.6 block's step 2 no longer matches the receiver.** It says a conflict
  means the event was seen before and the receiver returns 200 and stops. The
  shipped code claims the row and retries a released or stale one. T2.5a relies
  on that claim path for its F1a redelivery but did not change it. It belongs to
  T2.5 and T2.6, so I did not count it here.

## Checked and judged not a finding

Two candidates I raised and dismissed, with evidence, so a later round need not
re-derive them.

**The migration names the decision, and the column comment names the writers.**
`t2.5a-contract-change.md` says `_shared/calle.ts` writes terminal states in
`refuseRun` and on a failed dispatch, and that the column stays null there. A
third unguarded terminal writer exists on that path. `dispatch-mode.ts` writes a
terminal patch from a fixture and sets `completed_at` at line 405. So the
record's enumeration is short by one. Its criterion still holds for that writer:
the write is unguarded, it sets `completed_at`, and no call exists behind it. A
dry-run run therefore queues for no finaliser and receives no webhook. The record
is incomplete, not false, and its decision covers the case. Not a defect.

**The webhook's guard comment.** The comment above the zero-match read says the
zero match means another guarded write moved the run first. An unguarded writer
can also cause it. The next sentence names the tag read, and `readTerminalWriter`'s
own docblock says a null means no tagged writer claimed the move. The null case
is also in the artifact's own pin table. So the behaviour for every input is
correct and pinned. Not a defect.

## Cleanup

I created 35 `call_runs` rows, 86 `call_events` rows and 33 `webhook_events`
rows. The RLS pin rolled itself back. Cleanup ran as three deletes scoped to my
markers.

```sql
delete from public.call_events where call_run_id in
  (select id from public.call_runs where idempotency_key like 't25ar1-%');
delete from public.call_runs where idempotency_key like 't25ar1-%';
delete from public.webhook_events where event_id like 'evt_t25ar1_%';
```

```text
DELETE 86
DELETE 35
DELETE 33
```

The residue read then returned zero for all three.

```text
 runs | events_joined | webhook_events
------+---------------+----------------
    0 |             0 |              0
```

No row outside my markers was written. No secret reached this file, and no phone
number appears in it. The scratch copies and the driver stay under
`/tmp/orma-review-t25a-r1` and are not part of the repository.
