# T7.2a round 2 review

## Verdict

**APPROVE**

| C | H | M | L |
| --- | --- | --- | --- |
| 0 | 0 | 0 | 0 |

## Scope

Review span `b28fd3b..8e01b79` against the round 1 verdict recorded at `878b91a`. The remediation
changed `telegram/voice.ts` and `_shared/vertex_tests.ts` and added the contract note and the
remediation diary. The working tree also carries an unrelated uncommitted edit to
`dev-diary/PHASE-7-analysis.md` that adds the T7.2a block. I left it untouched. I read `AGENTS.md`,
the brief, the round 1 verdict, the remediation claim, the contract note, and `voice.ts`,
`voice_vertex.ts`, `_shared/vertex.ts` and `_shared/vertex_tests.ts` in full. I implemented nothing
and fixed nothing.

## Independent evidence

Every proof below is my own run at `8e01b79` on this machine. Deno 2.9.6.

1. Combined suite passed 38 tests, 0 failed. That is 29 voice plus 9 shared, so the
   `!import.meta.main` gate hides nothing.
2. Standalone shared suite passed 9 tests, 0 failed.
3. `deno check supabase/functions/telegram/index.ts` exited 0. The import graph typechecks through
   the new re-export.

URL ownership, from greps over the whole worktree.

- `vertexGenerateContentUrl` has exactly one implementation, `_shared/vertex.ts` lines 20 to 29.
  `telegram/voice.ts` line 113 holds only the re-export from `../_shared/vertex.ts`. Same name,
  same three parameters, same body as the deleted copy.
- `aiplatform.googleapis.com` appears in product code only at `vertex.ts` lines 26 and 27. The two
  hits in `vertex_tests.ts` lines 177 and 184 are string oracles inside tests.
- The OAuth token endpoint and the jwt-bearer grant appear in production code only in `vertex.ts`
  lines 76 to 80. The `voice_tests_d.ts` line 115 hit is a stub response, not a second mechanism.
- Existing imports still work. `voice_tests_b.ts`, `voice_tests_b2.ts` and `voice_tests_d.ts`
  import the name from `./voice.ts` and resolve to the shared function through the re-export.

Mutation run on `googleAccessToken`. I replaced both failure throws at `vertex.ts` lines 84 and 86
with a fall-through that returns an empty token. The standalone suite then printed
`FAILED | 7 passed | 2 failed`. Both new pins failed on their message assertions, which proves the
stub saw a generate attempt beyond the one recorded token call. The seven older tests stayed green,
including the happy-path token exchange and the non-OK generate pin. I restored the file from a
pre-mutation copy. Its checksum returned to
`10bc9a3f46dfa2d5afe21889554e61d6ac9c864fd2089c741cd0a899cd357b5e` and `git status` on `supabase/`
reported nothing. A second standalone run after restoration passed 9 of 9.

No network and no real credential. Both green runs used `--allow-read` alone, so any real fetch
would have died on permissions. `testServiceAccountJson` mints a synthetic keypair per call and no
PEM block is committed anywhere under `supabase/functions`.

## Finding closure

One row per round 1 finding. The pin names the check that fails today. The mutation says what breaks
if someone reverts the fix.

| where | what | pin | mutation |
| --- | --- | --- | --- |
| M `telegram/voice.ts` line 113 beside `_shared/vertex.ts` lines 20 to 29 | The duplicate URL builder is gone. One re-export binds the old import path to the single shared implementation. | A worktree grep finds `aiplatform.googleapis.com` in product code only at `vertex.ts` lines 26 and 27, and finds exactly one `vertexGenerateContentUrl` definition. The combined suite passes 38 with the three voice suites still importing from `./voice.ts`. | Restoring a local body would recreate a second exported definition. The definition-count grep then reports two and the round 1 rule fails. |
| M `_shared/vertex_tests.ts` tests at lines 219 to 247 and 249 to 280 against `_shared/vertex.ts` lines 84 and 86 | Both failure branches of the token exchange are pinned. Each test asserts the exact `google token exchange failed` message and exactly one recorded call. | My mutation run replaced both throws with an empty-token fall-through. The suite printed `FAILED | 7 passed | 2 failed` and named both new tests. | Any revert that lets a failed token exchange proceed fails both pins, because the generate stub throws `unexpected` and the message assertion rejects it. |

## Zero residue

- M URL ownership: zero residue. One implementation, one re-export, one definition in the grep, and
  all three voice suites pass through the re-export. No live second copy exists to import.
- M token failure pins: zero residue. Both branches fail their own tests under my mutation while
  every older test survives, so the pins are load-bearing and scoped to exactly the fixed behaviour.

## Diary check

The contract note names `telegram/voice.ts`, names `_shared/vertex.ts` as the shared owner, and
gives the reason in its authorized-change and why-authorized sections. The orchestrator authorized
the seam edit for this round only, which matches the owns line in the T7.2a block.

The remediation diary claims 38 and 9 passing runs, a clean `deno check`, a
`FAILED | 7 passed | 2 failed` mutation run, and a restored checksum. My runs reproduced every one.
Its checksum equals mine. It claims the call-count assertion proves generateContent never runs, and
both new tests carry that assertion at lines 246 and 280.

## False positives I rejected

**The re-export keeps `telegram/voice.ts` on the public import path for the URL convention.**
Deliberate and recorded in the contract note. Round 1 asked for one implementation, not for breaking
import paths. One binding to one owner is not a second convention.

**The two new tests repeat about thirty lines of stub scaffolding each.** They follow the recorded
call style of the existing suite. Repetition inside a test has no provable impact.

**`voice_tests_d.ts` line 115 names the token endpoint inside a stub.** A canned test double is not
a credential implementation. The request still travels through the shared client.

**Round 1 rejections stand.** The `vertex generateContent failed` rename has no reader outside the
catch-all in `captureVoiceNote`. The private `bytesToBase64` copy in `voice_vertex.ts` serves the
audio part. The suite pins header, payload, scope and the real RS256 sign path. The exported
synthetic key helper mints a fresh keypair per call. The `!import.meta.main` gate is the established
repo convention.

## Done-when

"Every existing `voice_vertex` Deno suite passes unchanged" holds. No voice test file changed and
all 29 voice tests pass. "The new shared suite proves the auth and generation path with an injected
fetch" now holds in full, because both token failure branches carry failing mutations behind them.
Both round 1 findings are closed, no new finding surfaced, and the product files match `8e01b79`
byte for byte. Nothing remains to fix.

T7.2a status stays untouched by this review. Landing the task remains the orchestrator's step.
