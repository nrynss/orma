# T2.1 review, round 3

Verdict: APPROVE

| Severity | Count |
| --- | ---: |
| C | 0 |
| H | 0 |
| M | 0 |
| L | 0 |

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| None | None | No findings. | The independent checks below passed. | No fix is required. |

## Independent checks

- `deno test --allow-env --allow-net supabase/functions/materialise/index.ts`
  passed all six tests. They pin the Kolkata instant, DST transitions, durable
  key handling, inactive-slot deletion, and handler rejection without the
  named secret.
- Read `supabase/migrations/20260912150000_schedule_materialise.sql`. Its named
  `cron.schedule` job uses `10 0 * * *`, which is 00:10 UTC. It looks up the
  secret at execution time from `vault.decrypted_secrets`, sends it only as
  `apikey`, and emits no `net.http_post` request without a non-empty value.
- Read `supabase/config.toml` and the handler. Only Materialise disables gateway
  JWT verification. `withSupabase({ auth: "secret:materialise" })` protects the
  handler, and the test proves an absent key returns 401 before database reads.
- Read `.env.example`, `.github/workflows/_env-example.yml`, and
  `scripts/bootstrap-env.sh`. Each inventories
  `ORMA_MATERIALISE_SECRET_KEY` as required secret material. No value appears
  in tracked configuration, migration text, or the cron command.
- Read the `call_runs` uniqueness constraint and materialisation predicates.
  The durable idempotency key remains unique. Deactivation still targets only
  future scheduled runs for the affected slot.
- `git diff --check` passed.

## Prior-finding residue

Round-one C1 has zero residue. The tracked named cron job invokes Materialise at
00:10 UTC.

Round-two C1 has zero residue. The scheduler uses a Vault-backed named secret,
fails closed when it is absent, and reaches a handler that independently
authorizes that secret despite `verify_jwt = false`.
