# T2.2 review, round 1

**Verdict:** REMEDIATE

**Counts:** C 0, H 3, M 1, L 0

`deno test supabase/functions/_shared/briefing.ts` passes all three unit tests.
Those tests stub the RPC response and do not execute the briefing SQL.

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| H | `supabase/migrations/20260912160000_briefing.sql:18-21` | An item without `since_date` ages from `i.created_at::date`, which is the database session date, not the profile's local date. For a non-UTC profile, an item created after UTC midnight can be one day younger locally than the briefing reports. At the 30-day threshold this promotes a non-aged item into the lead. | In an isolated database, create an `Asia/Kolkata` profile and an open item at `2026-08-13 20:00:00+00`. Call the RPC with `p_now = 2026-09-12 00:00:00+00`. Its local creation date is 14 August and its age is 29 days. The current function reports 30 days and emits an aged lead. | Reverting local conversion for `created_at` restores incorrect age claims for every profile west or east of UTC around local midnight. |
| H | `supabase/migrations/20260912160000_briefing.sql:49-60` | `last_call_summary` has no valid persisted source. The schema defines `calle_confidence` as the provider score and label, not its summary. `briefing` is the previous call's input, so it contains that call's own prior summary instead. A completed CALL-E fixture has a top-level `summary`, but no implemented storage path writes it to either expression. The next call therefore gets an empty or stale summary. | Insert a completed `call_runs` row whose `calle_confidence` is `{"score":0.82,"label":"high"}` and whose briefing has no prior summary, matching `testdata/calle/call-completed.json` and `spec.md` §2. Call the RPC. `last_call_summary` is empty although the completed provider payload contains a summary. No test exercises this row shape today. | Reverting a durable provider-summary source means completed calls cannot supply the required `last_call_summary` substitution. |
| H | `supabase/functions/_shared/briefing.ts:152-205`, `supabase/migrations/20260912160000_briefing.sql` | The claimed golden test is a TypeScript object that already contains the desired lead string. It never applies or calls the SQL function, inserts four `item_mentions`, or validates the 34-day calculation. The task's required fixture proof can pass while the database emits a different lead, including the timezone defect above. | Add an isolated database test that applies the migration, inserts a fixture profile, item, four mention rows, and a 34-day age, then calls `public.assemble_briefing`. Assert the returned `lead_line` verbatim and the rendered task byte-for-byte against the reviewed template. No database-backed briefing test exists today. | Reverting SQL ordering, count, age, or grant logic continues to leave the three mocked TypeScript tests green. |
| M | `supabase/migrations/20260912160000_briefing.sql:43-78` | The lead caps qualifying items at two, but emits every qualifying fact for each item. Two overdue, repeated, and aged items produce six separate facts. The product requires one or two things in the lead, so the call can open with a long list instead of its promised brief signal. | Insert two open items that are overdue, have three mentions, and are 30 days old. Call the RPC and count the sentences in `lead_line`. It returns six facts today, not one or two. | Reverting a fact-level cap lets a busy account turn the lead into the whole walk, which defeats give-before-take. |

There is no residue from an earlier T2.2 review. This is round 1.
