# T2.1 remediation, round 2

This remediation addresses C1 from `t2.1-round2.md`. It does not change the
verdict.

| Finding | Remediation | Independent pin | Mutation |
| --- | --- | --- | --- |
| C1 | Materialise now sets `verify_jwt = false` only for its function and wraps its handler in `withSupabase({ auth: "secret:materialise" })`. The cron command reads `ORMA_MATERIALISE_SECRET_KEY` from `vault.decrypted_secrets` during each execution and sends it only in `apikey`. The query returns no rows when Vault lacks a non-empty value. `.env.example`, the GitHub environment inventory, and `bootstrap-env.sh` all require the same secret. | `rg -n "secret:materialise|verify_jwt = false" supabase/functions/materialise/index.ts supabase/config.toml` shows the protected handler and function-specific gateway setting. `rg -n "vault.decrypted_secrets|ORMA_MATERIALISE_SECRET_KEY|apikey" supabase/migrations/20260912150000_schedule_materialise.sql` shows the runtime Vault lookup and no committed API key. `deno test --allow-env --allow-net supabase/functions/materialise/index.ts` confirms the materialisation behavior remains pinned. | Removing the named-key check exposes a service-role-writing endpoint. Restoring the bearer publishable key makes the scheduler unauthenticated. Removing the Vault predicate permits a request with no secret. |
