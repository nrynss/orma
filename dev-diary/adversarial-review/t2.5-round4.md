# T2.5 review, round 4

Verdict: **REMEDIATE**

Counts: C 0, H 0, M 0, L 1

This is a narrow round. It confirms the code has not changed since round 3, judges the three transfers, and states residue. I did not implement, review or remediate T2.5 before.

## Findings

| Where | What | Pin | Mutation |
| --- | --- | --- | --- |
| L. `dev-diary/PHASE-2-call-engine.md:117`, the T2.5 `owns` line, against `dev-diary/adversarial-review/t2.5-contract-change.md:11` | T2.5's `owns` line lists only `supabase/functions/_shared/poll.ts`. T2.5 also writes `supabase/functions/tick/index.ts` and `supabase/functions/calle-webhook/index.ts`. The first contract-change row adds both paths to T2.5. The closing note withholds only the last row, so that first row stands as applied. The block never received it. If T2.5 lands as written, the board shows T2.6 and T2.7a as the only writers of those files. That record is false. | Read line 117. It names one path. Read `git diff --stat HEAD` for the other two paths. It shows 190 changed lines in `calle-webhook/index.ts` and 224 in `tick/index.ts`. Remediation rounds 1 and 2 each list all three as edited files. | Add `supabase/functions/tick/index.ts` and `supabase/functions/calle-webhook/index.ts` to the T2.5 `owns` line. Revert that and the landed board again hides two of T2.5's three written paths. |

This finding meets all four conditions for orchestrator closure in `AGENTS.md`. It is markdown under `dev-diary/`. The fix touches no code, migration, policy, configuration or schema. Reading confirms it. This file records it by name and file. So the orchestrator may close it in the landing commit.

## Step 1. Code unchanged since round 3

I diffed each live file against round 3's frozen copy at `/tmp/orma-review-t25-r3`.

| File | `diff -u` result | SHA-256, live and frozen |
| --- | --- | --- |
| `supabase/functions/_shared/poll.ts` | no output, identical | `67e63f48…aa456` both |
| `supabase/functions/tick/index.ts` | no output, identical | `393879de…4bb66` both |
| `supabase/functions/calle-webhook/index.ts` | no output, identical | `58ca6f90…a3cce` both |

No T2.5 line differs. So I ran no live pins and no stub CALL-E. I placed no call and did not touch `up.sh`.

## Step 2. Type check and tests

`deno check` on the three files printed three `Check` lines and exited 0.

`deno test --allow-env --allow-read --allow-net` on the three files reported this.

- `calle-webhook/index.ts`: 31 tests
- `_shared/poll.ts`: 16 tests
- `tick/index.ts`: 52 tests
- Total: `ok | 99 passed | 0 failed`

Several log lines read "failed", such as `poll failed for run poll-thrown`. These are expected output from tests that inject failures. No test failed.

## Step 3. Transfer judgement

**Findings 1 and 2 to T2.5a.** The `owns` line for T2.5a at `PHASE-2-call-engine.md:137` lists `calle-webhook/index.ts` and `poll.ts`. It also lists a new migration and both type files. Both findings sit in those two code files. The done condition names pins F1a, F1b and F1c from round 3. It also demands both race orderings, a second event recording `already_resolved`, and a winning redelivery recording `applied`. That covers both findings. T2.5a `requires: T2.5`, so it cannot start before T2.5 lands. Its migration timestamp `20260913120000` sorts after the ingest migration it builds on. The transfer is sound.

**Finding 3 to T2.7.** The `owns` line for T2.7 at `PHASE-2-call-engine.md:197` lists `supabase/migrations/20260912180000_ingest.sql`. The fix belongs in `ingest_call_result` in that file. The done condition for T2.7 does not name `calle_failure`. The finding does not live there. It lives as an added row in the orchestrator note of `t2.7-round2.md`. That note raises the counts to C0 H1 M3 L3 and keeps the verdict at REMEDIATE. T2.7 is still `claimed`, not `done`. Its loop cannot reach APPROVE until a later round finds zero residue against that row. So the finding is tracked where the fix can be made. The transfer is sound.

**Landing T2.5 first.** I checked every defect and residual risk from rounds 1 to 3 for a home.

- The possibly mislabelled `refetched` outcome is findings 1 and 2. T2.5a tracks it.
- The `poll_timeout` that survives a late ingest is finding 3. T2.7 tracks it.
- A poll whose `polled` insert failed leaves its move with no timeline row. Round 3 folded this into finding 2, and T2.5a's `terminal_writer = poll` records the writer. Pin F1b is in its done condition.
- The webhook's move writes no provider detail. T2.7a re-fetches every terminal run before ingestion, and its done condition covers a webhook-terminalised run.
- The finaliser is still `unavailable("finalisation")`. T2.7a owns its replacement.
- Round 3 left two non-defects: a stale `poll_after` on a timed-out run, and possible double polling by overlapping ticks. `duePolls` filters on state, so it never polls that run again. The state guard still yields one winner. Neither is a defect.

The only untracked defect is the `owns` line above. It is therefore a finding against T2.5.

## Step 4. Zero residue

Round 3 reverted each round 1 and round 2 fix in its own copy, and each unit pin failed. Step 1 shows the code is unchanged since then. So rounds 1 and 2 leave zero residue.

Round 3 left three findings. None is T2.5 residue. T2.5a tracks findings 1 and 2. T2.7 tracks finding 3. No `t2.5-remediation-round3.md` exists, because no code changed.

T2.5 carries zero residue against rounds 1, 2 and 3. It carries the one new finding above.

## Residual risk

- **Live timing proof is still open.** The T2.5 done condition needs two live runs against the real CALL-E service. One must reach a terminal state by polling alone, with the webhook deliberately unregistered. Another must show the poll is a no-op once the webhook wins. Both need a real CALL-E key, and no round has run them. A live call is evidence, not a gate.
- **Interim mislabel until T2.5a lands.** A redelivered event can record `applied` for a move it did not make. The run's state stays correct. Only the timeline attribution is wrong.
- **Interim false failure until T2.7 fixes finding 3.** A call that recovers after a give-up keeps `poll_timeout` in `calle_failure`.
- **Two terminal events per call.** Pin F1a assumes CALL-E can send both `call.completed` and `call.result_validation_failed` for one call. Nobody has observed that.
- **Overlapping ticks.** `duePolls` takes no claim, so two overlapping ticks can poll one run twice. No round has pinned this.
- **`refetched` keeps `state` beside `outcome`.** T2.9 should read `outcome` before it trusts that state. The T2.9 block does not say this yet.
- **Shared `tick/index.ts`.** The same uncommitted diff holds T2.5's poll wiring and T2.8's dry-run edits. The landing commit must separate or deliberately combine them.

## Cleanup

I created no rows, started no process and placed no call. I wrote only this file.

## Orchestrator note: documentation finding closed directly

Recorded by the orchestrator on 2026-09-13, under the documentation exemption in `AGENTS.md`.

The single L finding is closed. `dev-diary/PHASE-2-call-engine.md`, T2.5 block, `owns` line now reads `supabase/functions/_shared/poll.ts, supabase/functions/tick/index.ts, supabase/functions/calle-webhook/index.ts`. This matches the first row of `t2.5-contract-change.md`.

All four exemption conditions hold. The finding lives in markdown under `dev-diary/`. The fix touches no code, migration, policy, configuration or schema. Confirming it needs reading only. This note records it by name and file.

With it closed, T2.5 carries no open finding at any severity. It is ready to land.
