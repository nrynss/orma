# T1.5 round 1

## Verdict

APPROVE

| Severity | Count |
| --- | ---: |
| C | 0 |
| H | 0 |
| M | 0 |
| L | 0 |

No findings. There are no prior review rounds, so no earlier residue exists.

## Independent pins

| Claim | Pin | Observed result |
| --- | --- | --- |
| Recorded valid result parses into typed values. | `deno eval --allow-read` read `testdata/calle/call-completed.json` and passed `structured_result` to `parseStructuredResult`. | A non-null result retained the captured item, evidence offset, empty retired list, commitments, mood, and slot request. |
| Malformed result is discarded with a durable reason. | The same isolated probe added an unknown top-level field. | The parser returned `result: null` and `structured_result.unknown is not allowed`. |
| Missing retirement evidence is discarded without a partial result. | The same isolated probe passed one retired item with no `evidence_offset_seconds`. | The parser returned `result: null` and named the required missing evidence field. |
| The shared parser type-checks. | `deno check supabase/functions/_shared/result.ts`. | The command completed successfully. |
