# T6.3 round-1 review: History

Verdict: REMEDIATE.

Counts: C 0, H 0, M 0, L 1.

## Pins

Seed run `40000000-0000-4000-8000-000000000001` is `completed`, `answered_extracted`, mood `energised`. `transcripts` holds 1 row with 2 turns (`agent` at offset 0, `user` at offset 8). `results` holds 1 row, `valid` true, with empty `retired_items` and one commitment (`30000000-0000-4000-8000-000000000001` at offset 8, due 2026-09-15). `item_mentions` holds 3 rows on the run (offsets 0, 2, 4). `commitments` holds 1 row (evidence offset 8).

Feeding those rows through `lastCallSummary` gives retired 0, committed 1, captured 0, `mentionCount` 3, with one Committed line at offset 8. The capture candidate (`Call Amma`, source `call`) predates the run (created 2026-09-06, run 2026-09-13), so it correctly stays out. History calls `lastCallSummary(run, rows.mentions, rows.commitments, result?.structured ?? null, zone, now)` (`+page.svelte:55`) from the same `$lib/today/model` import Today uses (`app/+page.svelte:47`), with the same inputs. Counts agree by construction.

PostgREST as `local@example.com` returns `call_runs` newest first by `scheduled_for` (scheduled, canceled, seed, then `0011`, `0012`, `0013`). The page query adds `state neq scheduled` and `state neq canceled` and returns the 4 completed runs plus one `failed` row (see drift note). `transcripts` returns 1 row, `results` 1 row, `item_mentions` 3 rows on the seed run, `commitments` 1 row. `firstrun@example.com` reads `[]` for `call_runs`, `transcripts`, and `item_mentions`.

Dev server on 5199: anonymous `GET /app/history` returns 303 to `/login` (`hooks.server.ts:68-70`). With a session cookie it returns 200 and the shell boots the client bundle (`kit.start` plus `app.js`). `?run=<id>&t=8` returns 200. The page sets `ssr = false`, so param handling is client side. `requestedRun` comes from `page.url.searchParams.get('run')` (line 28) and `requestedT` from `parseTParam` (line 29, `model.ts:57-62`). `expandedId` picks the requested run when it matches, else the newest run (lines 30-34). `isMarked` matches the turn whose `offsetSeconds` equals `requestedT` in the expanded run (lines 95-97). The mark renders as `data-turn-mark="true"` (line 152) and the effect scrolls to it (lines 99-102). Summary lines and turn offsets link through `historyHref` (`model.ts:51-54`), used at lines 137 and 154.

Speakers map `agent` to Orma and `user` to You (`model.ts:15-21`), matching both fixture turns. Offsets format through the shared `formatOffset`. Clocks use `formatClock` and `formatDayLabel` in `zone`, read from the profile timezone with an `Asia/Kolkata` fallback (lines 25, 60, 78). `MoodMarker` takes only `label` and `none` and exposes no edit path. Runs `0011` through `0013` have no `results` rows, so `hasResult` is false and the honest line renders (lines 145-147). The `failed`/`not_answered` row sets `noConnection` (line 59) and shows the did-not-connect line (lines 127-129). Live states render the on-the-call header with no summary (lines 120-122). Comments in `model.ts` and `+page.ts` describe this behavior accurately.

Fixture drift: a `failed`/`not_answered` run `4f000000-0000-4000-8000-00000000f901` for the local user appeared between my first and second queries (`call_runs` count 7 to 8). I inserted nothing. It usefully exercises the failed branch and changes no assertion above.

Scope: `git status` shows `web/src/routes/app/history/` (three files) and the handoff for this task, plus siblings paths (`patterns/`, `timeline/`, landing, their handoffs). No shared file changed.

## Findings

| # | Where | What | Pin | Mutation |
|---|---|---|---|---|
| 1 (L) | `web/src/routes/app/history/+page.ts:69` | The loader fetches `items(id, text)` for the user, but the view never reads `rows.items`. No line in `+page.svelte` references it. It costs one extra parallel PostgREST round trip per load with zero readers. | Grep finds no `rows.items` consumer in `history/+page.svelte`. The loader still selects the table on every load. | Deleting the fetch changes nothing visible. Keeping it invites a future reader to assume the view depends on it. |

## Done-when assessment

Phase doc: a fixture run renders its turns in order, clicking a retirement jumps to the offset where it was said, and a run with no result still shows its transcript.

1. Turns in order: pass. `parseTurns` sorts by offset (`model.ts:37-41`). The seed run carries turns at 0 and 8, which PostgREST returns verbatim.
2. Retirement jumps to its offset: pass on the code path, with a fixture caveat. No seed `retired_items` row exists, so no retirement link occurs in data. The identical path (summary line link at line 137 through `historyHref` to `isMarked` plus scroll) is exercised by the commitment line at offset 8. The task owns no fixture paths, so this is not a defect.
3. No-result run still shows its transcript: pass on the code path. Lines 145-162 render the honest line and any turns independently of `hasResult`. Runs `0011` through `0013` carry neither results nor transcript rows, so their transcript section is empty by data, not by code.
