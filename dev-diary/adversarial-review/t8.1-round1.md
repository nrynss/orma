# T8.1 round 1 review

- Reviewer: RevT81R1 (orchestrator session, did not implement)
- Date: 2026-09-14
- Subject: uncommitted `scripts/seed.ts` on `main` at `7dd77df`, 1002 lines
- Specification: the T8.1 block in `dev-diary/PHASE-8-ship.md`

## Verdict

**REMEDIATE**

| C | H | M | L |
| --- | --- | --- | --- |
| 0 | 2 | 2 | 2 |

The done-when list holds on the local stack. Two defects break what the seed exists for. A rerun undoes the demo's live retirement and deletes real mentions. The seeded history is too sparse for the analysis function, so Patterns has nothing to show.

## Scope

`git status` shows `scripts/seed.ts` as the only new T8.1 path. The phase doc status line reads `claimed:t8.1-impl`. No review file existed before this one. I read the whole script, the T8.1 block, `assemble_briefing`, `compute_pattern_facts`, the `analysis` report window, and the dry-run dispatcher. I changed no code.

## Independent evidence

Every measurement below is my own run against `scripts/p6-local-stack.sh` (API on 58321), account `local@example.com`, `--demo-date 2026-09-14`. Counts are for that account. The stack was torn down afterwards, and zero `orma-p6-local` containers remain.

1. **Baseline.** items 4, runs 6, mentions 3, events 2, transcripts 1, results 1, slots 1, consents 1, scheduled 1.
2. **Plan run.** Exit 0. Every count unchanged.
3. **First `--apply`.** items 7, runs 9 (3 with `orma:seed:` keys), mentions 8, events 11, transcripts 4, results 4. Slots, consents and scheduled runs unchanged at 1, 1 and 1.
4. **Second `--apply`.** Identical counts. The `call_events` ids changed, because the script deletes and reinserts them.
5. **Briefing.** `assemble_briefing(user, '08:00', '2026-09-14 09:00+05:30')` lead line reads "You've mentioned the dentist 3 times. It's been 34 days." The script's own check printed both ticks.
6. **Seeded runs.** 2026-08-17 `ok`, 2026-08-31 `ok`, 2026-09-11 `unknown`. All `completed`, `dry_run = true`, `terminal_writer = dry_run`.
7. **Non-local guard.** `ORMA_API_URL=https://orma-api.nryn.dev` with `--apply` and no confirmation flag exits 1 before any request.
8. **`--remove`.** Every count returns to the baseline in item 1. Zero seeded items remain. The T1.6 seed rows survive.

## Findings

| ID | Sev | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- | --- |
| F1 | H | `applyPlan`, items upsert (`status: "open"`) and the `item_mentions` delete by `item_id` | A rerun reverts live state and deletes rows the seed did not write. The demo retires the dentist through Items, per T8.4. Any later `--apply` sets it back to `open`. The rerun also deletes every mention of a seeded item, including mentions a real surface added. | Retire `the dentist` in SQL as a web retire does, and insert one mention with a null run. Before rerun: `retired mentions=4`. After `--apply`: `open mentions=3`. | Restoring the blind upsert and the delete-all-mentions path brings back the revert and the lost row. |
| F2 | H | `buildPlan`, run dates 28, 14 and 3 days before the demo | Patterns stays empty for a seeded production account. `analysis` reports on the last seven local days ending yesterday and skips when `sufficient_history` is not true, which needs three observed days. Only one seeded run falls in that window. T8.4 films Patterns, and the phase says patterns come from the real analysis over seeded rows. | Seeded runs with `local_date` between 2026-09-07 and 2026-09-13: 1. On the harness the window only passed because harness-only rows supplied the other days. On an account holding only seeded runs, `observed_days` is 1. | Moving the runs back outside the last seven days empties the report again. |
| F3 | M | `applyPlan`, mention offset `captured?.evidence_offset_seconds ?? 0` | Mentions point at the wrong moment. The passport mention on 2026-08-31 sits at offset 0, a turn about the dentist. The passport is said at 0:10. History links an offset into the transcript, so the jump lands on the wrong line. | Turn at each seeded mention offset: `renew the passport @0 -> "Hi … The dentist keeps coming up."` | Reverting to offset 0 for non-captured items repeats the mismatch. |
| F4 | M | `applyPlan`, `dispatched` event `request_body: "{}"` and run `briefing: null` | The Timeline shot shows an empty request. The dry-run dispatcher writes `maskedRequestBody(exactBody)`, the masked CALL-E request with its briefing variables (`dispatch-mode.ts:560`). The task says to copy the shapes that dispatcher writes. | `dispatched.detail.request_body` on a seeded run is `{}`. `call_runs.briefing` is null on all three. | An empty body again reads as machinery that sent nothing. |
| F5 | L | `lookupProfile`, `auth.admin.listUsers()` | Only the first page of users is searched. The default page is 50. An account beyond it reports no user. | Code reading. The call passes no page or filter. | A larger user list fails the lookup silently for later accounts. |
| F6 | L | `buildPlan`, `weekend groceries` | The item is internally inconsistent. Its source is `web` and it was created five days before the demo, yet run 3 captures it from the call three days before. | Plan output and `items.created_at` against the run-3 capture. | History shows a capture of an item that already existed. |

## Notes, not findings

- `--remove` against a non-local URL has no confirmation flag. Deleting only seeded rows is the rollback, so this is left to the operator's judgement.
- The disclosure paragraph exists and prints with every plan. T8.3 and T8.4 can lift it from there.
- Run 3's transcript says "It's been 31 days", which is correct for three days before the demo.
- The implementer reported the task done. Per `AGENTS.md`, it stays `claimed` until a review round approves it.
