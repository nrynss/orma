# Adversarial review: T6.1, round 1

- Reviewer: RevT61R1
- Date: 2026-09-14
- Subject: commit `512b472` on top of the claim commit `7d46692`, branch `main`.
- Specification: `dev-diary/PHASE-6-web-app.md` task T6.1, `t6.1-contract-change.md`, spec §2, §3, §7, §11 and §13.
- Verdict: **REMEDIATE**

## Counts

| Severity | Count |
|---|---|
| C | 0 |
| H | 1 |
| M | 3 |
| L | 1 |

## Scope

`git diff --name-only 7d46692 512b472` lists 20 paths. Nineteen sit on the T6.1 owns line. The twentieth is `dev-diary/adversarial-review/t6.1-handoff.md`, which is review markdown. No other path changed.

## Independent evidence

Every item below is a fresh measurement by this reviewer. Nothing is quoted from the handoff. No key, JWT or cookie value is written here.

1. **Check and build.** In `web/`, `npm run check` printed all five node checks as passed, including `today check passed`. svelte-check reported 307 files, 0 errors and 0 warnings. `npm run build` exited 0 with the Cloudflare adapter and wrote `_worker.js`.
2. **Secrets in the bundle.** A grep for `service_role`, `sb_secret`, `SUPABASE_SECRET` and `SECRET_KEY` over `.svelte-kit/cloudflare` and `web/src` matched one chunk. That match is the supabase-js key prefix test `startsWith("sb_secret_")`, not a key.
3. **Harness up.** `scripts/p6-local-stack.sh up` applied 17 migrations and the seed. It printed no key and no key name. It started five `*_orma-p6-local` containers. The other stacks on the machine kept running.
4. **Harness guards.** A second `up` while the stack existed exited 1 with the workdir message. `ORMA_P6_WORKDIR=/home/nryn/x ... down` exited 1 on the workdir guard. After `down`, a bounded listener on 58324 made `up` exit 1 with `port 58324 is already in use`, and it created no workdir and no container.
5. **Harness down.** `down` left 0 containers, 0 volumes and 0 networks matching `orma-p6`, and no `/tmp/orma-p6-stack`. The 13 containers of the other stacks were still up.
6. **Seed account SQL.** The scheduled run sat at `2026-09-15 02:30 UTC`, which is 08:00 Asia/Kolkata. A canceled run sat earlier, at 09:26 local today. The completed run had 3 `item_mentions`, 1 `commitments` row and 0 retired mentioned items. Open items had 1, 1, 1 and 0 mentions, with ages 34, 21, 8 and 2 local days.
7. **Seed account page.** The browser zone was Asia/Calcutta. /app showed Next call `Tomorrow, 08:00` and skipped the canceled run. It showed `Answered · 3 mentions`, one Committed line at `0:08`, and `Open · 4` with exactly the SQL counts and ages. There was 1 `nav`, 0 `.shell-bar`, and `aria-current` sat on Today.
8. **First-run account.** Profile zone America/New_York, 0 runs. Before 04:00 UTC the page read `Sunday 13 September`, `Welcome, First Run` and `Your first call Tomorrow, 08:00`, while the browser sat on Monday. After 04:00 UTC, which is New York midnight, the same page read `Monday 14 September` and `Today, 08:00`. The page follows the profile zone across a local midnight.
9. **Live account.** Profile zone Europe/London, one `awaiting_result` run. The page showed `Calling now`, `Your 04:50 call`, clocks in London time, and the boiler item at 2 mentions and 12 days, matching its two rows.
10. **Polling.** On the live page, the `call_runs` resource count grew from 5 to 6 over 20 seconds. After a client-side move to Settings it held at 6 for 36 seconds. Back on Today, SQL set the run to `completed`. Within 20 seconds the page flipped to the normal state at a count of 9, then held at 9 for 36 more seconds. No interval outlives the page or the run.
11. **Isolation.** With a password grant, `firstrun@example.com` read 0 `call_runs`, 0 `item_mentions`, 0 `commitments` and 0 `transcripts`, while the seed account read 3, 3, 1 and 1. An explicit `user_id=eq.<seed id>` filter as firstrun returned 0 rows. Each account's /app showed only its own rows.
12. **Redirects.** Signed out, `/app`, `/app/settings`, `/app/onboarding` and `/app/items` answered 303 to `/login`. `/login` answered 200 with 0 `href="/login"`. Signed in, `/login` answered 303 to `/app` for all three finished accounts. A new auth user with no profile got 303 to `/app/onboarding` from `/login`, `/app` and `/app/settings`. The magic-link callback with an admin `generate_link` token answered 303 to `/app` for the seed account and to `/app/onboarding` for the no-profile account. Outside `/app` signed in, the shell bar links Today.
13. **Error state.** With `select` on `call_runs` revoked from `authenticated` in the scratch database, /app showed `Could not read your calls: permission denied for table call_runs`, not blank panels.
14. **Phone and theme.** At 375 by 812 in light scheme, `.o-app` painted `rgb(251, 250, 248)` and `scrollWidth` equalled 375. The dark scheme rendered the TodayDark palette. Settings rendered the Login Widget and `Mint Start link` under one app nav marked Settings.
15. **Mutants of the Today check.** Each mutant ran on a copy in the scratchpad, never in the repo. `git status` stayed clean.

    | Mutant | Result |
    |---|---|
    | Clock via `Date#getHours` | fails |
    | Canceled run picked as next | fails |
    | Past scheduled run picked as next | fails |
    | `daysFromNow` in the browser zone | fails, only through the commitment due label |
    | `daysFromNow` in UTC | **passes** |
    | Canceled counted as a call for first run | fails |
    | Commitment mention not skipped | **passes** |
    | Retirement accepted at any time | **passes** |
    | Capture accepted at any time | fails |
    | Canceled run picked as last call | **passes** |
    | Created-at age in UTC | fails |
    | Now read in UTC for age | **passes** |
    | `addedLabel` in UTC | **passes** |
    | One pass in `zonedTimeToInstant` | **passes** |
    | Live states narrowed | fails |
    | Inactive slot rings | fails |
    | Today marked on every nav path | fails |
    | Mention count from lines, not rows | fails |
    | `postAuthPath(true)` back to Settings | onboarding check fails |
    | Onboarding submit back to Settings | onboarding check fails |

16. **Retirement probe.** SQL retired `Renew the passport` with `retired_at = now()`, as a web retire would. That was a day after the seed call, which only mentioned it at offset 2. Today then showed `RETIRED Renew the passport 0:02` under Last call.
17. **Consent probe.** SQL revoked firstrun's only `outbound_calls` consent, leaving 0 live consents. Today still showed `Your first call Today, 08:00`. `supabase/functions/_shared/calle.ts:347-350` refuses any run whose profile has no live `outbound_calls` consent.
18. **Settings under the harness.** Signed in against the harness, Settings showed `profiles telegram read failed`. Its resource list held `https://orma-api.nryn.dev/rest/v1/profiles` beside the local `http://127.0.0.1:58321` reads. `web/src/lib/telegram-link.ts:16` hardcodes the production API.
19. **Documentation style.** Added comments and the handoff hold no em dash and no prose semicolon. No authored sentence runs past 30 words. The only phone strings are the reserved fictional `+1555555010x` harness numbers.

## Findings

| ID | Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|---|
| F1 | H | `web/src/lib/today/model.ts:396` `lastCallSummary` | A Retired line needs only a mention on the run plus `status = retired` with `retired_at` after the run started. An item mentioned on the last call and retired later on the web, by MCP or by Telegram shows as retired on that call, with a false offset. Retiring an item that calls keep mentioning is the core T6.2 path, so this will be common. Today invents a retirement the call never made. | Evidence 16. After a later web retire, /app shows `RETIRED Renew the passport 0:02` for a call whose rows record no retirement. The Today check does not catch it: the any-time retirement mutant passes. | Revert the fix and a web retire after a call again reads as retired on that call. A check case with `retired_at` after the run ends must fail. |
| F2 | M | `web/src/routes/app/+page.svelte` first-run and Next call cards, `nextSlotInstant` fallback | Today promises a call to an account that cannot be called. A profile with no live `outbound_calls` consent, such as a T5.4 skip user or a revoked one, sees `Your first call Today, 08:00` or a Next call time. The dispatcher refuses that run. Spec §13 says no call is dispatched without consent. The page never reads `consents`. | Evidence 17. With 0 live consents, /app shows `Your first call Today, 08:00`, and `calle.ts:347-350` refuses the run. | Revert and an unconsented account again waits for a call that never rings. A page or check case with no live consent must show no call time. |
| F3 | M | `web/src/lib/today/check.ts` | The check does not pin several claims the handoff says it pins. Six mutants survive. They cover a canceled run as last call, which the brief names. They also cover a double-counted commitment mention (handoff claim 6), Today and Tomorrow in UTC, and now read in UTC for age. The last two are `addedLabel` in UTC and a single DST pass. | Evidence 15. Each surviving mutant prints `today check passed`. | Revert the strengthened cases and those regressions ship green again. |
| F4 | M | `scripts/p6-local-stack.sh` and the handoff run steps, with `web/src/lib/telegram-link.ts:16` | The harness exists so P6 work never probes production. Under it, Settings still sends its reads to `orma-api.nryn.dev` with a local JWT, and Mint and Unlink would post there too. Settings shows `profiles telegram read failed`, and the handoff does not mention it. T6.5 builds on this page. The fix sits outside the owns line, so it needs a contract change or a recorded limit. | Evidence 18. The resource list on local Settings holds `https://orma-api.nryn.dev/rest/v1/profiles`. | Revert and a P6 agent on the harness again sends Settings traffic to production without being told. |
| F5 | L | `web/src/routes/app/+page.svelte` live branch, `liveSteps` | The live state departs from `TodayLive.dc.html`. The artboard lists `On today's call` with `Orma is leading with these, in this order.`, taken from the briefing order. The page shows `Open · N` sorted by mentions instead. The first step reads `Picked up by the scheduler` where the artboard reads `Picked up`. | Evidence 9 against the TodayLive artboard text. | Revert and the live screen again drops the briefing order the design shows. |

## Notes

- The handoff opens with "Nothing is committed". The work landed in `512b472`, so that line is stale. It is review markdown, not product.
- At phone width, the offset links (20px), `Read the transcript` (24px), `All items` (24px) and Sign out (22px) sit under 44px tall. The artboards draw the same inline text links, and System applies 44px to buttons and controls. Every button and nav link here measured 44px or more.
- The seed commitment has its mention at offset 0 and its commitment at offset 8. Ingest writes both at one offset, so the harness never exercises the commitment skip. F3 covers the check side.
- A NY slot at 02:30 on the spring-forward day resolves to 03:30 EDT. No materialiser comparison was made. Not a finding.
- A run stuck in `claimed` would keep Today in its live state and polling indefinitely. `abandon_writes_state` should end such runs, so this is recorded, not filed.
- `open` items read with no row limit, and their mentions go through one `in` filter. PostgREST caps responses at 1000 rows by default. A heavy account could undercount silently later. Not reachable with demo data.
- The harness rows used earlier were changed by this review's probes, then destroyed by `down`. No linked project was touched. No code, config, phase doc or status line was edited. No commit was made.
