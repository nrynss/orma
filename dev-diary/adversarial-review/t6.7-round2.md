# T6.7 round 2 review

## Verdict

**APPROVE.** Production push: **GO.**

Reviewer: T67Rev2. This reviewer is fresh. It touched nothing in T6.2, T6.5, or T6.7. It fixed nothing.

| Severity | Count |
| --- | --- |
| C | 0 |
| H | 0 |
| M | 0 |
| L | 0 |

## Scope and evidence

This round read AGENTS.md in full. It read the T6.7 block, the round 1 file, the reopen note, the owner actions migration, and the storage policies migration. It brought up a fresh local stack with `scripts/p6-local-stack.sh up`. It ran every pin below against that stack through direct SQL. It masked all phone numbers. It changed no implementation, migration, type, or status file. It ran no db push and no deploy. It placed no calls. It tore the stack down afterwards. Zero `orma-p6-local` containers remain.

## Pin 1: setting match, re-measured

The `set_config` line in `delete_my_account` reads exactly:

```sql
perform set_config('storage.allow_delete_query', 'true', true);
```

The safeguard body in the migration `DO` block reads exactly:

```sql
IF COALESCE(current_setting('storage.allow_delete_query', true), 'false') != 'true' THEN
```

Both names read `storage.allow_delete_query`. They match character for character. The live stack confirms parity. Its shipped `storage.protect_delete` checks the same name. Its `protect_objects_delete` trigger reads `BEFORE DELETE ON storage.objects FOR EACH STATEMENT`.

## Pin 2: guard parity, re-measured

This round built a scratch user with one storage row, one auth row, and one profile row. Counts before read 1, 1, and 1.

A direct delete from `storage.objects` failed with this error:

```text
ERROR:  42501: Direct deletion from storage tables is not allowed. Use the Storage API instead.
HINT:  This prevents accidental data loss from orphaned objects.
```

The storage row count stayed at 1 after the failed delete. The guard held.

`delete_my_account` for the scratch user returned true. Counts after read 0 in storage, 0 in auth, and 0 in profiles. The function removed the storage row through the active safeguard. The existing cascades removed the rest.

## Pin 3: no-op proof, re-measured

This round captured the catalog state, then re-applied the `DO` block verbatim. Function definition md5 read `4371ae544fb1ef704f9c5d42265ac324` before and after. Trigger definition md5 read `45c5692d486583888bac16ef789d847f` before and after. Catalog counts stayed at 1 function and 1 trigger. The second application changed zero bytes.

The live function body also proves pre-existence. Its comment reads `set to 'true'`. The mirror comment reads `is set to true`. The block therefore created nothing here. Both creations sit behind existence checks on `pg_proc` plus `pg_namespace` and on `pg_trigger` plus `storage.objects`. The block stays a no-op in production where both objects already exist.

## Pin 4: cancel regression, re-measured

| Case | Result | Observation |
| --- | --- | --- |
| Cross-user cancel of a scheduled run | false | Row stayed `scheduled` |
| Own scheduled cancel | true | State `canceled`, disposition `canceled`, `billable` false, exactly one `canceled` event |
| Repeat cancel | false | Row already `canceled` |
| Claimed run cancel | false | Row stayed `claimed` |
| Already canceled run cancel | false | No new event |
| Null id | raise | `cancelling a call run requires an id` |

The diff of migration lines 1 through 45 against git HEAD is empty. Every `cancel_call_run` line stays byte identical to the round 1 approved text.

## Pin 5: scope, re-measured

Exactly one `owner_actions` migration file exists. No new migration files exist. A production push therefore applies exactly one file.

`git status` additionally shows the T6.7 claim line in `PHASE-6-web-app.md`, one unrelated wording change in `spec.md`, and the untracked reopen note. This reviewer touched none of them. None of them ships a database change.

## Findings

None at any severity.

| ID | Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- | --- |
| | | | | | |

## Residue against round 1

Round 1 approved with zero findings against a stack without the safeguard. It missed the production trigger entirely. Local deletion passed there while production deletion would have raised 42501 and blocked account removal. This round covers that gap. Pins 1 through 3 above ran against a stack that ships the real trigger body.

## Production-push readiness: GO

Run these exact next commands. This round did not run them.

1. `supabase migration list`, then `supabase db push`. Confirm first that only `20260914040000_owner_actions.sql` is pending.
2. `npm run types:generate` in `web/`, only if the check reports drift. Both generated type files already carry matching `cancel_call_run` and `delete_my_account` signatures. The fix changed no signature, so expect no drift.
3. `npm run types:check` in `web/`. It must pass clean.

Risks in these commands:

- `db push` applies every pending migration, not one file. Confirm the pending list first so nothing else rides along.
- If production guarded a different setting name, deletion would fail closed with 42501. Measured names match the shipped body, so this risk reads low.
- The `DO` block needs rights to create in the `storage` schema where the guard is absent. Standard pushes run with full rights, so this risk reads low.
- `set_config` uses transaction scope in `delete_my_account`. The flag cannot leak into other sessions.
