# T2.4 remediation, round 2

This remediation addresses C1 from `t2.4-round2.md`. It uses recorded fixtures
only and placed no call.

| Finding | Remediation | Pin | Mutation covered |
| --- | --- | --- | --- |
| C1 | Added the named `tick-runs` `pg_cron` job at every UTC minute. It reads the non-empty `ORMA_MATERIALISE_SECRET_KEY` from database Vault during each run, sends it only as `apikey`, and posts to the deployed tick function. Tick now disables gateway JWT verification only for its function and checks `secret:materialise` before any database read. | `deno test --allow-env --allow-net --allow-read supabase/functions/_shared/calle.ts supabase/functions/tick/index.ts supabase/functions/_shared/events.ts` passes 30 checks. The scheduler-auth check proves a request without the named key returns 401 before reads. Read `20260912170000_schedule_tick.sql` to confirm the minute schedule, Vault lookup, empty-key guard, and tick endpoint. | Removing the job leaves calls scheduled. Removing the empty-key guard sends an unauthenticated request. Removing handler authorization exposes a service-role-writing endpoint. |

Validation also ran `git diff --check`, which passed.
