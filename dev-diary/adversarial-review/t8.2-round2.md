# T8.2 round 2 review

- Reviewer: gpt-5.6-sol, independent round-two reviewer
- Date: 2026-09-14
- Subject: uncommitted `.github/workflows/keepalive.yml` and `scripts/restore-check.sh`
- Specification: T8.2 in `dev-diary/PHASE-8-ship.md`

## Verdict

**APPROVE**

| C | H | M | L |
| --- | --- | --- | --- |
| 0 | 0 | 0 | 0 |

## Scope and independent checks

I read `AGENTS.md`, T8.2, the canonical workflow environment example, round 1,
the remediation, both owned files, and the current uncommitted diff. I changed
no implementation path.

The workflow parses as YAML. Its only workflow token permission is
`contents: read`. The job uses the separate `BACKUP_REPO_TOKEN` only in Git's
explicit authorization header for the private backup clone and push. The
checkout needs only the read permission. The dump itself stays in
`RUNNER_TEMP`, then a temporary clone that the exit trap removes. It is neither
an artifact nor a file in this repository.

I traced the activity command independently. `curl` suppresses the body and
writes only `%{http_code}` to `status`. The following exact string comparison
accepts `200` and exits one for each other value. Exercising that branch with
captured values produced `200 -> 0`, `204 -> 1`, and `302 -> 1`. `curl` has no
redirect-follow option, so a 302 is captured as 302. A transport failure exits
from `curl` before the comparison because the shell step fails on the command.

`bash -n scripts/restore-check.sh` and `git diff --check` both exited zero. The
restore script accepts one existing dump, runs a named PostgreSQL 17 container
with `--network none`, restores only there, and reads one count for each public
base table. It has no linked-project URL or credential input.

## Findings

| ID | Sev | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- | --- |
| None | - | - | No defect found. | Exact-status branch check, YAML permission parse, shell syntax and whitespace checks. | Replacing the equality guard with `curl --fail` lets 204 and 302 pass. Removing the top-level permission returns the workflow token to repository defaults. |

## Round-1 residue

| Prior finding | Re-review result |
| --- | --- |
| H1, non-200 keepalive responses passed | Closed. The status is captured and compared to the literal `200`. The 204 and 302 branches independently exit one. |
| M1, workflow token permissions followed repository defaults | Closed. The top-level `permissions` map grants only `contents: read`, while the backup write uses its separately scoped fine-grained token. |

There is zero residue from round 1. T8.2's daily plus manual trigger, session
pooler dump, custom public-schema format, local isolated restore check,
fourteen-dump retention, and secret handling remain intact.
