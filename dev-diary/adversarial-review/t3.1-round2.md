# T3.1 round 2 review

## Verdict

**APPROVE**

| C | H | M | L |
| --- | --- | --- | --- |
| 0 | 0 | 0 | 0 |

## Zero-residue claim

Round one contained H1 and M1. This review found zero residue from each.

| Prior finding | Independent pin | Result |
| --- | --- | --- |
| H1. JWT-off assigned to T5.1, and `config.toml` missing. | `test -f supabase/config.toml` exited 0. The file contains `[functions.telegram]` and `verify_jwt = false`. T5.1 `owns` remains `supabase/config.toml (auth block)`. PHASE-3 owns the telegram block. The contract note names T3.1 as owner. PHASE-5 tells T5.1 not to edit JWT-off. | Resolved. |
| M1. `presentedWebhookSecret` decoded the path rest. | Independent Deno probe. `telegramWebhookUrl("https://orma-api.nryn.dev", "abc%2Fdef")` then `presentedWebhookSecret` on that URL pathname returned `abc%2Fdef`, not `abc/def`. A POST to that registered URL returned status 200, not 401. | Resolved. |

## Independent checks

`deno` was not on PATH. Deno 2.9.6 was installed only for this review.

`deno test --allow-net --allow-env --allow-read supabase/functions/telegram/index.ts` reported 5 passed, 0 failed.

A forged POST to `/functions/v1/telegram/wrong-secret` with a non-JSON body returned status 401 and an empty body. The fetch double was not called.

A POST to `/functions/v1/telegram` with no trailing secret returned status 401 and an empty body.

A GET to `/functions/v1/telegram/<secret>` captured setWebhook JSON whose `url` was `https://orma-api.nryn.dev/functions/v1/telegram/<secret>`. That URL did not contain `supabase.co`.

A GET to `/telegram/<secret>` (function-name prefix only) also registered. The captured `url` still used `ORMA_API_URL`.

A POST `/start` with the path secret made grammY call the Bot API. The outgoing JSON `text` equalled `START_LINK_PROMPT`. The mock result text was a dummy `x`, so the pin is the outbound body, not the Bot API echo.

`depsFromEnv` with a getter that always returns `undefined` threw `missing TELEGRAM_BOT_TOKEN`.

`TELEGRAM_BOT_TOKEN`, `TELEGRAM_WEBHOOK_SECRET`, and `ORMA_API_URL` appear in `scripts/bootstrap-env.sh` and `.github/workflows/_env-example.yml`. No values are committed.

The matcher source no longer calls `decodeURIComponent`. The only remaining call is the percent-secret test asserting the presented value is not decoded.

Remediation introduced no new defect inside the T3.1 seam. A stub `config.toml` that holds only `[functions.telegram]` matches current Supabase function-configuration docs.

## Accepted for this round

A `/start` placeholder in `index.ts` is expected until T3.5 owns `link.ts`.

Deno tests live in `index.ts` because that is the T3.1 `owns` seam.

A valid-secret POST with a non-JSON body returning 500 remains outside T3.1 done-when. It is not recorded as a finding.

T3.1 status remains `claimed:orma`. This review does not mark the task done.
