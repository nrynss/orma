# T4.1 round 2 review

## Verdict

**APPROVE**

| C | H | M | L |
| --- | --- | --- | --- |
| 0 | 0 | 0 | 0 |

## Zero-residue claim

Round one contained H getUser unpinned, M supabase.co guards unpinned, and L minified tests. This review found zero residue from each.

| Prior finding | Independent pin | Result |
| --- | --- | --- |
| H. Production `authenticateBearer` getUser branch unpinned. Injected `deps.authenticate` hid it. | Independent Deno suite on the phase-4 blob. Four named tests omit `authenticate`. Fetch double answers `/auth/v1/user`. Mutation `return { userId: "leaked-user" }` failed those four. Nine older tests stayed green. | Resolved. |
| M. `depsFromEnv` and `apiBase` host guards unpinned. Happy path never fed `*.supabase.co`. | Same Deno file. Three named reject tests for `apiBase`, `depsFromEnv`, and `createUserScopedClient`. Deleting both `includes("supabase.co")` throws failed all three. | Resolved. |
| L. Six tests minified onto dense one-liners after `import.meta.main`. | Read from `const testFn=`. Thirteen named blocks. Zero lines over 200 characters in the test tail. Average line length about 43. | Resolved. |

## Independent checks

Deno 2.9.6 on PATH.

Fetched remote `phase-4` blob for `supabase/functions/mcp/index.ts` (git blob `18656ab9a6b319a06fa7d1b729bbaaf3af37a92e`, commit `20fdee31d7c8df41b33a2c372b3de8814c09b560`) into `/workspace/orma-t41-r2`.

`deno test --allow-net --allow-env --allow-read supabase/functions/mcp/index.ts`
reported 13 passed, 0 failed.

Missing bearer returned 401 with message containing `missing bearer token`.

Invalid bearer returned 401 with message containing `invalid or expired`.

Valid token `tools/list` returned 200 with exactly the six `TOOL_NAMES`.

User A `tools/call` `list_items` returned only user A rows. User B id was absent.

`createUserScopedClient` fetch saw `Authorization: Bearer token-user-a` and anon `apikey`.

`authenticateBearer` without `deps.authenticate` returned fixture user A on getUser success.

The same path threw `AuthError` on getUser failure.

Handler without `authenticate` returned 401 on invalid JWT and 200 tool list on valid JWT.

`apiBase`, `depsFromEnv`, and `createUserScopedClient` each threw on a `*.supabase.co` URL.

Remediation introduced no new defect inside the T4.1 seam that this review records as a finding.

## Contract gaps (outside owns, not findings)

Proxy still rewrites host only. Path `/mcp` does not map to `/functions/v1/mcp`.
Named in `t4.1-contract-change.md`. Outside `owns`. Helper seam done-when stays testable on the function URL.

Gateway `verify_jwt` may still be true. Anonymous clients can die at the edge before `index.ts`.
Also named in the contract file. Outside `owns` unless config.toml is extended.

Tool bodies under `mcp/tools/` are T4.2. T4.1 keeps names and stubs. `list_items` select is intentional for isolation pins.

## Done-when on the helper seam

Valid token lists tools. Anonymous is refused at the handler. Cross-user `list_items` is refused under the injected user client.
Those three are green under the colocated suite.
Production getUser auth and project-host guards are now pinned as well.

T4.1 status stays `claimed:orma-impl`. This review does not mark the task done.
