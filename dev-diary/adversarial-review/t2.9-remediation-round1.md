# T2.9 remediation, round 1

This remediation addresses M1 from `t2.9-round1.md`. It does not change the
round-one verdict. C1 needs transition-owner integration.

| Finding | Remediation | Pin | Mutation covered |
| --- | --- | --- | --- |
| C1 | Recorded the integration contract. The owners of materialisation, claiming, dispatch, polling, webhook receipt, re-fetching, ingestion, and finalisation must call `recordCallEvent` after their state transition. This task owns only the helper. | The isolated materialise, claim, and dispatch sequence in `t2.9-round1.md` remains the integration pin. It must query ordered `call_events` rows after each owner adopts the contract. | Removing a transition call leaves that stage absent from the ordered timeline. |
| M1 | `finalised` now requires a non-blank terminal `state`. Failed, errored, or cancelled states also require a non-blank `failure_reason`. | `deno test --allow-env --allow-net supabase/functions/_shared/events.ts` rejects `{ state: "failed" }` before the POST and reports three passing tests. | Removing terminal validation makes the opaque terminal-failure test fail. |
