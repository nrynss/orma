# T4.2 round 1 review

## Verdict

**REMEDIATE**

| C | H | M | L |
| --- | --- | --- | --- |
| 0 | 1 | 1 | 1 |

## Findings

| where | what | pin | mutation |
| --- | --- | --- | --- |
| H `registerOrmaTools` in `mod.ts` | The export index will call only pins tool names. Registered `handler` bodies are never invoked. Hollow callbacks still pass the suite. | No colocated test calls `_registeredTools[name].handler` (or MCP `tools/call`) after `registerOrmaTools`. | Replace each register callback with a stub that returns `isError` text `stub`. All nine tests stay green. |
| M `retireItem` durable who | Spec says retire records who and surface. Surface lands in `retired_reason`. Who is only echoed as response `retired_by`. The items row has no durable who field. Schema has no `retired_by` column. | After retire, read the store row. Keys include `retired_reason` starting with `mcp`. `retired_by` is absent on the row. | Drop response `retired_by` only. The named retire test fails. Leave durable row unchanged. Suite still never fails a durable-who pin. |
| L retire reversible claim | Done-when asks for reversible retire. The handler hardcodes `reversible: true`. The test clears fields on the double itself. That is not an independent web restore pin. | Read the retire test. Restore is `stored.status = open` plus null retired fields on the fixture object. | Set `reversible: false` in the handler. Only the flag assert fails. Clearing fields on the double still "works". |

## Independent checks

Deno 2.9.6 on PATH.

Fetched remote `phase-4` blobs for `supabase/functions/mcp/tools/` into `/workspace/orma-t42-review`.
Git blobs: `types.ts` `4c9a0c29`, `handlers.ts` `1f6e869c`, `mod.ts` `bb8d3400`, `double.ts` `d8904050`, `tools_tests.ts` `cb588d2d`.
Tip commits for tools: `d1b88e07` (handlers) and `efb2cbda` (tests and double).

`deno test --allow-net --allow-env --allow-read supabase/functions/mcp/tools/tools_tests.ts`
reported 9 passed, 0 failed.

`add_item` wrote `source = mcp` for caller A with trimmed text.

`list_items` for A returned only `item-a1`. User B id was absent.

`retire_item` set status retired, `retired_at`, and `retired_reason` `mcp`. Cross-user retire on `item-b1` failed with not found.

`set_slot` inserted and updated for A. Update of `slot-b1` failed with not found.

`get_last_call` returned `run-a-new`. `get_patterns` returned `pat-a-new` with `facts.mentions.dentist === 3`.

User B last call and patterns stayed on B rows only.

`registerOrmaTools` listed exactly the six `TOOL_NAMES`.

Manual invoke of registered `add_item.handler` today returns source mcp. That path is still unpinned in the suite (see H).

Source mutation `ITEM_SOURCE_MCP` to `web` failed only the add_item test.

## Contract gaps (outside owns, not findings)

`supabase/functions/mcp/index.ts` still registers stubs via `notImplementedResult`.
It never imports `registerOrmaTools`.
Named in `t4.2-contract-change.md`. Outside `owns: supabase/functions/mcp/tools/`.

This does not make the tools seam itself false.
Handlers and `registerOrmaTools` exist and round-trip on the fixture double.
Live MCP still cannot exercise those handlers until index wiring lands.
Done-when on the helper seam stays fixture round-trips, surface on retire, and clean cross-user failure.

Proxy `/mcp` rewrite and gateway `verify_jwt` residuals from T4.1 remain unchanged.

## Done-when on the helper seam

Each tool round-trips on the owner-scoped double. Surface on retire is green. Cross-user retire and set_slot fail cleanly.
Those are green under the colocated suite.

The H finding still stands. The production registration export can be hollow while tests pass.
The M finding still stands. Durable who is missing from the item row.
Remediator must pin registered handlers and durable who (encode into `retired_reason`, or raise a schema contract).

T4.2 status stays `claimed:orma-impl`. This review does not mark the task done.
