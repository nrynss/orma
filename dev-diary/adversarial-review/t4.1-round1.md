# T4.1 round 1 review

## Verdict

**REMEDIATE**

| C | H | M | L |
| --- | --- | --- | --- |
| 0 | 1 | 1 | 1 |

## Findings

| where | what | pin | mutation |
| --- | --- | --- | --- |
| H `authenticateBearer` production branch | Invalid and expired token handling through `client.auth.getUser` is never exercised. Every auth test injects `deps.authenticate`. | No colocated test calls the handler without `authenticate` override against a double that returns getUser error or empty user. | Replace the getUser body with `return { userId: "leaked-user" }`. All six tests still pass. |
| M `depsFromEnv` and `apiBase` | Stack rule forbids `*.supabase.co` as `ORMA_API_URL`. The guards exist. The depsFromEnv test never feeds a project host URL. | `deno test` on the file. The named test only asserts the happy path URL lacks `supabase.co`. | Delete both `includes("supabase.co")` throws. Six tests still pass. |
| L colocated test tail | The six tests are minified onto dense one-liners after `import.meta.main`. Review and future edits are harder than the production shell above. | Read the file from the `const testFn=` line. | Reformat only. Behaviour pins stay green. |

## Independent checks

Deno 2.x on PATH.

`deno test --allow-net --allow-env --allow-read supabase/functions/mcp/index.ts`
from the pushed blob under `/workspace` reported 6 passed, 0 failed.

Missing bearer returned 401 with message containing `missing bearer token`.

Invalid bearer returned 401 with message containing `invalid or expired`.

Valid token `tools/list` returned 200 with exactly the six `TOOL_NAMES`.

User A `tools/call` `list_items` returned only user A rows. User B id was absent.

`createUserScopedClient` fetch saw `Authorization: Bearer token-user-a` and anon `apikey`.

Leaky mock mutation (drop `user_id===ownerId` filter) failed the isolation test only.

Stripping the Bearer header from `createUserScopedClient` failed the JWT test only.

Remote `phase-4` blob for `supabase/functions/mcp/index.ts` contains `testFn` and all six test names after commit `a917a2619055862da5888d82dce0fc9413b2c4a5`.

## Contract gaps (outside owns, not findings)

Proxy still rewrites host only. Path `/mcp` does not map to `/functions/v1/mcp`.
Named in `t4.1-contract-change.md`. Outside `owns`. Helper seam done-when stays testable on the function URL.

Gateway `verify_jwt` may still be true. Anonymous clients can die at the edge before `index.ts`.
Also named in the contract file. Outside `owns` unless config.toml is extended.

Tool bodies under `mcp/tools/` are T4.2. T4.1 keeps names and stubs. `list_items` select is intentional for isolation pins.

## Done-when on the helper seam

Valid token lists tools. Anonymous is refused at the handler. Cross-user `list_items` is refused under the injected user client.
Those three are green under the colocated suite.

The H finding still stands. Production getUser auth is unpinned. Remediator must add a pin that fails when that branch is neutered.

T4.1 status stays `claimed:orma-impl`. This review does not mark the task done.
