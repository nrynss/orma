# T8.1 remediation, round 2

Round 2 review returned REMEDIATE with C0 H0 M3 L3 against `scripts/seed.ts`.
This round fixes all six findings in `scripts/seed.ts`.

## Findings

| where | what | pin | mutation |
| --- | --- | --- | --- |
| M `dateTimeInTz` | Helper parsed wall time in the machine local zone, skewing UTC timestamps by machine offset. | Under Asia/Kolkata host, New York 08:00 produced 06:30Z (02:30 local) instead of 12:00Z (08:00 local). | `dateTimeInTz` now converts target calendar time to UTC using `Date.UTC` and `Intl.DateTimeFormat` parts only. New York 08:00 returns 12:00Z under any host `TZ`. Reverting brings back machine offset dependency. |
| M `buildPlan`, run 2 and run 3 `briefing.lead_line`, and run 3 transcript | Briefing leads and spoken turns stated counts and facts no rows supported at dispatch time. | Before run 3, the dentist held only 2 mention rows, yet run 3 briefing and spoken turn stated 3 times. | Run 1 lead now reads "Everything open is still current." Run 2 reads "It's been 30 days." Run 3 reads "It's been 31 days." Run 3 turn 0 speaks "The dentist, it's been 31 days." Reverting claims counts before rows exist. |
| M `scripts/seed.ts` imports, `applyPlan` CALL-E request building | Seed rendered an invented short task without the seven steps, and hardcoded production webhook host. | `position('Hello. This is Orma.' in request_body)` was 0, and webhook host was hardcoded to production. | Seed now imports `renderCallTask` from `_shared/briefing.ts`, `buildCallRequest` from `_shared/calle.ts`, and `maskedRequestBody` from `_shared/dispatch-mode.ts`. Webhook uses `ORMA_API_URL`. Reverting restores invented body. |
| L `applyPlan`, item `created_at` for captured items | Weekend groceries was created at 09:00 local, an hour after run 3 completed at 08:02 local. | `items.created_at > call_runs.completed_at` was true for weekend groceries against run 3. | `applyPlan` sets `created_at` for items with `capturingRunIndex` to that run's `completed_at`. Comparison is now false. Reverting to constant time brings back the discrepancy. |
| L `applyPlan`, `results.structured.commitments` and `call_events` | Ingested mention count did not match structured result entries because mentions had no backing result entries. | `ingested.detail.mention_count` against `results.structured` entries was 1/0, 2/0, and 2/1 across runs. | Each run's `results.structured` now carries commitments explaining the mentions (1 in run 1, 2 in run 2, 1 plus groceries capture in run 3). Entries equal mentions (1/1, 2/2, 2/2). Reverting breaks parity. |
| L `printDisclosure` | Disclosure text used four passive voice clauses. | Plan output contained "was inserted with", "were inserted in", "are seeded with", and "can be identified by". | The disclosure text now uses active voice throughout ("The seed creates...", "The seed writes...", "The seed adds...", "A single query matches..."). Reverting restores passive phrasing. |

## Proof

1. `scripts/p6-local-stack.sh up` verified against `firstrun@example.com` (America/New_York) and `local@example.com`.
2. Seeded New York runs sit at `12:00:00Z` (`08:00` local EDT) and `12:02:00Z` (`08:02` local EDT).
3. `briefing.lead_line` reads "Everything open is still current." for run 1, "It's been 30 days." for run 2, and "It's been 31 days." for run 3.
4. `assemble_briefing` at demo date returns "You've mentioned the dentist 3 times. It's been 34 days."
5. `dispatched.detail.request_body` contains "Hello. This is Orma.", masked phone `+155555XXXXX`, and webhook URL on the active `ORMA_API_URL`.
6. Weekend groceries `created_at` matches run 3 `completed_at` (`2026-09-11T12:02:00.000Z`).
7. Ingested mention counts equal `results.structured` entry counts (1, 2, 2).
8. Disclosure text runs in active voice with zero passive clauses.
9. `--remove` removes all seeded rows and preserves non-seed items.
10. Stack torn down cleanly. Zero containers remain.
