# T5.3 remediation round 1

This remediation addresses F1 C from `t5.3-round1.md`. It does not change the
verdict. T5.3 stays `claimed:orma-impl-53`.

| Finding | Remediation | Pin | Mutation covered |
| --- | --- | --- | --- |
| F1 C | `TelegramLinkClient` now requires `anonKey`. Missing it throws. There is no JWT fallback. `headers()` sends `apikey` as that publishable key and `authorization` as `Bearer` plus the user JWT. Settings `creds()` passes `anonKey: getPublishableKey()` into mint, unlink, and load. Posts still go to `ORMA_API_URL`. The project-host throw stays. | `cd web && npm run check` runs `scripts/check-telegram-link-headers.ts`. A fetch double asserts `apikey` is the anon fixture and `Authorization` is the user Bearer. Empty or omitted `anonKey` throws `missing publishable anon key`. Live helper mint through `https://orma-api.nryn.dev` inserted one `telegram_link_tokens` row. The same body with JWT as both headers returned 401 `Invalid API key` and count 0. Live `D9CZDZBY.js` has `apikey:s(e),authorization:\`Bearer ${t}\``. Live Settings node passes `anonKey:S()`. | Restore JWT as `apikey`. The fetch double fails with `mint: apikey was not the anon key`. Settings mint 401s again. `telegram_link_tokens` for that user stays at 0. The Start link never appears. |

## Commands run

`cd web && npm run check` exited 0. It printed `secret-key check passed`, then
`telegram-link header check passed`, then svelte-check with 0 errors and 0
warnings.

`cd web && npm run build` succeeded.

`cd web && npm run deploy` succeeded. Wrangler used OAuth. `CLOUDFLARE_API_TOKEN`
was not set. Worker version `5b61e822-d05f-468d-bbfe-c34f5eaad97c` is on
`orma.nryn.dev`.

A copy of the helper with `apikey: accessToken` restored failed the header
script with `mint: apikey was not the anon key` (exit 1). The repo copy was not
that mutant.

## Live probe

A throwaway auth user and `public.profiles` row were created through
`https://orma-api.nryn.dev`. Password grant returned a user JWT with
`role=authenticated`. `mintLinkToken` from the helper used the anon key as
`apikey` and that JWT as Bearer. The token table then held 1 row for that user.

JWT as both headers still returned 401 and count 0. That is the round 1 defect.

Cleanup deleted the tokens, the profile, and the auth user. Readback was 0 token
rows, 0 profile rows, and admin user 404. A later admin user list held 0 users
matching the probe prefix. `/tmp/t53-r1-probe.mjs` was removed. No secret was
printed. No phone number appeared.

## Files this round changed

- `web/src/lib/telegram-link.ts`
- `web/src/routes/app/settings/+page.svelte`
- `web/scripts/check-telegram-link-headers.ts`
- `web/package.json`
- `dev-diary/adversarial-review/t5.3-remediation-round1.md`

No T5.2 file was edited. The PHASE-5 status line was left as
`claimed:orma-impl-53`.
