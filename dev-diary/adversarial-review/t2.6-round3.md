# T2.6 review, round 3

**Verdict: REMEDIATE**

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| C | `supabase/functions/calle-webhook/index.ts:48-56` | Redaction retains untrusted JSON keys. A key can contain a phone number, email address, name, or transcript text. The capture log therefore still leaks personal data. | `deno eval --allow-env` imported `maskWebhookEvidence` and masked `{ "+1 (415) 555-0123": "Sam Example", nested: { "sam@example.com": true } }`. The output retained both raw keys. | Restoring raw keys leaks caller data through an unknown provider shape. |
| H | `supabase/functions/calle-webhook/index.ts:201-205` | Concurrent duplicate deliveries can both read `capture_pending`, then both log and mark the event. The pending check is not an atomic claim. | Two handlers that both receive `409` and synchronized `capture_pending` GET responses each invoke `capture`. The test must assert one invocation. | Restoring the separate read and capture permits replay evidence to appear more than once. |

All five local tests pass, but neither condition is covered. `deno check` and
`git diff --check` also pass.
