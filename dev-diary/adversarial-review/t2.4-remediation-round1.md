# T2.4 remediation, round 1

This remediation addresses every finding in `t2.4-round1.md`. It does not
change the round-one verdict. It uses recorded fixtures only and placed no call.

| Finding | Remediation | Pin | Mutation covered |
| --- | --- | --- | --- |
| C1 | `tick` now builds CALL-E dependencies from the canonical runtime inventory and passes every SQL-claimed run to `dispatchClaimedRun`. The dispatcher reads the source slot's `local_time` before briefing assembly. | The configured-tick check returns one claim and observes `POST /v1/calls` for a due run. | Removing the tick dispatcher leaves the configured-tick check without a CALL-E request. |
| C2 | `calle.ts` derives `webhook_url` from `ORMA_API_URL` and `ORMA_WEBHOOK_SECRET`. It adds no undocumented environment variable. | The canonical-runtime check passes only when the derived receiver URL is exact. | Restoring `ORMA_WEBHOOK_URL` makes startup reject the documented inventory. |
| H1 | Dispatch loads `testdata/calle/call-completed.json` and stores its recorded call id. The test pins the complete outbound JSON body with a fixed SHA-256 digest. | The fixture-backed dispatch check fails if the recorded response or any request field changes. | Altering the task, recipient, schema, webhook URL, metadata, or body ordering changes the digest. |
| T2.9 C1 integration | The claim owner appends `claimed` after the SQL transition. The dispatch owner appends `dispatched` after it persists the provider result. | The configured-tick check exercises both transition owners. Its requests include ordered `call_events` inserts. | Removing either integration leaves that transition absent from the timeline. |

Validation: `deno test --allow-env --allow-net --allow-read
supabase/functions/_shared/calle.ts supabase/functions/tick/index.ts` reports 26
passing checks. `git diff --check` passes.
