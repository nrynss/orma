# T2.1 remediation, round 1

| Finding | Remediation | Independent pin | Mutation |
| --- | --- | --- | --- |
| C1 | Added `20260912150000_schedule_materialise.sql`. It registers `materialise-runs` with `pg_cron` at 00:10 UTC. The job queues an authenticated `pg_net` POST to the Materialise Edge Function through `orma-api.nryn.dev`. | Apply the migration to an isolated Supabase database, then query `cron.job` for `jobname = 'materialise-runs'`. Its `schedule` is `10 0 * * *` and its command contains both `net.http_post` and `/functions/v1/materialise`. | Reverting the migration removes the cron job. Materialisation then needs a manual request and upcoming call runs are not created. |
