# T8.2 remediation round 1

| ID | Remediation | Pin |
| --- | --- | --- |
| H1 | The keepalive probe captures the suppressed response status and exits nonzero unless it is exactly `200`. | A local endpoint returning `204` or `302` makes the status comparison exit `1`. A `200` exits `0`. |
| M1 | The workflow declares `permissions: contents: read` at top level. The backup push still uses `BACKUP_REPO_TOKEN` through Git's explicit authorization header. | Parsing `.github/workflows/keepalive.yml` shows the read-only permission and the explicit backup-token push. |
