# T2.8 review, round 1

**Verdict:** REMEDIATE

**Counts:** C 2, H 1, M 0, L 0

`deno test --allow-read --allow-env --allow-net
supabase/functions/_shared/dispatch-mode.ts` passed four isolated tests. `deno
check supabase/functions/_shared/dispatch-mode.ts` and `git diff --check` also
passed. These checks do not exercise the dispatcher that can place a call.

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| C | `supabase/functions/_shared/dispatch-mode.ts:53-57,124-165`, `supabase/functions/_shared/calle.ts:216-287`, `supabase/functions/tick/index.ts:119-129` | The module is unreferenced by either production dispatch path. `dispatchClaimedRun` always performs `POST /v1/calls`, and tick always routes claimed runs there. `ORMA_DRY_RUN` and `call_runs.dry_run` are consequently ignored. With the default, unset environment variable, a due run can place a real call. T2.8 must extend the T2.4 seam so both modes assemble the same serialized request and the dry branch reaches `executeDryRun` before any outbound-capable operation. | Set no `ORMA_DRY_RUN` value, return a claimed run with `dry_run: false`, and drive `tick(depsFromEnv(...))` using a recording fetch. The run must create no request to `https://api.call-e.test/v1/calls`, while its recorded event body equals the live dispatcher body byte for byte. It currently sends the request because neither production module imports `dispatch-mode.ts`. | Removing the mode branch leaves the isolated helper green while default production dispatch dials a person. |
| C | `supabase/functions/_shared/dispatch-mode.ts:124-165` | Dry mode only writes a terminal `call_runs` patch and two timeline events. It neither persists the selected fixture's transcript and structured result nor invokes the result-ingestion seam that creates `items` and `item_mentions`. Thus it cannot meet T2.8's explicit full-run done condition. Record the cross-seam contract and wire dry terminal handling through the same authoritative result and ingestion path as a completed provider call. | Execute a completed dry fixture against an isolated database, then query `transcripts`, `results`, `items`, and `item_mentions` for the run. The completed fixture must produce the same rows as live completion. The current helper only calls injected event and run-update callbacks, so all four tables remain empty. | Reverting result persistence or ingestion still leaves a terminal dry run, but rehearsal no longer proves the user-visible data pipeline. |
| H | `supabase/functions/_shared/dispatch-mode.ts:140-161` | `executeDryRun` records `dispatched` before it applies the terminal `call_runs` update. If that update fails, the timeline claims a dispatch that never became terminal. The live T2.4 path records `dispatched` only after its state change. Make dry mode retain the same durable ordering and prove a failed update cannot leave a misleading event. | Make `updateRun` reject after a successful `recordEvent` in an integration test. Query `call_events`. It currently contains `dispatched` despite no terminal run state. | Reverting the ordering restores a false timeline entry whenever the terminal write is interrupted. |

There is no residue from an earlier T2.8 review. This is round 1.
