# T1.6 round 1

## Verdict

REMEDIATE

| Severity | Count |
| --- | ---: |
| C | 0 |
| H | 1 |
| M | 0 |
| L | 0 |

There are no prior T1.6 review rounds. No earlier residue exists.

## Findings

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| H | `supabase/seed.sql:123-148` | The seeded completed run says its disposition is `completed`, although the specification requires `answered_extracted` for a completed run with a result. Its `results.structured` is marked valid but fails Orma's parser. It uses `slot_request`, `item_text`, and `hopeful`, which the parser rejects. | An isolated stack on ports 55320-55329 reset from empty migrations. The seeded user signed in successfully. PostgREST returned the run with `state=completed`, `disposition=completed`, and `results.valid=true`. `deno eval` passed that stored structure to `parseStructuredResult`, which returned `result: null` with `structured_result.slot_request is not allowed`. | Reverting the fix leaves local browsing and downstream tests with a state that cannot occur after T1.5 validation. It hides incorrect disposition handling and treats an invalid extraction as valid. |

## Independent pins

| Claim | Pin | Observed result |
| --- | --- | --- |
| The seed resets from empty migrations without the linked project. | `supabase db reset --workdir /tmp/orma-t1-6-review.zMyWUp` against the isolated 55320-55329 port range. | Core, RLS, pg_cron, and pg_net migrations applied. `supabase/seed.sql` completed and the local containers restarted. |
| The seeded user can sign in and browse its own data through RLS. | Password grant for `local@example.com`, followed by exact-count PostgREST reads using that JWT. | Profiles 1, consents 1, slots 1, items 4, item_mentions 3, commitments 1, call_runs 1, call_events 2, transcripts 1, results 1. Pattern reports, deliveries, and webhook events returned 0. |
| Required seed relationships and ages are visible. | Authenticated PostgREST reads of `items`, `call_runs`, `transcripts`, `item_mentions`, and `commitments`. | The four seeded items are 34, 21, 8, and 2 days old. The run is completed, transcript has two turns, mentions have offsets 0, 2, 4, and the commitment has offset 8. |
