# T2.3 review, round 2

**Verdict:** REMEDIATE

**Counts:** C 0, H 1, M 0, L 0

## Prior-finding residue

| Prior finding | Independent pin | Result |
| --- | --- | --- |
| C1. The scheduler could claim a run before T2.4 could dispatch it. | `20260912161000_tick.sql` has no `cron.schedule` call. An independent Deno run of `supabase/functions/tick/index.ts` passed the no-dispatch test. Its recorded requests omit `rpc/claim_due_call_runs`. | Resolved. Scheduled rows remain untouched without a dispatcher. |
| H1. The mocked concurrency test did not exercise PostgreSQL. | I ran `bash supabase/migrations/test-tick-claims.sh` against a fresh temporary local Supabase stack. It applied the migration, used two `psql` sessions as `service_role`, observed one immediate claim and one skipped claim, then counted one claimed row. | Residue remains. The runner's blocking detector is not reliable. |

## Findings

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| H | `supabase/migrations/test-tick-claims.sh:104-109` | The runner measures the second session with whole seconds and accepts any duration below three. A broken `FOR UPDATE` without `SKIP LOCKED` waits almost all of the first session's three-second sleep. It then returns zero because the first session committed its state change. If the second session begins in the first fraction of a wall-clock second, the integer subtraction is two and the broken blocking query passes every assertion. The required nonblocking guarantee is therefore not pinned. | Replace the wall-clock integer check with a monotonic millisecond deadline that is materially below the three-second hold. Independently mutate `FOR UPDATE SKIP LOCKED` to `FOR UPDATE` and require the runner to fail. The current source proves the gap because `date +%s` discards sub-second time, while the first transaction holds the lock for three seconds. | Replacing `SKIP LOCKED` with ordinary `FOR UPDATE` can block overlapping ticks and still leave this acceptance runner green. |

## Independent checks

`deno test --allow-env --allow-net supabase/functions/tick/index.ts` reported 3 passed and 0 failed.

`bash supabase/migrations/test-tick-claims.sh` passed against a fresh temporary local Supabase stack. It applied all migrations through `20260912161000_tick.sql`, then observed the expected two-session claim result. This run validates the current path. It does not repair the unsound blocking pin above.

`git diff --check` passed.
