# T4.1 handoff

## What shipped
Streamable HTTP MCP transport in stateless mode at function slug `mcp`.
JWT auth via Authorization Bearer. User-scoped Supabase client with
`ORMA_API_URL` and `SUPABASE_ANON_KEY`. Never service role.

Six tool names registered. Five return not implemented. `list_items`
runs a scoped select for isolation pins before T4.2.

Colocated Deno tests live on the remote `phase-4` blob for
`supabase/functions/mcp/index.ts` (commit `a917a2619055862da5888d82dce0fc9413b2c4a5`).

## How to test
```bash
deno test --allow-net --allow-env --allow-read supabase/functions/mcp/index.ts
```
Local and independent review result: 6 passed, 0 failed.

Pins: missing token 401, invalid token 401, valid tools/list,
cross-user list_items isolation, JWT never service role,
depsFromEnv names.

## Contract gaps
See `t4.1-contract-change.md`. Proxy must rewrite `/mcp` to
`/functions/v1/mcp`. May need `[functions.mcp] verify_jwt = false`.
Tool bodies under `mcp/tools/` are T4.2.

## Residual
Round 1 review is `REMEDIATE`. Production `auth.getUser` branch and
project-host guards need pins. Status stays `claimed:orma-impl`.
Do not mark done.
