# T4.1 handoff (transport and authorisation)

Status stays `claimed:orma-impl`. This is not done. Reviewer owns the next verdict.

## What shipped

`supabase/functions/mcp/index.ts` on `phase-4`.

- Streamable HTTP MCP in stateless mode (`sessionIdGenerator` unset).
- JSON responses via `WebStandardStreamableHTTPServerTransport`.
- JWT from `Authorization: Bearer`. User-scoped Supabase JS client.
- Env (no defaults): `ORMA_API_URL`, `SUPABASE_ANON_KEY`.
- Never service role. Always `ORMA_API_URL`, never `*.supabase.co`.
- Six tool names registered. Five stubs say `not implemented`.
- `list_items` runs a scoped `items` select so isolation is pinable now.

## How to test

Local file with colocated tests (authoritative for pins):
`/workspace/orma-t41/supabase/functions/mcp/index.ts`

```bash
cd /workspace/orma-t41
deno test --allow-net --allow-env --allow-read supabase/functions/mcp/index.ts
```

Real result on this machine: **6 passed, 0 failed**.

Pins covered:

1. No token returns 401 auth error (not 500).
2. Invalid token returns 401 auth error.
3. Valid token `tools/list` returns the six names.
4. User A `list_items` cannot see user B rows (mock RLS client).
5. User client sends caller JWT and anon key, never service role.
6. `depsFromEnv` requires `ORMA_API_URL` and `SUPABASE_ANON_KEY`.

## Contract gaps (outside owns)

1. **Proxy path.** Spec wants `orma-api.nryn.dev/mcp`. Current proxy passes paths through unchanged. Function URL today is `/functions/v1/mcp`. Needs a rewrite outside T4.1 owns, or T4.3 docs the functions path.
2. **`verify_jwt`.** Default gateway verify may 401 before our handler. Clean protocol errors need `[functions.mcp] verify_jwt = false` in `supabase/config.toml`. That file is not in T4.1 owns.
3. **Tool bodies.** Full tool behaviour belongs to T4.2 under `mcp/tools/`. Stubs and the thin `list_items` select live in `index.ts` only for this transport pin.

## Residual

- Remote `phase-4` `index.ts` is the prod transport and auth shell (blob SHA `6d48593297102919a2e48c4f5e9530eeaf226fef`). Colocated Deno tests pass locally but are not yet in that remote blob. Re-push the local file that prints `6 passed` when the next agent can embed the full ~12KB content via `create_or_update_file` or `push_files`.
- No live JWT mint against production Auth in these tests (doubles only).
- Deploy and front-door smoke are not part of this commit.
- T4.2 should replace stubs and relocate handlers under `tools/`.
- Status stays `claimed:orma-impl`. Do not mark done.
