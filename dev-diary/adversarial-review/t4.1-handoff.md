# T4.1 handoff (landed)

Status: **done** after round 2 APPROVE (`fc471e49`).

## What shipped

`supabase/functions/mcp/index.ts` on `phase-4`.

- Streamable HTTP MCP in stateless mode.
- JWT Bearer auth. User-scoped Supabase client via `ORMA_API_URL` + `SUPABASE_ANON_KEY`.
- Never service role.
- Six tool names. Five stubs until T4.2. Thin `list_items` for isolation pins.
- Remediation pinned production `auth.getUser` and `*.supabase.co` guards.

## Pins

```bash
deno test --allow-net --allow-env --allow-read supabase/functions/mcp/index.ts
```

13 passed, 0 failed on the landed blob.

## Contract gaps still open

1. Proxy `/mcp` rewrite to `/functions/v1/mcp` (or T4.3 documents functions path).
2. `[functions.mcp] verify_jwt = false` in `config.toml`.
3. Tool bodies under `mcp/tools/` are T4.2.

## Next

T4.2 claimed. Replace stubs with real tools under `owns: supabase/functions/mcp/tools/`.
Wire them from `index.ts` only via import if a contract note is needed for index edits.
