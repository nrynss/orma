# T4.2 round 2 review

## Verdict

**APPROVE**

| C | H | M | L |
| --- | --- | --- | --- |
| 0 | 0 | 0 | 0 |

## Zero-residue claim

Round one contained H hollow `registerOrmaTools` handlers, M durable who on retire, and L reversible restore.
This review found zero residue from each.

| Prior finding | Independent pin | Result |
| --- | --- | --- |
| H. `registerOrmaTools` only pinned names. Hollow callbacks stayed green. | Independent Deno suite on phase-4 tip. Named test invokes each `_registeredTools[name].handler`. Mutation that stubs every register callback to `isError` text `stub` failed only that test. Eight others stayed green. | Resolved. |
| M. Who lived only in response `retired_by`. Row lacked durable who. | Same suite. Retire pin requires store `retired_reason` equal to `encodeRetiredReason(A)` and `parseRetiredReason` who A. Mutation writing surface-only `mcp` (or `mcp:note`) failed the retire pin and the registered retire path. | Resolved. |
| L. `reversible` hardcoded. Restore cleared fields on the fixture double. | Same suite. Restore goes through `restoreRetiredItem` and asserts the store row clears. Hollow restore that returns open without updating the store failed with `web restore via client must clear store row`. Setting `reversible: false` failed `must mark reversible`. | Resolved. |

## Independent checks

Deno 2.9.6 on PATH.

Fetched remote `phase-4` blobs for `supabase/functions/mcp/tools/` into `/workspace/orma-t42-r2`.
Git blobs: `types.ts` `837eb9ac`, `handlers.ts` `711cc00f`, `mod.ts` `060f2f85`, `double.ts` `d8904050`, `tools_tests.ts` `8b24e61b`.
Tip under review: `5b809114` (handler pins and restore test). Prior remediations on the tip lineage include `f7dc95fd` and `cb62535c`.

`deno test --allow-net --allow-env --allow-read supabase/functions/mcp/tools/tools_tests.ts`
reported 9 passed, 0 failed.

`add_item` wrote `source = mcp` for caller A with trimmed text.

`list_items` for A returned only `item-a1`. User B id was absent.

`retire_item` set status retired, `retired_at`, and `retired_reason` `mcp:<userA>`.
Store parse returned surface mcp and who A. Response `retired_by` matched A.
`restoreRetiredItem` cleared the store row. Restored `item-a1` listed as open again.

Cross-user retire on `item-b1` failed with not found.

`set_slot` inserted and updated for A. Update of `slot-b1` failed with not found.

`get_last_call` returned `run-a-new`. `get_patterns` returned `pat-a-new` with `facts.mentions.dentist === 3`.

User B last call and patterns stayed on B rows only.

`registerOrmaTools` listed exactly the six `TOOL_NAMES`.
Live registered handlers wrote an mcp item, listed it, retired with durable who, set a slot, and returned last call plus patterns.

Remediation introduced no new defect inside the T4.2 seam that this review records as a finding.

## Contract gaps (outside owns, not findings)

`supabase/functions/mcp/index.ts` still registers stubs via `notImplementedResult`.
It never imports `registerOrmaTools`.
Named in `t4.2-contract-change.md`. Outside `owns: supabase/functions/mcp/tools/`.

This does not make the tools seam itself false.
Handlers and `registerOrmaTools` exist and round-trip on the fixture double.
Live MCP still cannot exercise those handlers until index wiring lands.
Done-when on the helper seam stays fixture round-trips, surface and who on retire, restore, and clean cross-user failure.

Proxy `/mcp` rewrite and gateway `verify_jwt` residuals from T4.1 remain unchanged.

## Done-when on the helper seam

Each tool round-trips on the owner-scoped double. Surface and who on retire are green.
Restore clears the store row. Cross-user retire and set_slot fail cleanly.
Registered handlers are live under the colocated suite.

T4.2 status stays `claimed:orma-impl`. This review does not mark the task done.
