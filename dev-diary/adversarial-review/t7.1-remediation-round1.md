# T7.1 remediation, round 1

Round 1 commit `2174dbe` returned REMEDIATE with C0 H1 M2 L1 against the
implementation at `2e3d79a`. This round fixes all four findings inside
`supabase/migrations/test-facts.sh`. No fixture exposed a defect in the SQL
itself, so `20260913190000_facts.sql` stays byte-identical to `2e3d79a`.

## Findings

| where | what | pin | mutation |
| --- | --- | --- | --- |
| H `test-facts.sh` fixture timestamps, the dentist mentions, the `email Amma` created_at, the physio retirement, and the retirements and ages assertions | Every affected timestamp sat at 06:00 or 06:30 +05:30, so the Asia/Kolkata date and the UTC date agreed. Dropping the profile-timezone casts changed no fixture value. | In-window timestamps now sit at 01:00+05:30. Their local dates stay inside the window and their UTC dates fall one day earlier. dentist keeps mention_count 4. email Amma keeps age 18 from the local created date. physio retires on 2026-09-01 01:00+05:30, inside the window by local date with days_open 31 and average 31.0. The 2026-09-08 edge mention keeps 06:00 so nothing compensates. | All five `at time zone tz.timezone` casts removed from `facts.sql`. The harness aborts with `retirements must cover only this window and measure local days open: {"items": [], "avg_days_open": null, "retired_count": 0}` and exits 3. The physio retirement reads 2026-08-31 in UTC and leaves the window. The dentist count would drop to 3 and the email Amma age to 19 under the same mutation. |
| M `test-facts.sh` boundary profile, fourth user 00000000-0000-0000-0000-000000000774, and the new fourth assertion block | Fixtures covered 7, 2 and 0 observed days. Nothing held exactly 3, so changing the comparison to `> 3` passed every block. | Boundary user holds four completed answered runs over exactly three distinct local dates, 2026-09-01, 2026-09-02 and 2026-09-03 twice, with null slots and unique idempotency keys. The block asserts observed_days 3, sufficient_history true, completed_runs 4 and answered_runs 4. | `>= 3` changed to `> 3` in `facts.sql`. The harness aborts with `three observed days must already be sufficient history` and exits 3. Only the boundary block fails, because 7-day, 2-day and 0-day profiles answer the same under both comparisons. |
| M `test-facts.sh` three new open items and the rewritten ages and longest_surviving assertions | The main profile owned 3 open items and longest_surviving was asserted equal to ages. Any cap of 3 or more, or no cap, passed. | water plants, book flight and replace bulb join the main profile with no mentions, ages 10, 5 and 1 from since_date. ages now asserts all six open items in descending order, ending with tax return clamped to 0. longest_surviving asserts exactly the first five and excludes tax return at age 0. | `limit 5` deleted in `facts.sql`. The harness aborts with `longest_surviving must cap at five items and drop the youngest` and exits 3. The reported array carries six entries, tax return at age 0 among them, and misses the exact JSON. |
| L `test-facts.sh` cleanup function and the new residue guard before the pass line | `supabase stop` kept the database backup volume and the scratch root was never removed. Every run leaked one volume and one directory. | cleanup stops with `--no-backup` and removes `$scratch_root`. Before the pass line the harness calls cleanup once, then fails if the volume `supabase_db_$(basename "$scratch_root")` still exists or the scratch root still exists. The exit trap repeats the cleanup safely. | Removing `--no-backup` fails the script with `cleanup left the database volume supabase_db_orma-t7-1.U7c7mU behind.` and exit 1. Removing the `rm -rf` line fails it with `cleanup left the scratch root /tmp/orma-t7-1.sAUDdT behind.` and exit 1. Neither run reaches the pass line. |

## Proof

1. `bash supabase/migrations/test-facts.sh` passes after the changes. Five
   assertion blocks report DO, the transaction rolls back, and the script
   prints `T7.1 facts acceptance passed.` with exit 0.
2. Every mutation above was a temporary working-tree edit, reverted right
   after its run. Both files match their pre-mutation checksums. The harness
   copy of `facts.sql` is byte-identical to `2e3d79a`.
3. Two consecutive full runs with no intermediate cleanup both passed. After
   the second run no `supabase_db_orma-t7-1.*` volume and no
   `/tmp/orma-t7-1.*` directory remains. The eight volumes and directories
   left by the review runs were swept before the proof runs.

## Notes for the next round

The port randomisation keeps this runner off any default-port stack. The
residue guard checks the volume by exact name, which is enough because
`supabase stop` without `--no-backup` keeps the volume under its own name
rather than renaming it.
