### 2026-09-12 · T3.4 complete

**T3.4 Receipt delivery.** `supabase/functions/_shared/deliver-telegram.ts`
exports `deliverPostCallTelegram` and `deliverPatternTelegram`.
Injected `fetch`, bot token, `ORMA_API_URL`, and service role.
Colocated Deno tests pin disabled skip, deliveries row plus send,
failure error recording, fixture capture naming, and no CTA.

Skip choice: receipts off or missing chat id returns `skipped` with
no Telegram call and no `deliveries` row.

Round 1 APPROVE with zero findings. Pin:
`deno test --allow-read supabase/functions/_shared/deliver-telegram.ts`
(9 passed).

Contract gaps remain for T7.4 and T7.3 callers. See
`t3.4-contract-change.md`.

Status board: P3 → **5 / 5** (T3.1 through T3.5 done).
