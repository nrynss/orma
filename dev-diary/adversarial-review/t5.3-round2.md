# Adversarial review: T5.3, round 2

- Reviewer: RevT53R2
- Date: 2026-09-14
- Subject: uncommitted T5.3 work on `main` after the F1 header fix, measured against
  commit `b3f8689`. Round 1 returned REMEDIATE with one C.
- Specification: `dev-diary/PHASE-5-auth-shell.md` task T5.3, plus `t5.3-contract-change.md`.
- Verdict: **APPROVE**

## Counts

| Severity | Round 2 | Round 1 |
|---|---|---|
| C | 0 | 1 |
| H | 0 | 0 |
| M | 0 | 0 |
| L | 0 | 0 |

## Zero residue against round 1

Every pin in this table is a fresh measurement by this reviewer. None is quoted from the
implementer, the remediator, round 1, or the dispositions.

| Finding | Severity | Disposition under review | This round's own pin | Residue |
|---|---|---|---|---|
| F1, `headers()` sent the user JWT as `apikey` | C | `TelegramLinkClient` requires `anonKey`. Missing it throws. `headers()` sets `apikey` to that publishable key and `authorization` to `Bearer` plus the user JWT. Settings `creds()` passes `anonKey: getPublishableKey()`. `npm run check` runs `scripts/check-telegram-link-headers.ts`. | Throwaway auth user plus `public.profiles` row through `https://orma-api.nryn.dev`. Password grant JWT `role=authenticated`. POST with JWT as both headers returned 401 `Invalid API key`, count 0. `mintLinkToken` from the repo helper sent anon `apikey` and user Bearer, DELETE 204 then POST 201, count 1. Deep link prefix `https://t.me/orma_tele_bot?start=`. Live `D9CZDZBY.js` has `apikey:s(e),authorization:\`Bearer ${t}\``. Live Settings node calls `getSession()` then `anonKey:S()` where `S` is `getPublishableKey`. A `/tmp` copy with `apikey: accessToken` failed the header script (`mint: apikey was not the anon key`, exit 1). The repo helper still has `apikey: key`. | None |

## Pin results

Live app pins used `https://orma.nryn.dev`. API pins used `https://orma-api.nryn.dev`.
HTTP `Date` on those responses was 13 Sep 2026, 18:47Z to 18:55Z. Curl carried a browser UA.
`.env` was read privately. No secret, JWT, cookie value, or phone number is written here.

1. **Diff scope.** T5.3 writes are `web/src/lib/supabase.ts`, `web/src/lib/telegram-link.ts`,
   `web/src/hooks.server.ts`, `web/src/routes/+layout.ts`, `web/src/routes/+layout.server.ts`,
   `web/src/routes/+layout.svelte`, `web/src/routes/login/`,
   `web/src/routes/app/settings/+page.svelte`, `web/package.json`, `web/package-lock.json`,
   and `web/scripts/`. Owns plus the contract-change file cover every path. Review markdown
   under `dev-diary/adversarial-review/` is outside production. `supabase/config.toml` and
   `supabase/functions/auth-telegram/` were left unread as T5.2.

2. **`npm run check`.** In `web/` it prints `secret-key check passed`, then
   `telegram-link header check passed`, then svelte-check with 0 errors and 0 warnings.
   `package.json` `check` invokes `scripts/check-no-secret-key.mjs` and
   `scripts/check-telegram-link-headers.ts` before svelte-check.

3. **Header mutant.** A `/tmp` copy of `telegram-link.ts` restored `apikey: accessToken`.
   The header script on that copy exited 1 with `mint: apikey was not the anon key`.
   The repo file still has `apikey: key`. Re-running the header script in `web/` printed
   `telegram-link header check passed`.

4. **Secret-key plant.** Clean tree: `secret-key check passed`. `SUPABASE_SECRET_KEY=x` in
   `src/plant.env` exited 1. `sb_secret_` in `src/plant.ts` exited 1. A `service_role` JWT
   in `src/plant.ts` exited 1. Plants were removed. The repo has no plant file. Check passed
   again.

5. **Client URL and keys in the shipped JS.** Live `/_app/immutable/chunks/B7Bby2KJ.js`
   inlines `https://orma-api.nryn.dev` and throws if the URL contains `supabase.co`.
   The baked JWT `role` is `anon`. `sb_secret` appears only inside the library helper
   `startsWith("sb_secret_")`. `getRequestEvent` is absent. Live `D9CZDZBY.js` (telegram-link)
   posts to `https://orma-api.nryn.dev/rest/v1`. `supabase.co` there is only the project-host
   throw. Live Settings node `3.yIPbZFDf.js` has no paste form.

6. **Live routes, curl with a browser UA.** `GET https://orma.nryn.dev/` is 200 and public.
   `GET /login` is 200. `GET /app/settings` with no cookies is 303 `Location: /login`, and
   `-L` lands on `/login` 200. `GET /app` is 303 `/login`. `/login/callback` without a token
   is 200.

7. **Login copy and both paths.** Live login HTML has the email form and
   "Continue with Telegram". Live node `4.CCyq_xSf.js` calls `signInWithOtp` with
   `emailRedirectTo` from `LOGIN_CALLBACK_URL` (`https://orma.nryn.dev/login/callback` in
   `B7Bby2KJ.js`). It `fetch`es `${api}/functions/v1/auth-telegram`. It injects
   `https://telegram.org/js/telegram-widget.js?22` with `data-telegram-login` from
   `TELEGRAM_BOT_USERNAME` (`orma_tele_bot` in `D9CZDZBY.js`).

8. **Settings source.** Repo Settings calls `mintLinkToken`, `unlinkTelegram`, and
   `loadTelegramLinkState` with `{ accessToken, userId, anonKey: getPublishableKey() }`
   from `getSession()` then `data.session`. There is no paste-JWT form and no second mint
   path. Live `3.yIPbZFDf.js` matches that. It has "Mint Start link".

9. **Signed-in reload.** Admin `generate_link` for a throwaway user returned `hashed_token`.
   `GET /login/callback?token_hash=...&type=magiclink` followed one redirect (303 then 200),
   set cookie `sb-orma-api-auth-token` (`Secure`, `SameSite=Lax`, `Path=/`), and landed on
   Settings 200 with "Mint Start link". A second `GET /app/settings` with only that cookie
   was 200 with the same page and no `Location`. No paste form. Unsigned `/app/settings`
   is 303 `/login`, so the cookie is what kept the session. Curl plus the Set-Cookie jar
   is the reload pin.

10. **Settings mint row count (F1).** A throwaway auth user and `public.profiles` row were
    created through `https://orma-api.nryn.dev` with the service role. Password grant minted
    a user JWT with `role=authenticated`. Control POST to `/rest/v1/telegram_link_tokens`
    with the JWT as `apikey` and as `Authorization: Bearer` returned 401 `Invalid API key`.
    The hint named the anon or service_role API key. `telegram_link_tokens` for that
    `user_id` stayed 0. `mintLinkToken` from the repo helper used the anon key as `apikey`
    and that JWT as Bearer. Wrapped fetch saw DELETE 204 then POST 201, both to
    `orma-api.nryn.dev`, `apikey_is_anon=true`, `auth_is_bearer_user=true`,
    `apikey_is_jwt=false`, no project host. The count became 1. Deep link prefix
    `https://t.me/orma_tele_bot?start=`, token length 43, charset matched
    `LINK_TOKEN_PATTERN`.

11. **Deep link form.** Helper source, live `D9CZDZBY.js`, and the helper mint above all
    build `https://t.me/orma_tele_bot?start=`. `HEAD https://t.me/orma_tele_bot` returned
    200. A Telegram Start press was not executed.

12. **Probe hygiene.** Mint user, reload user, and the failed-callback user were deleted.
    Admin readback was 404. `telegram_link_tokens` and `profiles` for those ids were 0.
    `GET /auth/v1/admin/users` then listed 0 users, 0 matching the `rev-t53r2-` prefix.
    Probe workdirs under `/tmp` were removed. Numbers were masked. No secret was printed.

13. **Doc style on authored comments.** Added comments have no semicolon and no dash.
    Longest scanned comment sentence is 14 words. Login gloss keeps the design dash from
    the landing copy. Settings strings stay short.

## Findings

None. The round carries zero new findings across all severities.

## Notes, not findings

- T5.2 files were not reviewed. The login page still posts to `/functions/v1/auth-telegram`.
  That is the bridge, not the F1 mint path.
- No browser MCP was available. The network-tab pin is the shipped JS host list, not a
  DevTools capture. The only API host in app code is `https://orma-api.nryn.dev`.
- The landing footer still says sign-ups are closed. T5.3 does not own
  `web/src/routes/+page.svelte`.
- `app.d.ts` leaves `App.Locals` empty. `getAuthLocals` casts. `npm run check` still passed.
- Admin `GET /auth/v1/admin/users` with `SUPABASE_SECRET_KEY` returned 401. The same call
  with `SUPABASE_SERVICE_ROLE_KEY` returned 200. That is a key-name fact on this project,
  not a T5.3 web defect.
- The status line `claimed:orma-impl-53` was left untouched. This review changed no code,
  no config, and no status line.
