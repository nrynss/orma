# T2.7 remediation, round 3

Remediator: T27Remediate3. This round answers `t2.7-round3.md`, verdict REMEDIATE, C 0, H 0, M 1, L 3.
I did not implement, review or remediate T2.7 in an earlier round. The verdict is unchanged. All four
findings are fixed. I record no finding as a false positive, and I fixed nothing nobody found.

Owned paths touched: `supabase/functions/_shared/ingest.ts`,
`supabase/migrations/20260912180000_ingest.sql` and `supabase/migrations/test-ingest.sh`. No other
repository file changed. The RPC signature is unchanged, so the two `database.types.ts` files stay
untouched and no contract change row is needed.

File hashes at the end of this round:

```
04c608a1e22c0e09346052b7962b3e17  supabase/functions/_shared/ingest.ts
be80104cbf768ec69983d1f7304ffd3f  supabase/migrations/20260912180000_ingest.sql
dda3886fd8474fe5f76b8b9a4731a383  supabase/migrations/test-ingest.sh
```

## Acceptance outputs, observed

```
$ deno check supabase/functions/_shared/ingest.ts        exit 0
$ deno check supabase/functions/_shared/calle.ts         exit 0
$ deno check supabase/functions/_shared/dispatch-mode.ts exit 0
$ deno check supabase/functions/_shared/poll.ts          exit 0
$ deno test --allow-env --allow-read --net ... /ingest.ts
ok | 10 passed | 0 failed (30ms)
$ bash supabase/migrations/test-ingest.sh                EXIT=0 in 27.54s
T2.7 a busy port shifted the port band.
T2.7 completed fixture produced its rows.
T2.7 re-ingest changed no row.
T2.7 invalid fixture left a transcript and a disposition.
T2.7 unoffset retirement was rejected.
T2.7 concurrent ingest waited on the run lock.
T2.7 every boundary value ingest.ts accepts committed in PostgreSQL.
T2.7 all 18 values ingest.ts rejects raise in PostgreSQL.
T2.7 unmodified completed fixture rows: run_items=1 user_items=5 mentions=1 offsets=44 commitments=0 retired=1
T2.7 a call that recovered after a give-up cleared the give-up marker.
T2.7 the ingested row carried the repair list beside the counts of the rows.
T2.7 a failed call kept CALL-E's failure: poll 'failed not_answered null call_failed calling task status=NO ANSWER (Hangup by: bot) true' webhook 'failed not_answered null call_failed calling task status=NO ANSWER (Hangup by: bot) true'.
T2.7 a replay reported the stored proposal: summary 'yes' stored 'yes true 1'.
T2.7 ingestion acceptance passed.
```

The Deno suite grew from 9 tests to 10, and the acceptance script keeps all eighteen rejection values
and both boundary tables. The last two printed lines are the new SQL pins for findings one and four.

## Findings

| Finding | Remediation | Pin | Mutation covered |
| --- | --- | --- | --- |
| **M. The `calle_failure` clear over-reached.** The transferred round-2 L fix wrote `calle_failure = null` on every ingest. The poll writes CALL-E's own terminal failure there first and leaves `completed_at` null, so the finaliser re-fetches the run and the ingest wiped the provider reason. | The final `update public.call_runs` now takes its value from a case expression. When the payload names a `failure_code`, the run keeps that failure verbatim, with the payload's `failure_message`. When the payload names none, the run clears only a stored `poll_timeout`, which the payload contradicts. Any other stored value stays. A webhook-terminalised failed run, which carried no failure at all, now receives the payload's own failure. | Case eleven of `test-ingest.sh`. Run `...0af` carries the poll's whole terminal patch, the state, the writer tag, the confidence and CALL-E's failure, with no `completed_at`. Run `...0b0` carries the webhook's terminal state, which has no failure. Both ingest `testdata/calle/call-failed.json`. SQL reads `failed not_answered null call_failed calling task status=NO ANSWER (Hangup by: bot) true` for each. Case nine still measures the give-up clearing. | Reverting only the fix in the scratch tree `/tmp/t27r3-rem/f1` fails case eleven with `the run the poll failed with CALL-E's own failure read 'failed not_answered null null null true'`, and the script exits 1. Every earlier case stays green, so the failure belongs to case eleven. |
| **L. `PHONE_SCAN` missed a comma separator.** The run joined digit groups with `[\s()._/-]*`, so a comma broke the number into short runs. The whole number could leave in a thrown cause, or its first group could survive beside the mask. | `PHONE_SCAN` now joins digit groups across a comma as well. `scrub` masks the comma spellings whole, so no fragment of the number survives. | The Deno test `an RPC failure names its cause without a phone number or a key`, whose table grew from six spellings to eight. The two new rows are `###,###,####` and `###, ### ####`, and each asserts the masked marker and the surviving digit set. | Reverting only the separator class in `/tmp/t27r3-rem/f2` fails the test with `###,###,#### was not masked`, and `FAILED | 9 passed | 1 failed`. The other six spellings stay green. |
| **L. `requireTurns` refused shapes the column holds.** The round-2 M fix relaxed the checks to types, so a null, absent or string `offset_seconds`, a missing `speaker` and a null `text` still threw before any request left. The header's rule was false for all five. | `requireTurns` sanitises the turns and returns them as they arrived. It keeps every field the provider sent, so all five shapes reach the jsonb column. The header now says that only the characters a jsonb cast refuses are replaced. | The Deno test `a turn shape the column stores reaches the RPC`. It sends the five shapes in one call and compares each one on the wire body. | Reverting only `requireTurns` in `/tmp/t27r3-rem/f3` fails the test with `transcript turn 0 requires a numeric offset`, raised inside the old function, and `FAILED | 9 passed | 1 failed`. The sanitisation half of the round-2 fix stays green in that run. |
| **L. The `already_ingested` branch echoed the retry's request.** The branch returned `p_structured ->> 'slot_change_requested'`, so a replay carrying another proposal reported a value the run does not hold. | The branch reads the stored `results.structured` into a variable and returns its `slot_change_requested`. The `disposition` already came from the locked run row, so no field of the summary now comes from the retry's request. | Case twelve of `test-ingest.sh`. The first ingest stores `yes`. The replay carries `no`. The summary reads `yes`, and SQL reads the stored row as `yes true 1`, which is the proposal, the validity and the one `ingested` row. | Reverting only the branch in `/tmp/t27r3-rem/f4` fails case twelve with `the replay reported the proposal 'no', not the stored one`, and the script exits 1. Every earlier case stays green. |

## Reverted-fix runs

Each revert ran in its own copy of the tree under `/tmp/t27r3-rem`, built from the final three owned
files plus `fixtures.ts`, `result.ts`, every migration, `seed.sql` and `testdata/calle`. A diff between
that base tree and each revert shows one changed construct and nothing else. The two SQL reverts ran
the whole `test-ingest.sh` in the copy, which starts its own throwaway Supabase project. The two Deno
reverts ran `deno test --allow-env --allow-read --allow-net` on `ingest.ts`.

| Revert | Pin that failed | Observed |
| --- | --- | --- |
| `calle_failure = v_failure` back to `calle_failure = null` | case eleven | `the run the poll failed with CALL-E's own failure read 'failed not_answered null null null true'`, EXIT=1 |
| the comma dropped from the `PHONE_SCAN` class | the scrub test | `error: Error: ###,###,#### was not masked`, then `FAILED \| 9 passed \| 1 failed`, EXIT=1 |
| the round-2 `requireTurns` restored | the turn-shape test | `error: Error: transcript turn 0 requires a numeric offset`, raised inside `requireTurns`, then `FAILED \| 9 passed \| 1 failed`, EXIT=1 |
| the branch back to `p_structured ->> 'slot_change_requested'` | case twelve | `the replay reported the proposal 'no', not the stored one`, EXIT=1 |

Every failure message names the measurement that failed, and in all four runs the cases before it stay
green. The phone digits in the second row are masked here, and the number is the test's own synthetic
spelling.

## Residue against rounds 1 and 2

Zero, with how I checked it. The full acceptance script exits 0 on the final revision, so all ten
earlier cases stay green, including `unoffset retirement was rejected`, the concurrent-lock case, both
boundary tables and the repaired-entry list. The Deno suite exits 0 with 10 tests, so the sanitisation,
the repair table, the boundary dues, the re-ingest case and the key redaction all still hold. The two
round-2 halves that my changes touch keep their own pins. The turn sanitisation survives, because
`storableJson` replaces U+0000 and every unpaired surrogate. The give-up clear survives as case nine.

## Live drive through the shared stack

The pin for both SQL findings also runs as a live measurement, through real PostgREST at
`ORMA_API_URL=http://127.0.0.1:57031`, with the real `ingestTerminalRun`. The driver is
`/tmp/t27r3-rem/live-drive.ts`, and every row it touched carries the `t27r3r` marker. It never dialled
CALL-E, because it reads the recorded payloads and posts to the RPC alone.

The three seeded runs read, in SQL, after the drive:

```
failed:    failed not_answered null call_failed calling task status=NO ANSWER (Hangup by: bot)
           completed_at=true transcripts=1 results=1 events=1
recovered: completed answered_extracted unknown null completed_at=true
slot:      stored=yes valid=true results=1 events=1 runs_events=1
```

The failed run is the one the poll left. The recovered run carried a `poll_timeout` give-up and now
reads null. The slot run stored `yes`, and the replay carried `no`. The module returned
`alreadyIngested: true` with `slotChangeRequested: "yes"` for that replay, which is the value SQL
holds.

## The shared stack's function and its ACL

The shared stack carried the old body. I recreated the function there with
`create or replace function` from the migration file, then re-applied the three `revoke` statements
and the `grant` as the migration writes them. The ACL now reads
`postgres=X/postgres service_role=X/postgres`, with `anon=false`, `authenticated=false` and
`service_role=true`. That is what a fresh migration run reads. The stack granted execute to `anon` and
`authenticated` before this round, which is the deployment risk the round-3 review named in its
observation section.

The stored body is byte-identical to the migration's body, compared line for line between `pg_proc.prosrc`
and the text between the dollar quotes. The signature is unchanged, so no schema reload was needed.

## Cleanup

Every row this round created is deleted. The shared stack then read
`users=0 profiles=0 runs=0 runs_by_id=0 items=0 mentions=0 events=0 results=0 transcripts=0` for the
`t27r3r` marker and the three run ids. Each revert run stopped its own throwaway Supabase project. The
scratch trees under `/tmp/t27r3-rem` sit outside the repository, and they stay for the next reviewer. No
real call was placed, `CALLE_API_BASE` stayed the stub host, and `tick` was never driven.
