# T8.1 round 3 review

- Reviewer: RevT81R3 (adversarial reviewer, did not implement or remediate)
- Date: 2026-09-14
- Subject: uncommitted `scripts/seed.ts` on `main` at `8634482`, 1206 lines, after remediation round 2
- Specification: the T8.1 block in `dev-diary/PHASE-8-ship.md`
- Prior rounds: `t8.1-round1.md` (F1 to F6), `t8.1-remediation-round1.md`, `t8.1-round2.md` (F7 to F12), `t8.1-remediation-round2.md`

## Verdict

**REMEDIATE**

| C | H | M | L |
| --- | --- | --- | --- |
| 0 | 0 | 1 | 1 |

All round 1 and round 2 defects (F1 to F12) show zero residue. Two new findings prevent approval under the zero tolerance rule. The verification helper omits `p_now` when calling `assemble_briefing`. This omission breaks age verification whenever `--demo-date` differs from today. An unused variable declaration in `removeSeed` also causes `deno lint` to fail.

## Scope

I read `AGENTS.md`, the T8.1 specification, and all prior review and remediation files. I examined the full diff of `scripts/seed.ts` against `main`. I inspected `assemble_briefing`, `compute_pattern_facts`, `calle.ts`, `briefing.ts`, and `dispatch-mode.ts`. I performed independent measurements on a fresh local stack. I modified no application code.

## Independent evidence

Every measurement below reflects my own run against `scripts/p6-local-stack.sh` on port 58321. I tested accounts `firstrun@example.com` (America/New_York) and `local@example.com` (Asia/Kolkata). The service role key was kept secret. The local stack was torn down cleanly afterward.

1. **Baseline.** The database holds 3 profiles, 8 items (4 seeded), 7 runs (0 seed-key), 5 mentions, 2 events, 1 transcript, and 1 result. Slots (3), consents (3), scheduled runs (1), and pattern reports (1) match expectations.
2. **Plan run.** `deno run scripts/seed.ts --email firstrun@example.com --demo-date 2026-09-14` exits 0. A full database snapshot diff against baseline shows zero rows written.
3. **First `--apply` on `firstrun@example.com`.** The command writes 3 items, 3 runs, 5 mentions, 9 events, 3 transcripts, 3 results, and 4 commitments. Slots, consents, scheduled runs, and pattern reports remain unchanged.
4. **Second `--apply` (idempotence).** A second run leaves identical table counts across all tables.
5. **`--apply` on `local@example.com`.** The command seeds 3 items and 3 runs cleanly on the local developer account.
6. **`--remove` on `local@example.com`.** The rollback removes all 3 seeded items and 3 seeded runs. All 4 items from `supabase/seed.sql` remain completely intact.
7. **`assemble_briefing` verification.** An independent SQL call to `assemble_briefing` for `firstrun@example.com` returns `lead_line` "You've mentioned the dentist 3 times. It's been 34 days."
8. **Pattern history window.** `compute_pattern_facts` over 7 local days ending yesterday (`2026-09-07` to `2026-09-13`) returns `observed_days = 3` and `sufficient_history = true`.
9. **Timezone independence.** I evaluated `dateTimeInTz` across 5 host environments (`Asia/Kolkata`, `UTC`, `America/New_York`, `Pacific/Auckland`, `Europe/London`). New York 08:00 consistently returned `12:00:00.000Z`.
10. **Dispatch rows.** Seeded runs at demo-6, demo-4, and demo-3 store lead lines "Everything open is still current.", "It's been 30 days.", and "It's been 31 days." Each lead strictly reflects rows existing before that run.
11. **Dry-run request body.** Each `dispatched` event carries a masked CALL-E request with dynamic `ORMA_API_URL` (<redacted> webhook) and masked phone. The task text contains "Hello. This is Orma." and the full 7 steps.
12. **Captured item timestamps.** Weekend groceries has `created_at` matching run 3 `completed_at` (`2026-09-11 12:02:00+00`). The comparison `created_at <= completed_at` is true.
13. **Ingested mention counts.** Ingested and finalised mention counts match `results.structured` entry counts (1, 2, 2) and actual mention rows (1, 2, 2).
14. **Disclosure text voice.** The disclosure paragraph uses active voice throughout. I found zero passive clauses. All sentences have 30 words or fewer.
15. **Non-local safety guard.** `ORMA_API_URL=https://orma-api.nryn.dev` with `--apply` exits with code 1. Adding `--i-confirmed-dry-run` bypasses the guard.
16. **User pagination.** I created 55 dummy users so `firstrun@example.com` sat on page 2. `lookupProfile` successfully paginated to page 2 and resolved the profile.
17. **Retirement survival (F1).** I retired the dentist in SQL and added a null-run mention. A rerun preserved `retired` status, `retired_reason`, and all 4 mentions.
18. **`--remove` on `firstrun@example.com`.** The rollback deleted all seeded rows. The account's 2 non-seeded items survived. The database returned to exact baseline counts.
19. **Teardown.** `scripts/p6-local-stack.sh down` completed cleanly. Zero `orma-p6-local` containers remain.

## Residue against round 1 and round 2

| ID | Prior claim | Residue | Evidence |
| --- | --- | --- | --- |
| F1 | Rerun preserves item status and non-seed mentions | Zero | Evidence 17 |
| F2 | Runs at demo-6, -4, -3 yield 3 observed days | Zero | Evidence 8 |
| F3 | Transcript mention offsets match spoken turns | Zero | Verified in SQL |
| F4 | Stored briefing and dry-run dispatcher request body | Zero | Evidence 10, 11 |
| F5 | User lookup paginates `auth.admin.listUsers` | Zero | Evidence 16 |
| F6 | Groceries source is `call` with matching timestamps | Zero | Evidence 12 |
| F7 | `dateTimeInTz` is invariant to host machine timezone | Zero | Evidence 9 |
| F8 | Briefing lead lines backed by prior rows at dispatch | Zero | Evidence 10 |
| F9 | Full CALL-E task template and dynamic webhook URL | Zero | Evidence 11 |
| F10 | Captured item `created_at <= completed_at` | Zero | Evidence 12 |
| F11 | Ingested mention count matches structured entries | Zero | Evidence 13 |
| F12 | Disclosure paragraph written in active voice | Zero | Evidence 14 |

## Findings

| ID | Sev | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- | --- |
| F13 | M | `verifyCounts`, lines 1005 to 1008 | `verifyCounts` calls `assemble_briefing` without passing `p_now`. The function defaults to current time `now()`. When the operator passes `--demo-date` with any date other than today, age calculation uses today instead of the demo date. The script prints a failure claiming the lead line does not include age. | Run `deno run scripts/seed.ts --email firstrun@example.com --demo-date 2026-09-20 --apply`. Output prints `✗ Lead line does not include age: "You've mentioned the dentist 3 times."`. | Passing `p_now: dateTimeInTz(plan.demoDate, 8, 0, plan.profile.timezone)` evaluates the briefing at the demo date and succeeds under any demo date. Reverting the parameter restores the failure. |
| F14 | L | `removeSeed`, line 1059 | `removeSeed` declares `const userId = profile.id` but never uses it. This unused variable causes `deno lint` to exit with an error. | Run `deno lint scripts/seed.ts`. Output exits with code 1 and prints `error[no-unused-vars]: 'userId' is never used at line 1059:9`. | Removing `const userId = profile.id` allows `deno lint scripts/seed.ts` to pass with exit code 0. Restoring the line fails `deno lint`. |

## Notes

- The database contents for any `--demo-date` are correct. `assemble_briefing` returns 34 days when given the demo date timestamp. The defect in F13 lies solely in the script omitting `p_now` during verification.
- The stack was torn down cleanly. Zero containers remain.
- All sentences in this document contain 30 words or fewer. I used no semicolons and no em dashes.
