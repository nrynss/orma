# T6.7 reopen verification

## What changed and where

Only `supabase/migrations/20260914040000_owner_actions.sql` changed. No new migration file exists. No other file changed.

`delete_my_account` now calls `set_config` with `storage.allow_delete_query` set to true before it deletes storage rows. The call uses a transaction scoped flag. It names the production trigger `storage.protect_objects_delete` in a comment. It notes the Storage API uses the same switch. It notes Postgres accepts undefined two part settings as placeholders. The call stays a no-op where the trigger is absent.

The same file now ends with a `DO` block. The block creates `storage.protect_delete` only when that function is missing. It creates `protect_objects_delete` only when that trigger is missing. The mirrored body matches production verbatim. It checks the same setting name. It raises the same message with the same hint and code. It defines a statement level trigger like production. Comments state production manages its own safeguard and the block exists for local parity. The block changes nothing in production.

`cancel_call_run` stays byte identical. The diff touches no line inside it.

## Local pins from the scratch stack

Pin A pins the mirrored guard. A direct delete from `storage.objects` failed with `Direct deletion from storage tables is not allowed. Use the Storage API instead`. The row count stayed at 1 after the failed delete. The guard held locally.

Pin B pins the bypass. `delete_my_account` for scratch user `00000000-0000-4000-8000-00000000d001` returned true. The storage row count moved from 1 to 0. The auth row count moved from 1 to 0. The profile row count moved from 1 to 0. The function removed the storage row through the active safeguard and the cascades removed the rest.

Pin C pins unchanged cancellation. Cancelling an owned scheduled run returned true. The run moved to `canceled` with a `canceled` disposition and `billable` false. Exactly one `canceled` event row exists for it. A repeat cancel returned false. Cancelling a claimed run returned false. Cancelling an already canceled run returned false. Cancelling another account run returned false and the row stayed `scheduled`. A null id still raised `cancelling a call run requires an id`.

## Explicit reviewer focus

Check the setting name in `delete_my_account` against the trigger body. Both must read `storage.allow_delete_query`. Check the mirrored function body against production character by character. Check the trigger level reads `FOR EACH STATEMENT`. Check the no-op proof for production. Both creations sit behind existence checks on `pg_proc` plus `pg_namespace` and on `pg_trigger` plus `storage.objects`. A rerun of the block on the scratch stack changed zero bytes in the function definition and the trigger definition. Check the cancel regression above. Confirm the diff leaves every `cancel_call_run` line untouched.
