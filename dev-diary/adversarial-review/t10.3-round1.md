# T10.3 review, round 1

**Verdict:** REMEDIATE

**Counts:** C 0, H 1, M 0, L 0

## Scope and evidence

I reviewed T10.3 against its full phase specification, the T10.1 and T10.2
handoffs, and the current dirty tree. P9 page restyling remains baseline and
is not a finding. This review covers the new shared confirmation component and
its two mounts only.

`npm run check` passed in `web/`. It includes the secret, onboarding, Today,
auth-header, and Svelte checks. `git diff --check` also passed. The component
uses the authenticated confirmation function, calls the verifier RPC, has a
ten-minute resend lock, renders server errors, and makes no browser write to
`phone_confirmed_at`.

## Findings

| Where | What | Pin | Mutation |
| --- | --- | --- | --- |
| H1: `web/src/lib/phone-confirm/PhoneConfirm.svelte:96`, mounted inside `web/src/routes/app/onboarding/+page.svelte:144` and `web/src/routes/app/settings/+page.svelte:54` | `PhoneConfirm` creates its own `<form>` inside each page's existing form. The confirmation submit event bubbles after `verify` calls `preventDefault`, so it also invokes `startCalls` or `saveProfile`. On onboarding that re-runs setup while code verification is in flight. In Settings it submits profile settings as an unintended side effect. The confirmation step must expose a submit callback or stop propagation, and must not nest forms. | Browser confirmation-submit pin: mount each page, enter a valid code, submit it, and assert exactly one `verify_phone_code` RPC occurs with zero onboarding/profile write requests. It fails today because the ancestor form submit handler receives the same event. | Revert the form-boundary fix and confirming a code again re-runs onboarding or saves Settings, which can create conflicting requests and violates the single, in-place confirmation flow. |

## Zero residue

This is the first review round. There are no prior T10.3 findings to carry.
