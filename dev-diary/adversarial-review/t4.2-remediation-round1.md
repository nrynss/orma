# T4.2 remediation round 1

Verdict from review stays REMEDIATE. This note only records fixes. T4.2 is not marked done.

| Sev | Finding | Fix |
| --- | --- | --- |
| H | `registerOrmaTools` only pinned names so hollow callbacks stayed green | Test now invokes each `_registeredTools[name].handler`. Hollow `stub` callbacks fail. |
| M | Durable who missing on item row (`retired_by` response-only) | Encode `retired_reason` as `mcp:<userId>` or `mcp:<userId>:<note>`. Pin parses who from the store row. |
| L | `reversible` hardcoded plus double self-clear | Added `restoreRetiredItem` client clear path. Test pins restore updates the store row, not fixture assignment. |

## Owns

Only `supabase/functions/mcp/tools/` plus this diary file.
Index stub wiring stays a contract gap. Not edited here.

## Schema

`items` has no `retired_by` column. Who and surface live in `retired_reason`.
No T1.1 schema contract raised.

## Test

```bash
deno test --allow-net --allow-env --allow-read supabase/functions/mcp/tools/tools_tests.ts
```

Local result: **9 passed, 0 failed**.

Always ORMA_API_URL, user JWT, no service role.
