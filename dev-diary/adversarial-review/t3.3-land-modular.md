# T3.3 land note — modular restore (2026-09-12)

After the insert/edit UX fix, restoring the full monolith `voice.ts` via GitHub MCP
hit payload-size limits. Production behavior is unchanged:

- `captureVoiceNote` keeps insert success separate from edit (never `VOICE_FAIL_TEXT` after insert)
- Adapters live in `voice_rest.ts`, `voice_telegram.ts`, `voice_vertex.ts`
- Deno tests load via `voice_tests.ts` → `voice_tests_{b,b2,c,d}.ts`
- `voice.ts` re-exports adapters so the pin command stays:
  `deno test --allow-read supabase/functions/telegram/voice.ts`

Round-2 APPROVE still holds for the H finding. This note records the file layout only.
