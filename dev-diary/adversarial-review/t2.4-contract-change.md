# T2.4 contract change

Round one found that dispatch could not be reached from the deployed tick and
its webhook URL needed canonical configuration. Round two found that its now
working handler still had no minute scheduler. T2.4 now owns those seams.

| Where | What | Pin | Mutation |
| --- | --- | --- | --- |
| T2.4 ownership | Add tick wiring and canonical environment inventory paths. | Round-one C1 and C2 show dispatch is unreachable and startup lacks its URL. | Removing these paths leaves a passing client that never dispatches. |
| T2.4 ownership | Add `supabase/migrations/20260912170000_schedule_tick.sql` and the function-specific tick gateway configuration. | Round-two C1 shows that no tracked minute job invokes the dispatcher. | Removing either path leaves due calls scheduled forever or exposes a service-role-writing handler. |
