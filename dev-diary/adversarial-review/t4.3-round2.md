# T4.3 round 2: adversarial re-review of docs/mcp.md

**Verdict: REMEDIATE.**

Counts: C 0, H 0, M 1, L 0. One new finding. The five fixable round-1 findings
show zero residue. M3 stays adjudicated outside T4.3, and the facts behind that
ruling measure true.

## What I measured

I probed the live endpoint today with two throwaway accounts. I created each
account through the admin API with the service role key and inserted its
profile row by hand. Each account took a password grant with the anon key. I
deleted both accounts afterwards. I verified both deletions. Auth reads answer
404. Item and profile
counts read 0. The system reads 0 users and 0 profiles right now.

Connect. Initialize against `/mcp` returned 200 with `protocolVersion
2025-03-26` and `serverInfo orma 0.1.0`. I sent no `apikey` header. tools/list
named exactly the six documented tools in the doc's order. The same initialize
against `/mcp/` returned 200.

Shapes. add_item returned exactly the ten documented fields with
`source "mcp"`. list_items returned an array of that same shape and dropped
retired rows. retire_item returned exactly the thirteen documented fields with
`retired_surface "mcp"` and `reversible true`. get_last_call and get_patterns
returned `null` payloads on an account with no calls.

Failures. A missing token answered 401 with code `-32001` and message
`unauthorized: missing bearer token`. A malformed token answered the same code
with message `unauthorized: invalid or expired token`.

Restore. A PATCH under the caller token cleared the status and the retired
fields, and the item re-entered list_items. That measurement decides the one
finding below.

Hygiene and style. A scan of the whole file found zero semicolons and zero em
or en dashes. The longest sentence runs 25 words. The only UUID-shaped strings
are the three fabricated 1/2/3 patterns. No `eyJ` token, no `sb_secret` key,
no phone-shaped and no email-shaped string appears.

Scope. `git status --porcelain` shows the review subject, the adjudicated
README entry, the adjudicated untracked design file, the round-1 review file,
and this verdict file. That is the T4.3 landing set. Nothing else.

## Findings

| Where | What | Pin | Mutation |
|---|---|---|---|
| `docs/mcp.md`, section "The six tools", the list_items closing sentence | N1. The sentence "Once the items are retired or restored, the array comes back empty" is false for the restored case. A restored item is open again and re-enters the list. Round 1 measured that re-entry as true prose, and the reword replaced it with this falsehood. | Live probe today. I added an item, retired it, and the list dropped it. I restored it with a PATCH under the caller token, which answered 204. list_items then returned that exact row, matched on its id. The array held the item and did not come back empty. | The defect ships if the sentence stays. An agent that codes against the doc concludes a restored item stays hidden and misreads every later list response. Deleting the clause "or restored" restores the truth the previous sentence already carries. |

No round-1 finding carries residue. N1 is a new defect, not residue.

## Zero residue against round 1

Each row names the round-1 finding, my own pin from today, and the residue
verdict.

| Finding | Round-1 defect | My pin today | Residue |
|---|---|---|---|
| H1 | Present-tense claim that the web app restores a retired item | A scan of `web/src` finds no restore surface, only generated types and landing copy. All six `PHASE-6-web-app.md` tasks read `not-started`, and T6.2 owns retire and restore. The doc now says restores arrive with the web app. | Zero |
| M1 | Claim that the app's sign-up creates the profile | The landing page footer reads "Not yet open for sign-ups". No migration defines a profile-creating trigger. I had to insert each probe profile by hand. The doc now says accounts come provisioned with their profile row and sign-up opens later. The system reads 0 users and 0 profiles, so the claim holds for every account that exists. | Zero |
| M2 | Real probe identifiers in all six worked examples | A UUID scan finds exactly the three fabricated 1/2/3 patterns. No `eyJ`, no `sb_secret`, no phone-shaped and no email-shaped string appears anywhere in the file. | Zero |
| L1 | Claim that the proxy maps only the exact `/mcp` path | `proxy/src/index.ts` maps `/mcp` and `/mcp/` and nothing deeper. My initialize against `/mcp/` returned 200. The doc now names both forms. | Zero |
| L2 | Two actorless passive sentences | The endpoint sentence now opens with "The endpoint refuses". The examples sentence now opens with "The examples below show". Both name their actor. | Zero |

## M3 adjudication record

The orchestrator ruled M3 outside T4.3 at round 1. I verified the facts behind
the ruling and re-litigate nothing.

1. `git diff dev-diary/README.md` is a pure append. It adds 41 lines and
   deletes none. The whole addition is one entry, "2026-09-13 · T4.2 and
   T4.2a landed, MCP is live", the orchestrator's own handoff entry. It lands
   in the T4.3 landing commit per the ruling.
2. The design tree is one untracked file, `design/orma-app/orma-app-screens.html`.
   It appears in no diff of this task. `git diff --stat -- design/` is empty.
   On phase-4's history only two commits touch `design/`, both from the
   separate web design workstream, and neither contains this file. The file
   exists on another workstream's scratch branch only. No T4.3 agent created
   or touched it.

## Notes for the orchestrator

- N1 lives in `docs/mcp.md` prose. The fix deletes four words and touches no
  code path, migration, policy, configuration value or schema. Confirming it
  needs reading, plus the probe I already ran. It meets the four
  documentation-shortcut conditions in `AGENTS.md`.
- Round-1's H1, M1, M2, L1 and L2 need no further work. Their fixes hold.
- One observation, filed as no defect. The MCP transport requires an
  `Accept: application/json, text/event-stream` header. My first probe without
  it got 406. The doc's "any client that speaks Streamable HTTP" already
  covers this, since such a client sends those headers. Nothing to fix.

## Orchestrator closure

N1 is closed directly under the AGENTS.md documentation shortcut, on
2026-09-13. The list_items closing sentence in `docs/mcp.md` now reads "Once an
item is retired, the array no longer carries it", which matches the round-2
probe that showed a restored item re-entering the list. No code path, migration,
policy, configuration value or schema changed. Closed by the orchestrator in
the T4.3 landing commit. T4.3 flips to done on this round's zero residue.
