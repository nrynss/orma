# T3.3 round 1 review

## Verdict

**REMEDIATE**

| C | H | M | L |
| --- | --- | --- | --- |
| 0 | 1 | 0 | 0 |

## Findings

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| H | `captureVoiceNote` in `supabase/functions/telegram/voice.ts` | After `insertVoiceItem` succeeds, a throwing `editMessage` falls into the outer catch. The ack is rewritten to `VOICE_FAIL_TEXT`. The item row already exists. The user is told the capture failed. | Independent Deno probe against the shipped helper. `editMessage` threw on the first `Recorded:` edit. `listItemsByUser` returned one row. Result status was `failed`. Result `editText` equalled `VOICE_FAIL_TEXT`. The log contained `edit:9:` plus `VOICE_FAIL_TEXT`. | Restoring the single try around insert and edit makes a flaky `editMessageText` look like a failed transcription while the transcript and audio sit in the database. |

## Independent checks

`deno` was already on PATH as Deno 2.9.6.

`deno test --allow-read supabase/functions/telegram/voice.ts` reported 28 passed, 0 failed.

Eleven of those tests register because `voice.ts` imports `link.ts`. Seven register from `capture.ts`. Ten live in `voice.ts`.

Ack is the first log line. Download and transcribe run after ack. A linked capture edits message `9` with `recordedVoiceReply` of the transcript and attaches `voice_ok:<item-id>`.

The listed item `audioUrl` includes the item id and `item-audio`. It does not include `supabase.co`.

A throwing transcribe edits the ack to `VOICE_FAIL_TEXT`, attaches no confirm, and inserts zero rows. An empty transcript does the same.

An unlinked chat acks first, edits to `START_LINK_PROMPT`, and inserts nothing.

The REST double uploads to `https://orma-api.nryn.dev/storage/v1/object/item-audio/...` before `POST /rest/v1/items`. The insert body sets `audio_url` and `source` telegram. The service role value is absent from the body.

`createTelegramVoiceChat` sends `VOICE_ACK_TEXT` on `sendMessage` before `getFile`. A later `editMessageText` targets that `message_id` and includes the confirm button.

`vertexTranscriberFromEnv` with `GEMINI_MODEL` unset throws `missing GEMINI_MODEL`.

`rg captureVoiceNote supabase/functions/telegram/index.ts` matched nothing.

## Accepted for this round

The contract note is honest. T3.3 owns only `voice.ts`. T3.1 still must call `captureVoiceNote`. `items.audio_url` and bucket `item-audio` still need a T1.1 migration.

Done-when ack, in-place edit, stored audio link, and failed-transcription copy hold on the helper when the success edit succeeds. They do not hold on a live voice update until T3.1 calls it. That gap is the named contract change, not a silent owns breach.

Upload-then-insert can leave an orphan object if the items POST fails. The shipped REST test still passes. That gap is accepted for this round as a Storage cleanup concern outside the done-when pins.

Tests live in `voice.ts` because that is the T3.3 `owns` seam.

T3.3 status remains `claimed:orma`. This review does not mark the task done.
