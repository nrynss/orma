# Adversarial review: T5.3, round 1

- Reviewer: RevT53R1
- Date: 2026-09-13
- Subject: uncommitted T5.3 work on `main`. T5.2 files were present and ignored.
- Specification: `dev-diary/PHASE-5-auth-shell.md` task T5.3, plus `t5.3-contract-change.md`.
- Verdict: **REMEDIATE**

## Counts

| Severity | Count |
|---|---|
| C | 1 |
| H | 0 |
| M | 0 |
| L | 0 |

## Pin results

Every pin below is a fresh measurement by this reviewer. Nothing is quoted from the implementer.

1. **Diff scope.** T5.3 writes are `web/src/lib/supabase.ts`, `web/src/hooks.server.ts`, `web/src/routes/+layout.ts`, `web/src/routes/+layout.server.ts`, `web/src/routes/+layout.svelte`, `web/src/routes/login/`, `web/src/routes/app/settings/+page.svelte`, `web/package.json`, `web/package-lock.json`, and `web/scripts/`. Owns plus the contract-change file cover every path. `supabase/config.toml` and `supabase/functions/auth-telegram/` were left unread as T5.2.

2. **`npm run check`.** In `web/` it prints `secret-key check passed`, then svelte-check with 0 errors and 0 warnings.

3. **Secret-key plant.** A `/tmp` copy of `check-no-secret-key.mjs` passed on a clean tree. `SUPABASE_SECRET_KEY=x` in `src/plant.env` exited 1. `sb_secret_` in `src/plant.ts` exited 1. A `service_role` JWT in `src/plant.ts` exited 1. The plant directory was removed. The repo has no plant file.

4. **Client URL and keys in the shipped JS.** Live `/_app/immutable/chunks/B7Bby2KJ.js` inlines `https://orma-api.nryn.dev` and throws if the URL contains `supabase.co`. The baked JWT `role` is `anon`. `sb_secret` appears only inside the library helper `startsWith("sb_secret_")`. `getRequestEvent` is absent. Live `rDOGFoUT.js` (the telegram-link helper) posts to `https://orma-api.nryn.dev/rest/v1`. `supabase.co` there is only the project-host throw.

5. **Live routes, curl with a browser UA.** `GET https://orma.nryn.dev/` is 200 and public. `GET /login` is 200. `GET /app/settings` with no cookies is 303 `Location: /login`, and `-L` lands on `/login` 200. `GET /app` is 303 `/login`. `/login/callback` without a token is 200.

6. **Login copy and both paths.** Live login HTML has the email form and "Continue with Telegram". Live node `4.5_ZshcnE.js` calls `signInWithOtp` with `emailRedirectTo` `https://orma.nryn.dev/login/callback`, and `fetch`es `${api}/functions/v1/auth-telegram`. It injects `https://telegram.org/js/telegram-widget.js?22` with `data-telegram-login` `orma_tele_bot`.

7. **Settings source.** Live node `3.CI4rVP57.js` calls the existing mint helper with `getSession()` then `data.session` as fallback. It has "Mint Start link" and no paste-JWT form. Source matches that.

8. **Signed-in reload.** Admin `generate_link` for a throwaway user returned `hashed_token`. `GET /login/callback?token_hash=...&type=magiclink` followed one redirect, set cookie `sb-orma-api-auth-token` (`Secure`, `SameSite=Lax`, `Path=/`), and landed on Settings 200 with "Mint Start link". A second `GET /app/settings` with only that cookie was 200 with the same page and a non-null session. No browser DevTools session existed. Curl plus the Set-Cookie jar is the reload pin.

9. **Settings mint row count.** A throwaway auth user and `public.profiles` row were created through `https://orma-api.nryn.dev` with the service role. `POST /auth/v1/verify` minted a user JWT with `role=authenticated`. Helper-shaped `POST /rest/v1/telegram_link_tokens` (apikey and Bearer both the user JWT) returned 401 `Invalid API key`. `telegram_link_tokens` for that `user_id` stayed 0. The same body with `apikey` equal to the anon key and `Authorization: Bearer` equal to the user JWT returned 201. The count became 1. Finding F1.

10. **Deep link form.** Helper source and live `rDOGFoUT.js` build `https://t.me/orma_tele_bot?start=`. `HEAD https://t.me/orma_tele_bot` returned 200. The Settings UI never produced a link, because mint 401s. A Telegram Start press was not executed.

11. **Probe hygiene.** Both probe users were deleted. Admin readback was 404. `telegram_link_tokens` and `profiles` for those ids were 0. `GET /auth/v1/admin/users` then listed 0 users. Probe workdirs under `/tmp` were removed. Numbers were masked. No secret was printed.

12. **Doc style on authored comments and Settings copy.** Added comments have no semicolon and no dash. Login gloss matches `Login.dc.html` and keeps the design dash. Settings strings stay at or under 9 words per sentence.

## Findings

| # | Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|---|
| F1 | C | `web/src/lib/telegram-link.ts` `headers()`, called from `web/src/routes/app/settings/+page.svelte` `mint()`. T5.3 does not own the helper. | Settings reuses `mintLinkToken` with `session.access_token` as required. That helper sends the user JWT as `apikey`. Live PostgREST through the front door rejects that header. Mint therefore inserts no row, and the Start link never appears. The P3 handoff is blocked. A header fix needs a contract change on `web/src/lib/telegram-link.ts`. | Throwaway user plus profile. Helper-shaped POST returned 401 `Invalid API key`, hint `Double check your Supabase anon or service_role API key`, count 0. Anon `apikey` plus user Bearer returned 201, count 1. Cleanup readbacks were 404 and 0 rows. | Restore JWT-as-apikey headers and Settings mint 401s again. `telegram_link_tokens` for that user stays at 0. |

## Notes, not findings

- T5.2 files were not reviewed. `POST /functions/v1/auth-telegram` with the anon key returned 401 `unauthorized`. That is the unlanded bridge, not a T5.3 path defect. The login page still posts to that URL.
- No browser MCP was available. The network-tab pin is the shipped JS host list, not a DevTools capture. The only API host in app code is `https://orma-api.nryn.dev`.
- The landing footer still says sign-ups are closed. T5.3 does not own `web/src/routes/+page.svelte`.
- `app.d.ts` leaves `App.Locals` empty. `getAuthLocals` casts. `npm run check` still passed.
- The status line `claimed:orma-impl-53` was left untouched. This review changed no code, no config, and no status line.
