# T1.3 remediation round 1

| Finding | Remediation | Pin | Mutation |
| --- | --- | --- | --- |
| M1 | `types:generate` now writes the generator output to a temporary file. It copies that file to both consumers only after generation succeeds. | A forced generator failure exits nonzero and leaves both generated files unchanged. | Restoring the pipeline lets `tee` mask a generator error and erase both type contracts. |
