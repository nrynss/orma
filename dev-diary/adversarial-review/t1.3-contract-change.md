# T1.3 contract change request

## Live schema dependency and script ownership

T1.3 requires types generated from the live schema. The linked project initially
did not contain the local P1 migration. Generation therefore returned an empty
`public.Tables` object, despite the local schema migration defining every
contract table.

| Where | What | Pin | Mutation |
| --- | --- | --- | --- |
| Linked Supabase project `pyuubklpkhjngiqqwypf` | Apply the reviewed T1.1 migration before T1.3 generates and commits types. Resolved under operator authorization. | Initial pin: `supabase migration list --linked` reported local `20260911000000_core_schema.sql` and `20260911000001_rls.sql` with empty remote values. The approved file was applied alone through `supabase db query --linked --file`, then recorded. The current list shows T1.1 remote and T1.2 local only. | Committed empty types would erase every table contract from the web app and Edge Functions. |
| `dev-diary/PHASE-1-contracts.md` T1.3 ownership | Assign an owned location for the generation and drift-check script, or make the script a separate task. Resolved by adding `web/package.json` to T1.3. | The original seam owned only two generated files, but its done condition required `npm run types:check`. The extended seam now owns the package manifest that defines `types:generate` and `types:check`. | A seam-bound implementation cannot meet the drift-check condition without changing an unowned build surface. |

## Local migration inventory

The checked-in P1 migration files are:

- `supabase/migrations/20260911000000_core_schema.sql`
- `supabase/migrations/20260911000001_rls.sql`

The second file is an uncommitted concurrent T1.2 worktree change. T1.3 did not
review, modify, deploy, or include it. T1.3 generated both consumer files only
after applying T1.1.

## Verification

`npm run types:check` passes against the linked live schema. A temporary change
to `Json` made the check exit with status 1. The generated type was restored and
the check passed again.
