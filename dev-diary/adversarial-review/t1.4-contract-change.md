# T1.4 contract change request

## Scope conflict

T1.4 owns `testdata/calle/`. Its done condition requires a fixture loader in
`supabase/functions/_shared`, and its prose requires updating
`docs/calle-call.md`. Neither path belongs to T1.4.

| Where | What | Pin | Mutation |
| --- | --- | --- | --- |
| `dev-diary/PHASE-1-contracts.md` T1.4 | Extend T1.4 ownership to the fixture loader and CALL-E vocabulary document, or split each change into a separately owned task. | `git diff --name-only` shows either required path lies outside the current owns seam. | A seam-bound implementation cannot meet its stated done condition. |

## Operational blocker

T1.4 requires a new real CALL-E call to obtain `call-failed.json` and the
three webhook captures. No operator authorization exists for that chargeable
call or webhook action. No fixture was invented from the completed shape.

The blocked output is `testdata/calle/call-failed.json` and the
`testdata/calle/webhook-*.json` captures. T1.5 and T2.4 depend on the fixture
contract. T2.6 consumes the webhook captures.
