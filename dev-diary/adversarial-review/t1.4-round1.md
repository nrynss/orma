# T1.4 round 1 review

## Verdict

**REMEDIATE**

| C | H | M | L |
| --- | --- | --- | --- |
| 0 | 1 | 1 | 1 |

## Findings

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| H | `supabase/functions/_shared/fixtures.ts` | The required fixture loader does not exist. No typed object can return every file in `testdata/calle/`, so consumers have no frozen import contract. | `test -f supabase/functions/_shared/fixtures.ts` exits 1 while `testdata/calle/` contains four JSON files. | Removing the loader leaves T1.5 and the call engine parsing files ad hoc or unable to load the recorded contract. |
| M | `docs/calle-call.md` | The document does not record either observed failed-call value: top-level `failure_code` `call_failed` with its message, or attempt-level `failure_code` `408`. T1.4 requires the observed vocabulary. | `jq -r '[.failure_code, .failure_message, .recipients[0].attempts[0].failure_code] | @tsv' testdata/calle/call-failed.json` returns `call_failed`, `calling task status=NO ANSWER (Hangup by: bot)`, and `408`. Searching `docs/calle-call.md` finds none. | Removing the vocabulary makes later reconciliation code guess which failure fields are terminal versus attempt-level. |
| L | `dev-diary/adversarial-review/t1.4-contract-change.md` | The opening scope-conflict text says T1.4 does not own the loader or document. The current T1.4 `owns` line includes both paths. The later authorization note is current, but the opening decision record is stale. | The T1.4 `owns` line names `supabase/functions/_shared/fixtures.ts` and `docs/calle-call.md`, while the contract-change opening says neither belongs to T1.4. | Removing the correction misleads the next agent into reopening an already-resolved ownership conflict. |

## Independent checks

`jq -e .` accepted all four fixture files. `call-failed.json` has a terminal
`status` of `failed`, top-level `failure_code` and `failure_message`, plus the
provider attempt failure fields. A phone-pattern scan found no unmasked E.164
value in `testdata/calle/` or `docs/calle-call.md`.

No webhook fixture is a finding. The current T1.4 specification explicitly
defers webhook captures to T2.6, which owns the receiver and
`testdata/calle/webhook-*.json`. The stale contract-change record should state
that resolved ownership and timing decision plainly.
