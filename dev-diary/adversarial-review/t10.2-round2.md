# T10.2 review, round 2

**Verdict:** REMEDIATE

**Counts:** C 0, H 1, M 0, L 0

## Scope and evidence

I independently reviewed the T10.2 specification, round-one review, remediation
record, and the current owned diff. `deno check` passed for `confirm-phone`,
`calle-webhook`, and `calle.ts`. `deno test --allow-env --allow-net
--allow-read supabase/functions/_shared/calle.ts
supabase/functions/calle-webhook/index.ts` passed 88 tests. `git diff --check`
passed.

## Findings

| Where | What | Pin | Mutation |
| --- | --- | --- | --- |
| `supabase/functions/confirm-phone/index.ts:66-79`, `supabase/functions/calle-webhook/index.ts:273-281` | **H1: A successful CALL-E confirmation call can be lost when its post-call database patch fails.** The handler receives the provider call id, then treats a failed `phone_confirmations` patch as a failed call and does not retain that id. The subsequent webhook matches on both confirmation id and `calle_call_id`, so it updates zero rows, its false result is ignored, and the event is marked handled. The user cannot verify a code from the call that actually happened. A later request can place another call after the cooldown. Retain and reconcile the returned id, or leave the row in a recoverable dispatch state that the webhook can claim by confirmation id. | Drive the live dispatch seam with a provider `201` containing `call_live`, then make the first confirmation PATCH fail. Deliver a terminal webhook whose authoritative response carries `phone_confirmation_id` and `call_live`. The row must retain `calle_call_id = call_live` and a verification-eligible state, or the webhook must repair it. It currently reads `state = failed`, `calle_call_id is null`, then accepts the webhook without updating the row. | Revert the post-acceptance reconciliation. The same provider-success and database-write-failure pin strands the call and lets a later request dial again. |

## Zero residue

Round-one H1 has zero residue. The `expire_phone_confirmations_on_phone_change`
trigger expires requested and dialled rows on every phone change, so the A to B
to A mutation cannot recover A's code.

Round-one M1 has zero residue. Dry-run dispatch now stores a private masked
request, and the confirmation-specific masker removes the code, destination,
and webhook secret. The independent in-file pin passed.

H1 above is new residue introduced by the remaining live dispatch and webhook
handoff. It does not reopen either round-one finding.
