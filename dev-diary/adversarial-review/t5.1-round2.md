# Adversarial review: T5.1, round 2

- Reviewer: RevT51R2
- Date: 2026-09-13
- Subject: uncommitted diff on `supabase/config.toml` (45 insertions, zero deletions) plus the
  F4 prose rewrite in `dev-diary/PHASE-5-auth-shell.md`, measured against commit `9e9550d`.
  Round 1 returned REMEDIATE with one H, one M and two L.
- Specification: `dev-diary/PHASE-5-auth-shell.md` task T5.1 and its done-when.
- Verdict: **APPROVE**

## Counts

| Severity | Round 2 | Round 1 |
|---|---|---|
| C | 0 | 0 |
| H | 0 | 1 |
| M | 0 | 1 |
| L | 0 | 2 |

## Zero residue against round 1

Every pin in this table is a fresh measurement by this reviewer. None is quoted from the
implementer, the remediator, round 1 or the dispositions.

| Finding | Severity | Disposition under review | This round's own pin | Residue |
|---|---|---|---|---|
| F2, rate limit 30 exhausts in shared hours | H | `[auth.rate_limit] email_sent` raised 30 to 90, comment names Resend's daily ceiling | File declares 90. `supabase config diff` reports zero declared changes, so the live limiter equals 90. My run: ten `POST /auth/v1/otp` to a fresh address, 17:22:31Z to 17:22:59Z, 28 s wall, ten HTTP 200, zero 429, per-send 2.71 to 2.96 s. Resend's free tier allows 100 emails per day per resend.com pricing, so the comment is accurate and 90 sits under the real ceiling. | None |
| F1, four live settings undeclared | M | Four keys added to the file with their live values | `config diff` reports zero declared changes and the four keys no longer appear as `remote_only`. The file carries `enable_confirmations = true`, `otp_length = 8`, and both `auth.mfa.totp` flags true. Declared equals live on all four. | None |
| F3, six live settings no task owns | L | Accepted drift, to be named in the T5.1 landing handoff entry | `config diff` reports exactly six `remote_only` entries: `db.pooler.default_pool_size`, `db.pooler.max_client_conn`, `storage.analytics.enabled`, `storage.analytics.max_namespaces`, `storage.image_transformation.enabled`, `auth.sms.twilio.enabled`. That is the disposition's list, exactly, and nothing else. | None |
| F4, prose said HTTP API, artifact speaks SMTP | L | Closed by the orchestrator under the documentation shortcut | The rewritten prose names Supabase custom SMTP speaking Resend's SMTP interface from Supabase's own servers. It agrees with `host = "smtp.resend.com"`, port 587 and the AGENTS.md port-block scope. The edit changes no configuration value. The rewritten lines carry no semicolon, no dash, 26 words at most per sentence. | None |

## Pin results

All live pins ran through `https://orma-api.nryn.dev` between 17:22Z and 17:31Z on 2026-09-13.

1. **Diff scope.** `git status --porcelain` names the config file, the phase file and the
   untracked round 1 verdict. `git diff --stat` shows 45 insertions and zero deletions on
   `supabase/config.toml`, so the diff is a pure addition after `[functions.mcp]`. Every
   `[functions.*]` block, including both `verify_jwt = false` lines, stays byte identical.
   The phase file diff touches the status line and the one prose paragraph only.
2. **Config diff, zero declared differences.** `supabase config diff` on CLI 2.117.0, run
   read-only against project `pyuubklpkhjngiqqwypf` with the `.env` names exported. Six
   changes total, all class `remote_only`, zero `update`, zero `insert`, zero `delete`. The
   six are the F3 set. Nothing declared differs from live.
3. **Settings.** `GET /auth/v1/settings` with the anon key answers 200 in 0.35 s.
   `external.email` is true and is the only provider on. `mailer_autoconfirm` is false.
   `anonymous_users` is false. This matches the done-when shape.
4. **End-to-end magic link.** One `POST /auth/v1/otp` for `rev-t51r2-a@probe.nryn.dev`
   answered 200 in 3.30 s. That latency is the SMTP-submission signature, since GoTrue fails
   the request when the mailer fails. `POST /auth/v1/admin/generate_link` with the legacy
   `SUPABASE_SERVICE_ROLE_KEY` minted a magiclink action link in 0.10 s with
   `redirect_to=https://orma.nryn.dev`. The link, rewritten to the front door and followed
   with curl, made exactly one redirect and landed on `https://orma.nryn.dev` with HTTP 200
   and a session fragment. The mint step answered in 0.10 s, so it placed no second email.
5. **Rate run.** Ten sequential `POST /auth/v1/otp` for the fresh address
   `rev-t51r2-b@probe.nryn.dev`, 17:22:31Z to 17:22:59Z. Ten HTTP 200 with empty bodies,
   zero 429, wall time 28 s, per-send 2.71 to 2.96 s. Round 1 measured 18 sends at roughly
   16:45Z to 17:01Z, still inside this rolling hour, so the run is green with prior rounds'
   volume in the bucket, which is the exact condition F2 described.
6. **Secret hygiene.** A scan of `supabase/config.toml` finds no `re`-prefixed key shape and
   no `sb_secret`, `cfut_` or JWT-shaped string. The only credential values are
   `env(RESEND_API_KEY)` and `env(RESEND_FROM)`. The sender address resolves to
   `@send.nryn.dev`.
7. **Doc rules on added text.** `supabase/config.toml` contains zero semicolons and zero em
   or en dashes anywhere. The longest added comment sentence is 20 words. The rewritten task
   prose passes the same rules, 26 words at most per sentence, active voice.
8. **Probe hygiene.** The admin users list held exactly two users before deletion, both
   mine. Both deleted over the admin API, and both readbacks answer 404. The users list now
   holds zero rows. My session placed 11 auth emails into the rolling hour: one settings
   probe plus the ten-send rate run.

## Findings

None. The round carries zero new findings across all severities.

## Notes, not findings

- The working tree sits on `main` at `9e9550d`, which is the same commit as local
  `phase-4`. The brief named `phase-4`. The tree is identical, so no pin is affected.
- `supabase config diff` without the `.env` names exported reports a phantom `update` on
  `auth.email.smtp.admin_email`, because it cannot resolve `env(RESEND_FROM)`. Run the diff
  the documented way, with the names exported. This is reviewer error I made and corrected,
  not an artifact defect.
- The done-when line in the T5.1 block reads "lands on the app; and ten requests". That
  semicolon predates this task, sits in HEAD unchanged, and stays outside the F4 edit and
  the diff. Noted for the next editor of that file.
- The F3 landing handoff entry does not exist yet, correctly, because the task is not
  landed. The disposition binds the landing commit to name all six settings. The measured
  six-name set in the residue table above is the exact list for that entry.
- The status line `claimed:orma-impl-51` was left untouched. This review changed no code, no
  config, no status line, and left zero probe rows behind.
