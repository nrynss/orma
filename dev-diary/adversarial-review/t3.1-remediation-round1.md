# T3.1 remediation round 1

This remediation addresses H and M from `t3.1-round1.md`. It does not change
the verdict. T3.1 stays `claimed:orma`.

| Finding | Remediation | Pin | Mutation covered |
| --- | --- | --- | --- |
| H | Added `supabase/config.toml` with `[functions.telegram] verify_jwt = false`. T3.1 now owns that block. T5.1 still owns only the auth block. The contract note and PHASE-3 `owns` line name the same owner. PHASE-5 tells T5.1 not to edit JWT-off. | `test -f supabase/config.toml` exits 0. The file contains `[functions.telegram]` and `verify_jwt = false`. T5.1 `owns` remains `supabase/config.toml (auth block)`. | Removing the file or flipping `verify_jwt` leaves Telegram on the gateway JWT check. T5.1 magic-link work cannot restore webhook delivery. |
| M | `presentedWebhookSecret` returns the raw path rest after `/telegram/`. It no longer `decodeURIComponent`s. `telegramWebhookUrl` still inserts the secret as the path segment Telegram will POST. Compare stays constant-time. | `deno test --allow-net --allow-env --allow-read supabase/functions/telegram/index.ts` passed, 5 tests. The new test uses secret `abc%2Fdef`. `presentedWebhookSecret` on the registered pathname equals `abc%2Fdef`, not `abc/def`. GET and POST with that URL pass the matcher. | Restoring `decodeURIComponent` makes the percent-secret test fail. Telegram POSTs to the registered URL would 401. |
