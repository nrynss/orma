# T2.3 remediation, round 1

This remediation addresses C1 and H1 from `t2.3-round1.md`. It does not alter
the round-one verdict or the task status.

| Finding | Remediation | Validation | Mutation covered |
| --- | --- | --- | --- |
| C1 | The tick migration no longer creates `tick-runs`. The handler now skips `claim_due_call_runs` when T2.4 has not supplied `dispatchClaimed`. It can still advance later stages when those seams exist. | `deno test --allow-env --allow-net supabase/functions/tick/index.ts` includes a check that an absent dispatcher never calls the claim RPC. `git diff --check` must pass. | Restoring the cron job or an unconditional claim lets an unavailable dispatcher strand a scheduled call. |
| H1 | Added `supabase/migrations/test-tick-claims.sh`. It starts a temporary PostgreSQL-only local Supabase project on unique ports, applies every migration through `20260912161000_tick.sql`, and seeds one due run. Two independent `psql` sessions invoke the RPC as `service_role`. The first holds its claim transaction open. The second must return zero before the first commits. The runner asserts one returned row in total and one claimed row. | `bash supabase/migrations/test-tick-claims.sh` passed against a fresh local stack. It observed the applied migration, the function grant, PostgreSQL row locking, a nonblocking skipped claim, and the persisted row state. | Removing `SKIP LOCKED` blocks the second session. Separating selection from update returns two claims. Breaking the grant or migration makes the runner fail before its row assertions. |
