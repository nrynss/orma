# T8.1 remediation, round 1

Round 1 review `f21fa5d` returned REMEDIATE with C0 H2 M2 L2 against `scripts/seed.ts`.
This round remediates all six findings in `scripts/seed.ts`.

## Findings

| where | what | pin | mutation |
| --- | --- | --- | --- |
| H `applyPlan`, item upsert and `item_mentions` deletion | A rerun reverted live state to open and deleted non-seed mentions. | When the dentist was retired and given a fourth mention with a null run, `--apply` reverted the item to open and dropped mentions from 4 to 3. | `applyPlan` queries existing items first and preserves `status`, `retired_at`, and `retired_reason`. It deletes `item_mentions` strictly by `call_run_id in (seedRunIds)`. Reverting to blind upsert and item-id deletion restores the revert and data loss. |
| H `buildPlan`, run dates relative to demo date | Seeded runs were placed 28, 14, and 3 days before demo. Only one fell in the 7-day window ending yesterday. | On an account without test harness rows, `compute_pattern_facts` returned `observed_days = 1` and `sufficient_history = false`. | Run dates are now 6, 4, and 3 days before the demo date. All three fall in `[demoDate - 7, demoDate - 1]`. `observed_days` is 3 and `sufficient_history` is true. Moving runs outside the window empties the pattern report. |
| M `buildPlan` and `applyPlan`, transcript mention offsets | Mention offsets fell back to 0 for non-captured items. The passport mention landed at 0:00 instead of 0:10. | `item_mentions` for `renew the passport` held `offset_seconds = 0`, pointing at the dentist turn in the transcript. | `buildPlan` assigns explicit `offsetSeconds` for each mention. The passport mention sits at 10s in run 2, and groceries at 14s in run 3. Reverting to fallback 0 restores the misaligned turns. |
| M `applyPlan`, `call_runs.briefing` and `dispatched` event `request_body` | Runs stored `briefing = null`, and `dispatched` recorded `request_body = "{}"`. Timeline showed an empty request. | `call_runs.briefing` was null and `dispatched.detail.request_body` was `{}` on all three seeded runs. | `buildPlan` builds a `Briefing` object for each run, stored in `call_runs.briefing`. `applyPlan` saves the masked CALL-E request matching `dispatch-mode.ts` into `dispatched.detail.request_body`. Reverting to null or empty body shows blank Timeline requests. |
| L `lookupProfile`, user listing pagination | `listUsers()` fetched only the default first page of 50 users. Accounts beyond page 1 failed lookup. | User lookup called `auth.admin.listUsers()` without pagination arguments. | `lookupProfile` now loops through pages with `auth.admin.listUsers({ page, perPage })` until it finds the user or reaches the end. Reverting to the unpaged call fails lookups for accounts past page 1. |
| L `buildPlan`, `weekend groceries` item attributes | The item had `source = 'web'` and was created 5 days before demo, yet run 3 captured it 3 days before. | `weekend groceries` showed `source = 'web'` and `since_date` 5 days before demo, conflicting with its run 3 capture. | `weekend groceries` now has `source = 'call'`. Its `since_date` and `created_at` match run 3's date and completion timestamp. Reverting attributes restores the contradiction. |

## Proof

1. `scripts/p6-local-stack.sh up` verified against `local@example.com` and `firstrun@example.com`.
2. A plan run writes zero rows.
3. First `--apply` on `firstrun@example.com` seeds 3 items, 3 runs, 5 mentions, 3 briefings, and 9 events.
4. `compute_pattern_facts` on `firstrun@example.com` reports `observed_days: 3` and `sufficient_history: true`.
5. Retiring the dentist and adding a 4th mention survives a second `--apply` without reversion or mention loss.
6. Mention offsets match transcript turns: passport at 10s, groceries at 14s, dentist at 0s.
7. `call_runs.briefing` is populated on every run, and `dispatched.detail.request_body` contains masked CALL-E JSON.
8. `weekend groceries` is consistent: `source = 'call'`, `since_date = '2026-09-11'`.
9. `--remove` removes all seeded rows for the account, leaving local seed rows intact.
10. The stack was torn down cleanly. Zero containers remain.
