# T6.6 round-1 review: Operator timeline

Reviewer: T66Rev (fresh, did not implement). No implementation, migration,
type, or status file was modified. Ad-hoc scratch rows were removed after
measurement. Counts are back to the seed baseline.

Verdict: **APPROVE**

Severity counts: C 0, H 0, M 0, L 0.

## Pins (measured, not quoted)

- Seed baseline via psql: 7 `call_runs`, 2 `call_events`. The completed run
  `40000000-0000-4000-8000-000000000001` (local@example.com, completed,
  answered_extracted) carries exactly rows id 1 `dispatched`
  (2026-09-13 05:57:17 UTC) and id 2 `finalised` (2026-09-13 06:57:17 UTC)
  ordered by (at, id).
- Loader (`+page.ts` lines 31 to 46) orders the events query by at ascending
  then id ascending. `sortSteps` (`model.ts` lines 40 to 46) re-sorts the
  selected steps the same way. `groupByRun` (lines 52 to 65) sorts each
  group. Every clock renders through `formatClock(event.at, zone)` with the
  zone from the signed-in profile (`profiles.timezone`, Asia/Kolkata for the
  seed account), seconds shown on steps.
- Ad-hoc failed run `4f000000-0000-4000-8000-00000000f901` inserted per the
  handoff produced events id 3 `dispatched` and id 4 `finalised`, both under
  the seed owner. `stoppedRuns` (`model.ts` lines 84 to 94) filters state to
  failed or no_result and names the last sorted step kind, so the stopped
  section reads Stopped at finalised. Cleanup delete ran after measurement.
  Counts returned to 7 runs and 2 events.
- PostgREST as local@example.com: own `call_runs` returns 6 rows, own
  `call_events` returns ids 1 and 2, a query filtered to the live@example.com
  run `4f000000-0000-4000-8000-0000000000c1` returns `[]`, and the same run
  row query returns `[]`. As live@example.com the account sees only its own
  run with zero events. Anonymous event read returns `[]`. Every loader
  query is owner-scoped: runs by `.eq("user_id", userId)`, events by
  `.in("call_run_id", runIds)` built from those runs, profile by own id.
  An unknown `?run=` id falls into That run is not on your timeline, so a
  cross-user id leaks nothing.
- Dev server on port 5201 (`env $(../scripts/p6-local-stack.sh env) npm run
  dev -- --port 5201`): signed-out `GET /app/timeline` returns HTTP 303 to
  `/login`, and `/login` returns HTTP 200 titled Sign in. The signed-in 200
  body is client-rendered under the Supabase session so curl cannot hold a
  session for it. The guard redirect plus the loader trace above carry that
  clause instead.
- All eight kinds in `CALL_EVENT_KINDS` (`events.ts` lines 7 to 16) have a
  `glossFor` branch (`model.ts` lines 147 to 189): materialised, claimed,
  dispatched, polled, webhook_received, refetched, ingested, finalised. The
  default branch (lines 190 to 191) returns `Recorded ${kind}`, so an
  unknown kind renders verbatim and never crashes.
- Live tag: `isLiveState` covers claimed, dispatched, awaiting_result
  (`model.ts` line 28), rendered at `+page.svelte` line 98. Rehearsal tag on
  `dry_run` at line 99. Picker default `pickDefaultRunId` (lines 71 to 79)
  returns the newest run with events and falls back to the newest run, and
  `?run=` selects a known id or falls back to that default (lines 27 to 31).
  Refresh calls `invalidate('orma:timeline')` only (lines 51 to 58). Grep
  over the route finds no setInterval, setTimeout, or animation frame, so
  there are no timers. `maskPhones` (lines 100 to 102) replaces every
  `+\d{7,15}` shape before display and is applied to each payload at line 48.
- Scope: `git status` shows the new `web/src/routes/app/timeline/` plus its
  handoff for this task. Sibling paths (history, patterns, landing, phase
  doc, stack script) belong to concurrent tasks and were not touched.

## Findings

No findings. Every check above was measured against rows, responses, code
lines, or HTTP status codes.

| Where | What | Pin | Mutation |
|---|---|---|---|
| (none) | (none) | (none) | (none) |

## Done-when assessment (P6 T6.6)

Done when: a completed run reads as an ordered story, a failed run names
where it stopped, and no other user's events are reachable.

- Ordered story: pass. The seed completed run has exactly dispatched then
  finalised by (at, id), and loader, grouping, and step sort all use that
  order with profile-timezone clocks.
- Failed run names where it stopped: pass. The ad-hoc failed run rendered
  through `stoppedRuns` names finalised as the last kind, then was deleted
  per the handoff.
- No other user's events reachable: pass. Cross-user PostgREST reads return
  zero rows, every loader query is owner-scoped, and foreign `?run=` ids
  show the not-on-timeline state.
