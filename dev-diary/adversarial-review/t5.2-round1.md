# Adversarial review: T5.2, round 1

- Reviewer: RevT52R1
- Date: 2026-09-13
- Subject: uncommitted T5.2 work on branch `main` at `6d00da5`. New
  `supabase/functions/auth-telegram/index.ts` and a five-line
  `[functions.auth-telegram]` insert in `supabase/config.toml`. Sibling T5.3
  paths were present and ignored.
- Specification: `dev-diary/PHASE-5-auth-shell.md` task T5.2 and its done-when,
  plus `dev-diary/spec.md` section 7 and `t5.2-contract-change.md`.
- Verdict: **APPROVE**

## Counts

| Severity | Count |
|---|---|
| C | 0 |
| H | 0 |
| M | 0 |
| L | 0 |

## Pin results

Every pin below is a fresh measurement by this reviewer. Nothing is quoted from
the implementer. Live pins ran through `https://orma-api.nryn.dev` with curl
between 18:22Z and 18:25Z on 2026-09-13. Python computed HMAC locally. It never
called the front door.

1. **Diff scope.** `git diff --stat -- supabase/config.toml` is five insertions
   and zero deletions. `git diff -U0` inserts only the new table after
   `[functions.mcp]`. `[auth]` is not in the hunk. Every other `[functions.*]`
   key stays byte identical with HEAD. The function tree holds one untracked
   file, `supabase/functions/auth-telegram/index.ts`.

2. **In-file tests.** `deno test --allow-net --allow-env --allow-read
   supabase/functions/auth-telegram/index.ts` reported 11 passed, 0 failed, in
   28 ms. The independent test hash is SHA-256 of the bot token as the HMAC
   key, then HMAC-SHA256 over the sorted data-check string.

3. **Revert-copy pin.** A `/tmp` copy had the HMAC compare removed. The
   one-byte hash test failed with `expected 401, got 200`. The copy was
   deleted. The production compare is still present. No `/tmp` residue.

4. **Live CORS and methods.** OPTIONS returned HTTP 204. Allow-Origin was
   `https://orma.nryn.dev`, not `*`. Allow-Methods listed POST and OPTIONS.
   Allow-Headers listed content-type and authorization. GET returned 405 with
   `{error: method not allowed}` in 0.14 s.

5. **Gateway JWT off, mint still gated.** A POST with no Authorization and a
   one-byte hash flip returned 401 `{error: unauthorized}` in 0.14 s. That is
   the handler body, not a gateway missing-JWT page. A missing `hash` field
   also returned 401. A stale `auth_date` (now minus 301 s) with a valid HMAC
   returned 401 in 0.14 s.

6. **Live valid HMAC, working session.** A widget payload signed with
   `TELEGRAM_BOT_TOKEN` (token not printed) posted with no Authorization.
   HTTP 200 in 0.85 s. Body had `access_token`, `refresh_token`, `expires_in`
   3600, and `token_type` bearer. `GET /auth/v1/user` with that JWT returned
   200 for the same user id. `app_metadata.telegram_user_id` matched the
   widget id. 0.85 s is not the SMTP signature, so `generateLink` did not send
   mail.

7. **Both paths, one account, one item.** Admin created
   `rev-t52r1-email@probe.nryn.dev`. A bearer session plus a valid widget
   returned 200 for that user id and kept the email. It did not mint
   `tg-…@telegram.invalid`. Admin readback kept `provider` and `providers`
   and added `telegram_user_id`. A later widget POST with no bearer returned
   the same user id. A profile and one item were inserted for that user.
   `GET /rest/v1/items` with the telegram-login JWT returned that one row.

8. **Extra widget fields.** A payload with `last_name`, `username`, and
   `photo_url` in the HMAC returned 200. The same fields present with a hash
   that omitted them returned 401.

9. **Nothing signed in the database.** The new file has no `jwt.sign`, no
   `pgjwt`, and no local signing. The session comes from `POST /auth/v1/verify`
   with `token_hash` and `type` `email` after `POST /auth/v1/admin/generate_link`
   with `type` `magiclink` and the service role.

10. **`ORMA_API_URL` only.** `depsFromEnv` throws `missing NAME` for each of
    `TELEGRAM_BOT_TOKEN`, `ORMA_API_URL`, `SUPABASE_SERVICE_ROLE_KEY`, and
    `SUPABASE_ANON_KEY`. A `*.supabase.co` `ORMA_API_URL` throws. Live traffic
    used `https://orma-api.nryn.dev` only.

11. **Secret hygiene.** A scan of the new file and the config insert finds no
    live key, no JWT, no Resend key, and no phone number. Test doubles are
    labelled as test values.

12. **Doc style of added comments.** Zero semicolons. Zero em or en dashes.
    Longest added sentence is 16 words. Active voice throughout.

13. **Probe hygiene.** Three probe users were created and deleted through the
    admin API. All three readbacks are 404. `GET /auth/v1/admin/users` now
    returns zero users. `profiles`, `items`, and `telegram_link_tokens` each
    report `content-range: */0`.

## Findings

None. The round carries zero findings across all severities.

## Notes, not findings

- Lookup of an attached email user scans at most four pages of 50 admin users.
  Telegram-first users still match on the synthetic email inside `generateLink`.
  Email-first attach beyond that bound is unpinned. The live project had zero
  users before this probe, so the bound did not fire.
- The handler returns a session subset (`access_token`, `refresh_token`,
  `expires_in`, `token_type`, `user.id`, `user.email`). It omits `expires_at`.
  The minted JWT still authorised `/auth/v1/user` and owner RLS on `items`.
- An OPTIONS request from `https://evil.example` still sent Allow-Origin
  `https://orma.nryn.dev`. The browser would block it. That is not a `*` leak.
- The status line `claimed:orma-impl-52` was left untouched. This review
  changed no code, no config, and no status line.
