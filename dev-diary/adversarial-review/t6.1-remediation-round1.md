# Remediation: T6.1, round 1

- Role: Remediation. This agent did not implement or review T6.1.
- Date: 2026-09-14
- Review: `t6.1-round1.md`, verdict REMEDIATE, findings F1 to F5.
- Base: `9e9550d` on `main`. Nothing is committed. The verdict is unchanged.

## Changed files

- `web/src/lib/today/model.ts`
- `web/src/lib/today/load.ts`
- `web/src/lib/today/check.ts`
- `web/src/routes/app/+page.svelte`
- `web/src/lib/telegram-link.ts` (the API URL only)
- `scripts/p6-local-stack.sh`
- `dev-diary/adversarial-review/t6.1-handoff.md`

Every path sits on the T6.1 owns line, including the row added after round 1.

## Findings

| ID | Severity | Fix | Pin now | Mutation check |
|---|---|---|---|---|
| F1 | H | `lastCallSummary` in `model.ts` takes the run's `results.structured`. A Retired line needs a `retired_items` entry for that item and offset, plus a mention on the run at the same key. `callRetirementKeys` reads the entries. Ingest writes the entry, the retirement and the mention in one transaction. A later web, MCP or Telegram retire writes none of them. `load.ts` reads `results.structured` for the last call under RLS. The `retired_at` rule is gone. | The harness seed run has 3 mentions, 1 commitment and an empty `retired_items`. SQL retired `Renew the passport` with `retired_at = now()`, as a web retire does. /app showed `Answered · 3 mentions`, one Committed line at 0:08 and no Retired line. For the positive case, SQL added a `retired_items` entry for `Call Amma` at offset 4, where a mention already sat. /app then showed `RETIRED Call Amma 0:04` and still no passport line. That matches SQL: two retired items are mentioned on the run, and only Call Amma's retirement is recorded there. New check cases cover an item retired a day after the call, a run with no result row, and an entry whose offset has no mention. | Restoring `item.status === "retired" && retired_at >= runStart` on a copy failed with `a later web retire is not a retirement on this call: expected 1, saw 2`. |
| F2 | M | `callBlock` in `model.ts` mirrors `dispatchOne` in `calle.ts:337-360`, in the same order. First it wants a confirmed number (`phone_e164` and `phone_confirmed_at`). Then a live `outbound_calls` consent. For a materialised run, the run's `slot_id` must name an existing slot. `load.ts` reads the profile phone columns, the `outbound_calls` consents and slot ids. Both the first-run card and the Next call card show the reason with an Open Settings link instead of a time. The page never displays the number. | SQL revoked firstrun's only consent, which left 0 live `outbound_calls` rows. /app showed `YOUR FIRST CALL`, `Calls are off`, the Settings sentence and `Open Settings`, with no call time. Before remediation the reviewer saw `Your first call Today, 08:00`. The seed account, which has a live consent and a slotted run, still showed `Tomorrow, 08:00`. Eleven check cases cover it. Two callable cases show a time, for a run and for a slot fallback. Three consent cases block: no row, a revoked row and another kind. Three phone cases block: unconfirmed, missing and no profile. Two slot cases block: no slot and a vanished slot. One asserts the text points to Settings. | Making `callBlock` return null on a copy failed with `no consent row blocks: expected consent, saw undefined`. |
| F3 | M | `check.ts` gains a case for each surviving mutant. A canceled run completing after a real call. A commitment mention on a call-captured item. A next call at 20:00 UTC, which is tomorrow in Kolkata. Age with now at 20:00 UTC on 13 September, for both `since_date` and `created_at`. An added label for an item created at 20:00 UTC. A 06:30 New York slot on the spring-forward day, 8 March 2026. | `npm run check` prints `today check passed`. The unmutated scratch copy also passes. | Each mutant ran on a scratchpad copy of `src/lib/today` and `src/lib/ui`, never in the repo. Canceled run as last call failed: `expected done, saw late-canceled`. Commitment mention not skipped failed: `expected 1, saw 2`. `daysFromNow` in UTC failed: `expected Tomorrow, 01:30, saw Today, 01:30`. Now read in UTC for age failed: `expected 1, saw 0`. `addedLabel` in UTC failed: `expected added on Telegram today, saw added on Telegram yesterday`. One pass in `zonedTimeToInstant` failed: `expected 2026-03-08T10:30:00.000Z, saw 2026-03-08T11:30:00.000Z`. `git status` lists only the intended files. |
| F4 | M | In `telegram-link.ts`, `ORMA_API_URL` reads `getOrmaApiUrl()` from `$lib/supabase` whenever Vite provides `import.meta.env`. Settings passes that constant as `apiUrl`, so this one line moves every Settings read, mint and unlink. The node header check runs without Vite and cannot resolve `$lib`. Only there does the production URL stand, behind its fetch double. Nothing else in the file changed. | `npm run check` prints `telegram-link header check passed`. Signed in as live@example.com against the harness, `/app/settings` listed two `http://127.0.0.1:58321/rest/v1/profiles` resources, 0 resources on `orma-api.nryn.dev`, and no `profiles telegram read failed`. `npm run build` exits 0 with the top-level await in place. | With the hardcoded production URL restored, Settings goes back to sending its reads to `orma-api.nryn.dev`, as evidence 18 recorded. I did not rerun that revert against the harness. |
| F5 | L | `model.ts` adds `briefingOrder` and `todaysCallView`. They read the `- text (id: uuid)` lines that `assemble_briefing` writes into `open_items`, keep that order, and take counts and ages from rows. With no briefing, or no `open_items` text, the view falls back to the open list and flags it. In the live state, the page shows `On today's call` and `Orma is leading with these, in this order.`. The fallback shows `Open · N` and says the order is still to come. `load.ts` reads `briefing` for the live run only. The first step reads `Picked up`. The harness sets the live run's briefing from `assemble_briefing`. | SQL set the live briefing to list the tax form (0 mentions, 3 days) before the boiler (2 mentions, 12 days). /app showed `ON TODAY'S CALL`, the design sentence, then `Send the tax form not mentioned yet · 3 days` and `Book the boiler service 2 mentions · 12 days`. That order differs from the mention sort. After SQL set `briefing` to null, the page showed `OPEN · 2`, the fallback sentence, and the boiler first. The steps read `Picked up`, `Call placed`, then the poll clock. | Ignoring the briefing on a copy failed with `a briefing drives the list: expected true, saw false`. Restoring the old step copy failed with `first step copy follows TodayLive: expected Picked up, saw Picked up by the scheduler`. |

## Verification

- `npm run check` in `web/` passed all five node checks. svelte-check reported 307 files, 0 errors and 0 warnings.
- `npm run build` exited 0 and wrote `_worker.js` and `.assetsignore`.
- `scripts/p6-local-stack.sh up` applied every migration, the seed and the harness rows, including the live briefing. It printed no key.
- The browser zone was Asia/Calcutta. Page text came from the dev server on port 5199 with password sign-in, as the handoff describes.
- A separate node pin signed in by password grant and made the same RLS reads. It gave the same states: consent block for firstrun, and briefing order for live.
- `scripts/p6-local-stack.sh down` left 0 `orma-p6` containers, 0 volumes and no `/tmp/orma-p6-stack`. The 13 other containers stayed up. The dev server was stopped, and no `vite dev` process remains.

## Notes

- A retired item that a later restore reopens still reads as Retired on its call. The call really did retire it, and the result row proves it.
- The Captured rule still uses `source = call` and `created_at` after the run started. No finding named it, so it is unchanged.
