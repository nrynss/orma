# T10.3 remediation round 1

This remediation addresses H1 from `t10.3-round1.md`. It does not change the
verdict. T10.3 remains `claimed:gpt-5.6-sol` pending a fresh review.

| Finding | Remediation | Pin | Mutation covered |
| --- | --- | --- | --- |
| H1 | `PhoneConfirm` no longer renders a form. Its verifier is a `type="button"`, and Enter prevents default before invoking the same verifier. The host onboarding and Settings forms cannot receive a confirmation submit event. | `node --experimental-strip-types --disable-warning=ExperimentalWarning src/lib/phone-confirm/check.ts` confirms exactly one `verify_phone_code` RPC, no confirmation form or submit control, and both host form mounts. A form-free verifier cannot submit the host forms, so it causes zero profile or slot writes. `npm run check` and `git diff --check` pass. | Restoring an inner form or submit control fails the boundary check. A confirmation submission can then reach `startCalls` or `saveProfile` and create an unintended host write. |
