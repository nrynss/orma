# T6.4 round-1 review: Patterns — REMEDIATE

Reviewer measured the database, PostgREST as the browser would read it, the dev
server on port 5200, and the model helpers directly. One finding: the harness
edit silently deletes another task's fixture line.

## Pins (all measured 2026-09-14, fixture stack as found)

- Report row: 1 `pattern_reports` row for the seed account
  (`00000000-0000-4000-8000-000000000001`), period 2026-09-07 to 2026-09-13.
- Facts keys (14): ages, mentions, period_end, period_days, retirements,
  period_start, answered_runs, observed_days, required_days, completed_runs,
  longest_surviving, mood_distribution, sufficient_history, answer_rate_by_slot.
- Facts scalars: `period_days` 7, `completed_runs` 4, `answered_runs` 4,
  `mood_distribution` [{energised 1}, {low 1}, {ok 1}, {stressed 1}],
  `sufficient_history` true, `required_days` 3, `observed_days` 4.
- Prose: "Across 7 days you answered 4 calls. The calls heard you as
  energised 1 time, ok 1 time, low 1 time and stressed 1 time."
- In-period runs: 4 completed rows with `local_date` inside 2026-09-07 to
  2026-09-13, ordered by `scheduled_for`: stressed 09-10, low 09-11,
  ok 09-12, energised 09-13, all `answered_extracted`. Two further seed rows
  sit outside the window (canceled 09-14, scheduled 09-15).
- PostgREST as local@example.com: latest-report read returns 1 row with all
  14 facts keys; runs read returns 6 rows in `scheduled_for` order with the
  same 4 moods first; pattern deliveries read returns 1 row
  (telegram, `call_run_id` null, `sent_at` 2026-09-13T05:57:30Z, about 1 day
  old), so the sent-line predicate (sent, null or in-period run) holds.
- PostgREST as firstrun@example.com: 0 reports, 0 runs, and the seed report
  is invisible (`[]`), so RLS isolation holds and the loader takes the
  not-yet branch (`report` null). Not-yet counts derive from rows:
  `completedSoFar([])` is 0 and `requiredDays` with no facts falls back to 3.
- Model helpers run against DB-shaped rows: `trendRuns` over the 6 loader
  rows returns exactly the 4 in-period ids in chronological order;
  `moodLane` maps the 4 moods to lanes 0 to 3 and unknown or null to hollow;
  `periodLabel` gives "7 to 13 September".
- Dev server port 5200: `/` returns 200; `/app/patterns` signed-out returns
  303 to `/login`, identical to `/app`, `/app/items`, and `/app/settings`.
  No signed-out shell leaks. (No 200 page body is served signed-out by
  design. The signed-in render was verified by code path, not pixels.)
- Harness diff: the only `scripts/p6-local-stack.sh` change is one hunk
  inside `insert_harness_rows` (27 additions, 5 deletions), all dates
  `now()`-relative, all inserts under fixed harness UUIDs, facts via the
  read-only `compute_pattern_facts`. No production side effects.
- Scope: `web/src/routes/app/patterns/` is new and untracked as expected.
  The other working-tree modifications (phase-doc status lines, landing
  `+page.svelte`) belong to sibling tasks, not this one.

## No-hallucination check (T7.2 rule)

Every integer in the prose appears in `facts`. 7 is `period_days`. 4 is
`completed_runs` and `answered_runs`. Each 1 is one `mood_distribution`
entry (energised 1, low 1, ok 1, stressed 1). All four mood names in the
prose are present in the distribution. Counts stated, zero unpinned numbers.

## Findings: 0 C, 0 H, 1 M, 0 L

| # | Where | What | Pin | Mutation |
|---|-------|------|-----|----------|
| 1 | `scripts/p6-local-stack.sh`, `insert_harness_rows` (M) | The edit deletes the pre-existing `update public.call_runs set briefing = public.assemble_briefing(...)` line for the live run. That line is outside the owned "fixture pattern_reports row only" scope, and T6.1 Today reads `call_runs.briefing` for the live run (`web/src/lib/today/load.ts` `briefingRes`) to order "On today's call". The live run now has `briefing` null (pinned), so the briefing-order branch can no longer be exercised from the fixture and Today silently falls back to the open list. | `briefing is not null` is false for run `4f00...0000c1`; `git diff HEAD` shows the 3-line update removed in the same hunk as the T6.4 additions; `grep briefing web/src/lib/today/load.ts` shows the consumer. | Restoring the fix means re-adding the update line. Reverting the fix (keeping the deletion) leaves the live-run briefing null and T6.1 demoing its fallback instead of briefing order. |

## Done-when assessment

- "A fixture report renders with its facts visible": PASS. One report row
  with 14 facts keys reads as the owner over PostgREST, and the view renders
  the prose as plain paragraphs plus every facts key beside its value under
  the "Computed by SQL" heading.
- "The mood trend draws from real rows": PASS. The 4 in-period completed
  rows are exactly the trend input in chronological order, with the seed
  energised run last by `scheduled_for`; out-of-period rows are excluded
  and the loader sort plus `trendRuns` filter were both executed, not read.
- "A user with too little history sees an honest not-yet state": PASS.
  firstrun@example.com has 0 reports and 0 rows, hits the not-yet branch,
  and sees row-counted "0 calls" with the 3-day rule and no invented trend.

## Verdict

REMEDIATE. One M finding, no other findings. The page itself measures
clean against every clause. The required fix is to restore the deleted
briefing update line in `insert_harness_rows` so the fixture keeps the
live-run briefing T6.1 depends on.
