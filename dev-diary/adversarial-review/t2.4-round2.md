# T2.4 review, round 2

**Verdict:** REMEDIATE

**Counts:** C 1, H 0, M 0, L 0

## Findings

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| C | `supabase/migrations/20260912161000_tick.sql:1-35`, `dev-diary/PHASE-2-call-engine.md:84` | C1's handler wiring now works when invoked, but no `pg_cron` job invokes that handler every minute. T2.3 deliberately deferred `cron.schedule` until T2.4 supplied dispatch. This remediation imports the dispatcher but does not add the deferred scheduler migration to T2.4's ownership. A scheduled row will therefore never reach the configured handler or CALL-E without an out-of-band invocation. | Apply the current migrations, then query `cron.job` for the tick job. It returns no row. Insert a due scheduled run and wait one minute. Its state remains `scheduled`, with no `POST /v1/calls` and no `claimed` or `dispatched` `call_events` rows. | Removing the scheduler leaves a passing direct-handler test while production calls remain permanently scheduled. |

## Zero-residue check of round-one findings

- **C1, handler wiring:** resolved at `supabase/functions/tick/index.ts:60-68` and `141-148`. The configured-tick pin reaches `rpc/claim_due_call_runs`, writes `claimed`, and posts to CALL-E.
- **C2, canonical webhook configuration:** resolved at `supabase/functions/_shared/calle.ts:121-135`. `webhook_url` derives solely from `ORMA_API_URL` and `ORMA_WEBHOOK_SECRET`.
- **H1, fixture and complete request coverage:** resolved at `supabase/functions/_shared/calle.ts:380-412`. The test loads `call-completed.json`, pins its digest, and pins the complete serialized outbound body.
- **T2.9 transition integration within this seam:** resolved for the transitions this task owns. `claimed` is appended before dispatch and a successful provider persistence appends `dispatched` with the CALL-E id and durable state.

## Independent checks

`deno test --allow-env --allow-net --allow-read supabase/functions/_shared/calle.ts supabase/functions/tick/index.ts supabase/functions/_shared/events.ts` passed 29 checks.

`git diff --check` passed.

The direct-handler checks do not substitute for the missing `pg_cron` activation.
