# T8.1 handoff: Seed data and disclosure

Task completed and approved. Four review rounds ran to an APPROVE verdict with zero findings.

## What exists now

`scripts/seed.ts` provides the seed script for Orma demo accounts. It populates history that real calls never generated.

1. **Seed data**:
   - Primary item: "the dentist" (`seeded = true`, `source = 'call'`, `since_date` 34 days before demo date).
   - Extra items: "renew the passport" (21 days) and "weekend groceries" (captured on run 3).
   - Three completed dry-run call runs on separate local dates (demo-6, demo-4, demo-3).
   - Each run holds a stable idempotency key under `orma:seed:`, a synthetic transcript, and structured results with commitments and captures.
   - Structured result entries match ingested and finalised mention counts exactly (1 in run 1, 2 in run 2, 2 in run 3).
   - Stored briefing lead lines strictly match rows before dispatch ("Everything open is still current.", "It's been 30 days.", "It's been 31 days.").
   - Dispatched events carry the full reviewed `CALL_TASK_TEMPLATE` with masked phone and dynamic `ORMA_API_URL` webhook.
   - Weekend groceries `created_at` matches run 3 `completed_at`.

2. **CLI flags**:
   - `--email <address>`: targets a specific profile, paginating user lookups across multiple pages.
   - `--demo-date <YYYY-MM-DD>`: custom demo date, defaults to today in the profile's timezone.
   - `--apply`: writes rows. Refuses non-local URLs without `--i-confirmed-dry-run`.
   - `--remove`: idempotent rollback. Deletes only seeded rows and preserves pre-existing items.

3. **Disclosure paragraph**:
   - Prints with every plan in clear active voice.
   - Names all seeded items, runs, dates, and the dry-run nature of the data.

## Validation

- `deno lint scripts/seed.ts` exits 0 with zero warnings or errors.
- Plan run writes zero rows across all tables.
- Reruns with `--apply` are completely idempotent.
- An item retirement on the web survives reruns with all mentions intact.
- `assemble_briefing` returns "You've mentioned the dentist 3 times. It's been 34 days."
- `compute_pattern_facts` reports `observed_days = 3` and `sufficient_history = true`.
- `dateTimeInTz` produces identical UTC instants across all host machine timezones.
- `--remove` deletes all seeded rows and preserves baseline local data.
- Round 4 review approved with zero findings (C=0, H=0, M=0, L=0).
