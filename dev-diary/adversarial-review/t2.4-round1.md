# T2.4 review, round 1

**Verdict:** REMEDIATE

**Counts:** C 2, H 1, M 0, L 0

## Findings

| Severity | Where | What | Pin | Mutation |
| --- | --- | --- | --- | --- |
| C | `supabase/functions/tick/index.ts:45-65, 135-139` | The deployed tick still sets `dispatchClaimed` to `undefined` and imports no T2.4 dispatcher. It therefore deliberately skips `claim_due_call_runs`, so `dispatchClaimedRun` is unreachable in production and no scheduled run can reach CALL-E. The T2.4 owns line names only `calle.ts`, which prevents the needed wiring. Record a contract change before expanding that seam. | Start `createAuthenticatedTickHandler(depsFromEnv())` with a due fixture. The response reports `claimed: 0`, no request reaches `rpc/claim_due_call_runs`, and no `POST /v1/calls` occurs. | Reverting the future tick wiring leaves the client present but every due call permanently scheduled. |
| C | `supabase/functions/_shared/calle.ts:91-104` | Runtime requires `ORMA_WEBHOOK_URL`, but this is not a defined Orma runtime name. The canonical runtime list and bootstrap script define `ORMA_API_URL` and `ORMA_WEBHOOK_SECRET` instead. Consequently `depsFromEnv()` throws `missing ORMA_WEBHOOK_URL` in every configured deployment. The webhook URL must be derived from those canonical values, or the configuration contract must be changed deliberately. | Call `depsFromEnv` with every documented runtime and CALL-E variable, including `ORMA_API_URL` and `ORMA_WEBHOOK_SECRET`. It throws before a dispatcher can start. | Removing a local-only `ORMA_WEBHOOK_URL` workaround makes live dispatch fail closed at startup. |
| H | `supabase/functions/_shared/calle.ts:285-329` | The tests replace the recorded CALL-E response with a hand-written partial object and never load `testdata/calle/call-completed.json`. They prove only `id` and `status` parsing. They do not pin the committed provider response shape that T2.4 is required to build against, nor do they assert the full outbound contract of `task`, inline `result_schema`, and `webhook_url`. | Mutate a field in `testdata/calle/call-completed.json`. `deno test --allow-env --allow-net supabase/functions/_shared/calle.ts` still passes because the test does not read the fixture. | A provider-shape or inline request-contract regression can pass T2.4 tests and first appear during a real phone call. |

## Independent checks

`deno test --allow-env --allow-net supabase/functions/_shared/calle.ts` passed 6 checks. The passing suite does not change the two deployment blockers or establish fixture coverage.

`git diff --check` passed.
