# T1.6 remediation round 1

| Finding | Change | Pin | Outcome |
| --- | --- | --- | --- |
| H1 | `supabase/seed.sql` now uses `answered_extracted`, parser-valid commitment `item_id`, `slot_change_requested`, and `energised` mood. The completed event records the same disposition. | An isolated Supabase stack on ports 55520-55529 reset from empty migrations. Its stored completed run had `state=completed`, `disposition=answered_extracted`, `mood=energised`, and `results.valid=true`. `deno run` read the stored `results.structured` through Docker and passed it to `parseStructuredResult`. | The parser returned a non-null typed result with empty captured and retired lists, one commitment, `slot_change_requested=no`, `mood=energised`, and `reason=null`. |
