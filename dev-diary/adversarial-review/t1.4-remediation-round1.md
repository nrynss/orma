# T1.4 round 1 remediation

| Finding | Remediation | Independent validation |
| --- | --- | --- |
| H1 | Added `supabase/functions/_shared/fixtures.ts`. `loadCallFixtures` loads all four committed files into exported TypeScript fixture types. | `deno check supabase/functions/_shared/fixtures.ts` checks the loader without a network request. |
| M1 | Added the terminal and attempt-level failed-call vocabulary to `docs/calle-call.md`. | `jq` reads the three values from `testdata/calle/call-failed.json`, and `rg` finds each value in the document. |
| L1 | Reworded the opening of `t1.4-contract-change.md` to record the resolved ownership extension. | Reading the T1.4 `owns` line and the revised opening confirms the same three owned paths. |
