# T2.7 remediation, round 4

Remediator: T27Remediate4. This round answers `t2.7-round4.md`, verdict REMEDIATE, C 0, H 0, M 0, L 3.
I did not implement, review or remediate T2.7 in an earlier round. The verdict is unchanged. All three
findings are fixed. I record no finding as a false positive, and I fixed nothing nobody found.

Owned paths touched:

* `supabase/functions/_shared/ingest.ts`
* `supabase/migrations/test-ingest.sh`

`supabase/migrations/20260912180000_ingest.sql` is unchanged this round. Both script findings are
pins, and their fixes live in the script. The RPC body, its signature and its ACL are exactly the ones
round 4 reviewed, byte for byte.

File hashes at the end of this round:

```
9d76e4f6d7cfa09bbabb638efb59aace  supabase/functions/_shared/ingest.ts
be80104cbf768ec69983d1f7304ffd3f  supabase/migrations/20260912180000_ingest.sql
7bd5e24010b1b12da36243b48851583d  supabase/migrations/test-ingest.sh
```

The first and third hashes differ from the round-4 block. The second is identical, which is the
strongest available statement that no prior SQL fix moved.

## Acceptance outputs, observed

```
$ deno check supabase/functions/_shared/ingest.ts
$ deno check supabase/functions/_shared/calle.ts
$ deno check supabase/functions/_shared/dispatch-mode.ts
$ deno check supabase/functions/_shared/poll.ts
  (each one exit 0)
$ deno test --allow-env --allow-read --allow-net supabase/functions/_shared/ingest.ts
ok | 10 passed | 0 failed (21ms)
$ deno test --allow-env --allow-read --allow-net supabase/functions/_shared/calle.ts
ok | 31 passed | 0 failed (30ms)
$ deno test --allow-env --allow-read --allow-net supabase/functions/_shared/dispatch-mode.ts
ok | 8 passed | 0 failed (3ms)
$ bash supabase/migrations/test-ingest.sh                          EXIT=0
T2.7 a busy port shifted the port band.
T2.7 completed fixture produced its rows.
T2.7 re-ingest changed no row.
T2.7 invalid fixture left a transcript and a disposition.
T2.7 unoffset retirement was rejected.
T2.7 concurrent ingest waited on the run lock.
T2.7 every boundary value ingest.ts accepts committed in PostgreSQL.
T2.7 all 18 values ingest.ts rejects raise in PostgreSQL.
T2.7 unmodified completed fixture rows: run_items=1 user_items=5 mentions=1 offsets=44 commitments=0 retired=1
T2.7 a call that recovered after a give-up cleared the give-up marker.
T2.7 the ingested row carried the repair list beside the counts of the rows.
T2.7 a failed call kept CALL-E's failure: poll 'failed not_answered null call_failed calling task status=NO ANSWER (Hangup by: bot) true' keep 'failed not_answered null busy CALL-E reported a busy line true' webhook 'failed not_answered null call_failed calling task status=NO ANSWER (Hangup by: bot) true'.
T2.7 a replay reported the stored proposal: summary 'yes' stored 'yes true 1'.
T2.7 ingestion acceptance passed.
```

The Deno suite keeps ten tests and the phone table inside them grew from eight spellings to nineteen.
The script keeps all eighteen rejection values and both boundary tables. The case-eleven line now
carries a third reading, the keep row, between the poll row and the webhook row.

## Findings

| Finding | Remediation | Pin | Mutation covered |
| --- | --- | --- | --- |
| **L. The `PHONE_SCAN` class was enumerated, and nine of thirty five spellings leaked.** En dash, em dash, zero width space, soft hyphen, apostrophe, backslash, colon plus space, middle dot and fullwidth digits left the number whole. The comment claimed "any separator a person writes between them". | The class now joins digit groups across anything that is not a letter or a digit. `PHONE_SCAN` is `/[+\uFF0B]?\p{Nd}(?:[^\p{L}\p{N}\ue000]*\p{Nd}){6,}/gu`. A separator is any run that is neither a Unicode letter nor a Unicode digit, so all nine spellings now join. `\p{Nd}` replaces `\d`, so fullwidth digits count. The private-use sentinel that carries a lifted uuid or timestamp is excluded from the separator class, so a lifted value cannot join a run beside it. The comment now states what the pattern matches and what it does not, including the count-like shapes it over-matches on purpose. | The module test `an RPC failure names its cause without a phone number or a key`, whose table grew from eight spellings to nineteen, and the live stack probe below over twelve spellings. | Restoring the enumerated class in `/tmp/t27r5-revert/f1` fails the test with `###\u2013###\u2013#### was not masked`, then `FAILED \| 9 passed \| 1 failed`, EXIT=1. The same revert leaks eleven spellings through the real module, measured twice, once with a local response and once against real PostgREST. |
| **L. Case eleven's poll half could not fail for the invariant it named.** It seeded the run with the fixture's own failure code, so the payload branch alone satisfied it and the `else v_run.calle_failure` keep branch was unmeasured. | `failed_run` now stores `no_answer` while its payload names the fixture's `call_failed`, so the payload branch must supersede a different stored code. A new `kept_run` stores `busy` and ingests a payload that names no failure at all, so only the final `else` can write that value. The case comment now names which run measures which branch, and the printed line carries all three readings. | Case eleven of `test-ingest.sh`, the new `kept_row` assertion. | `else v_run.calle_failure` back to `else null` in `/tmp/t27r5-revert/f2` fails the whole script at the kept row with `the run whose payload repeated no CALL-E failure read 'failed not_answered null null null true'`, EXIT=1. Under the pre-fix case the same revert left the script green. |
| **L. Two `reject_cases` entries claimed ownership and named an id that exists for nobody.** Both the retirement update and the commitment existence check raised on the missing row, so removing either ownership predicate changed nothing. | A second user and one item of theirs are seeded, and the last two entries name that item. The retirement update must now refuse a row that exists for another user, and the commitment check must refuse the same. The other sixteen entries were audited at the same time. Each one raises for the reason it names, which is a uuid cast, a timestamp cast, an integer range, a blank text or a jsonb escape. | Case seven of `test-ingest.sh`, the last two entries of `reject_cases`. | Dropping `and user_id = p_user_id` from the retirement update in `/tmp/t27r5-revert/f3a` fails the whole script with `PostgreSQL accepted a result that ingest.ts rejects: {"captured_items":[],"retired_items":[{"item_id":"00000000-0000-0000-0000-0000000000b8","evidence_offset_seconds":62}]}`, EXIT=1. Dropping it from the commitment `exists` in `/tmp/t27r5-revert/f3b` fails with the same line for the commitment entry, EXIT=1. |

## The scrub, measured live

Two probes drive the real `ingestTerminalRun`. Neither prints a digit of the number, because each one
replaces every digit with `#` before it writes a line.

* `/tmp/t27r5/scrub-probe.ts` runs the module against a local response in the PostgREST shape, so the
  table can cover 44 spellings. The body is `{"code":"22007","message":"invalid input syntax for type
  uuid: <spelling> for call run <uuid>", ...}`, which is the shape the reviewer's probe took from the
  wire.
* `/tmp/t27r5/stack-probe.ts` runs the same module against real PostgREST at
  `ORMA_API_URL=http://127.0.0.1:57031`. The run id is a spare uuid followed by the spelling, so
  PostgreSQL echoes both in its own `invalid input syntax for type uuid` body. The uuid direction and
  the mask direction are measured in one request. The cast fails before the RPC runs, so the probe
  writes no row. SQL then read zero runs, zero events and zero profiles for its two ids.

The number is the synthetic one the shipped test suite uses, rendered here with its digits masked.

### The local probe, 44 spellings, shipped module

| Shape | Input (digits masked) | Shipped module | Enumerated class |
| --- | --- | --- | --- |
| plain | `##########` | `[masked number]` | `[masked number]` |
| space | `### ### ####` | `[masked number]` | `[masked number]` |
| hyphen | `###-###-####` | `[masked number]` | `[masked number]` |
| dot | `###.###.####` | `[masked number]` | `[masked number]` |
| slash | `###/###/####` | `[masked number]` | `[masked number]` |
| comma | `###,###,####` | `[masked number]` | `[masked number]` |
| comma space | `###, ###, ####` | `[masked number]` | `[masked number]` |
| underscore | `###_###_####` | `[masked number]` | `[masked number]` |
| parenthesised | `(###) ###-####` | `([masked number]` | `([masked number]` |
| plus | `+###########` | `[masked number]` | `[masked number]` |
| plus space | `+# ### ### ####` | `[masked number]` | `[masked number]` |
| plus hyphen | `+#-###-###-####` | `[masked number]` | `[masked number]` |
| non breaking space | `###\u00a0###\u00a0####` | `[masked number]` | `[masked number]` |
| narrow no break space | `###\u202f###\u202f####` | `[masked number]` | `[masked number]` |
| tab | `###\t###\t####` | `[masked number]` | `[masked number]` |
| newline | `###\n###\n####` | `[masked number]` | `[masked number]` |
| digit space | `#### ## ####` | `[masked number]` | `[masked number]` |
| dot space | `###. ###. ####` | `[masked number]` | `[masked number]` |
| comma hyphen | `###,-###,-####` | `[masked number]` | `[masked number]` |
| extension | `### ### #### ext ##` | `[masked number] ext ##` | `[masked number] ext ##` |
| trailing space | `### ### #### ` | `[masked number] ` | `[masked number] ` |
| tel prefix | `tel:+###########` | `tel:[masked number]` | `tel:[masked number]` |
| lone closing paren | `###) ###-####` | `[masked number]` | `[masked number]` |
| en dash U+2013 | `###\u2013###\u2013####` | `[masked number]` | number whole |
| em dash U+2014 | `###\u2014###\u2014####` | `[masked number]` | number whole |
| zero width space U+200b | `###\u200b###\u200b####` | `[masked number]` | number whole |
| soft hyphen U+00ad | `###\u00ad###\u00ad####` | `[masked number]` | number whole |
| apostrophe U+2019 | `###\u2019###\u2019####` | `[masked number]` | number whole |
| ascii apostrophe | `###'###'####` | `[masked number]` | number whole |
| backslash | `###\\###\\####` | `[masked number]` | number whole |
| colon space | `###: ###: ####` | `[masked number]` | number whole |
| middle dot U+00b7 | `###\u00b7###\u00b7####` | `[masked number]` | number whole |
| fullwidth digits | `\uff14\uff11\uff15\uff15\uff15\uff15\uff10\uff11\uff12\uff17` | `[masked number]` | number whole |
| fullwidth parens | `\uff08\uff14\uff11\uff15\uff09\uff15\uff15\uff15\uff10\uff11\uff12\uff17` | `[masked number]` | number whole |
| uuid beside | `<uuid> ##########` | `<uuid> [masked number]` | `<uuid> [masked number]` |
| timestamp beside | `####-##-## ##########` | `####-##-## [masked number]` | `####-##-## [masked number]` |
| benign small int | `value # is out of range for type integer` | untouched | untouched |
| benign lettered | `item # of # mentions # times` | untouched | untouched |
| benign version | `deno #.#.#` | untouched | untouched |
| long integer | `value ########## is out of range` | `[masked number]` | `[masked number]` |
| comma digit list | `scores #, #, #, #, #, #, # today` | `[masked number]` | `[masked number]` |
| compact stamp | `id ############## stored` | `[masked number]` | `[masked number]` |
| superscript break | `x\u00b2 ### ### ####` | `x\u00b2 [masked number]` | `x\u00b2 [masked number]` |
| roman break | `\u216b ### ### ####` | `\u216b [masked number]` | `\u216b [masked number]` |

44 rows, 41 masked and three left alone. The three untouched rows hold no phone number, and the last
five rows are the deliberate over-match the comment now states. Under the shipped module no phone
spelling leaks and the uuid and the timestamp keep their digits in every row.

### The stack probe, real PostgREST, shipped module

```
hyphen             | masked=yes | uuid=kept | out=... type uuid: "<uuid> [masked number]"
comma              | masked=yes | uuid=kept | out=... type uuid: "<uuid> [masked number]"
en dash            | masked=yes | uuid=kept | out=... type uuid: "<uuid> [masked number]"
em dash            | masked=yes | uuid=kept | out=... type uuid: "<uuid> [masked number]"
zero width space   | masked=yes | uuid=kept | out=... type uuid: "<uuid> [masked number]"
soft hyphen        | masked=yes | uuid=kept | out=... type uuid: "<uuid> [masked number]"
apostrophe         | masked=yes | uuid=kept | out=... type uuid: "<uuid> [masked number]"
backslash          | masked=yes | uuid=kept | out=... type uuid: "<uuid> [masked number]"
colon space        | masked=yes | uuid=kept | out=... type uuid: "<uuid> [masked number]"
middle dot         | masked=yes | uuid=kept | out=... type uuid: "<uuid> [masked number]"
fullwidth digits   | masked=yes | uuid=kept | out=... type uuid: "<uuid> [masked number]"
benign version     | masked=no  | uuid=kept | out=... type uuid: "<uuid> deno #.#.#"
```

The same probe against the enumerated class reads `masked=no` for all eleven phone rows, and its
output at that point is the number whole beside the surviving uuid. That is the round-4 finding,
reproduced through the real module and the real PostgREST.

## Reverted-fix runs

The base tree is `/tmp/t27r5-revert/base`, built from the final three owned files plus `fixtures.ts`,
`result.ts`, every migration, `seed.sql` and `testdata/`. Each revert tree is a copy of it, and a diff
between the base and each copy holds one construct and nothing else. The two script reverts ran the
whole `test-ingest.sh` in their copy, which starts its own throwaway Supabase project.

| The fix reverted | Pin that failed | Observed |
| --- | --- | --- |
| the general `PHONE_SCAN` class back to the enumerated one, `/tmp/t27r5-revert/f1` | the module scrub test | `error: Error: ###\u2013###\u2013#### was not masked: ...`, then `FAILED \| 9 passed \| 1 failed`, EXIT=1 |
| the same class, `/tmp/t27r5-revert/f1/scrub-probe.ts` | the live scrub table | 11 phone rows read `masked=no` and return the number whole, against 0 under the shipped module |
| the same class, `/tmp/t27r5-revert/f1/stack-probe.ts` against real PostgREST | the live scrub table | the same 11 rows read `masked=no`, and the uuid survives in every row |
| `else v_run.calle_failure` back to `else null`, `/tmp/t27r5-revert/f2` | case eleven, the kept row | `the run whose payload repeated no CALL-E failure read 'failed not_answered null null null true'`, EXIT=1 |
| `and user_id = p_user_id` dropped from the retirement update, `/tmp/t27r5-revert/f3a` | case seven, the retirement entry | `PostgreSQL accepted a result that ingest.ts rejects: {"captured_items":[],"retired_items":[{"item_id":"00000000-0000-0000-0000-0000000000b8","evidence_offset_seconds":62}]}`, EXIT=1 |
| `and user_id = p_user_id` dropped from the commitment `exists`, `/tmp/t27r5-revert/f3b` | case seven, the commitment entry | `PostgreSQL accepted a result that ingest.ts rejects: {"captured_items":[],"retired_items":[],"commitments":[{"item_id":"00000000-0000-0000-0000-0000000000b8","evidence_offset_seconds":80}]}`, EXIT=1 |

Every revert fails, every case before the failure stays green, and each failure names the measurement
that failed. The first three rows are the round-4 finding one revert. The fourth row is the mutation
round 4 recorded as passing, and it now fails. Rows five and six are the two ownership predicates the
finding named, each measured alone.

## Residue against rounds 1, 2 and 3

Zero. The migration file is byte-identical to the round-4 hash, so every SQL fix rounds 2, 3 and 4
reviewed is untouched, including the give-up clear, the keep branch and the payload branch. The script
exits 0 with all thirteen earlier lines green, so the completed fixture, the re-ingest, the invalid
fixture, the unoffset retirement, the run lock, both boundary tables, all eighteen rejection values,
the fixture counts, the give-up clear, the repair list, the payload failure and the stored proposal
all still hold.

The Deno suite exits 0 with ten tests, so the sanitisation, the repair table, the boundary dues, the
re-ingest case, the turn shapes and the key redaction all still hold. The module's exported surface is
unchanged, namely `TerminalCallSource`, `IngestDeps`, `IngestResult`, `depsFromEnv` and
`ingestTerminalRun`, so `calle.ts` and `dispatch-mode.ts` check and test green without a change.

The one behaviour change outside the three findings is the wider separator class. The comment now
states both directions, and the two count-like shapes it over-matches are the trade the round-4 review
already recorded as residual risk.

## Cleanup

The shared stack holds no row from this round. SQL read zero runs, zero events and zero profiles for
the two ids the stack probe used, and the probe never reached the RPC. The throwaway project of each
script run stopped itself, and no container named `orma-t2-7` remains. The scratch trees under
`/tmp/t27r5-revert` and the probes under `/tmp/t27r5` sit outside the repository. No real call was
placed, `CALLE_API_BASE` stayed the stub host, `tick` was never driven, and no secret or unmasked
phone number appears in this file.
