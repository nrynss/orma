# T2.7 remediation, round 5

Remediator: T27Remediate5. This round answers `t2.7-round5.md`, verdict REMEDIATE, C 0, H 0, M 0,
L 4. I did not implement, review or remediate T2.7 in an earlier round. The verdict is unchanged.
All four findings are fixed. I record no finding as a false positive, and I fixed nothing nobody
found.

Owned paths touched:

* `supabase/functions/_shared/ingest.ts`
* `supabase/migrations/test-ingest.sh`

`supabase/migrations/20260912180000_ingest.sql` is unchanged this round, byte for byte. Finding
three is a pin gap in the script, and the RPC already writes both values. Its body, signature and
ACL are exactly the ones round 5 reviewed.

File hashes at the end of this round:

```
13a4d456c61dde5da155704f0757684a  supabase/functions/_shared/ingest.ts
be80104cbf768ec69983d1f7304ffd3f  supabase/migrations/20260912180000_ingest.sql
69f4e138a8cb3b47f86acad2d955ab7c  supabase/migrations/test-ingest.sh
```

The second hash is identical to the round-5 block, which is the strongest available statement that
no prior SQL fix moved.

## Acceptance outputs, observed

```
$ deno check supabase/functions/_shared/ingest.ts                                     exit 0
$ for f in calle dispatch-mode poll; do deno check supabase/functions/_shared/$f.ts; done   exit 0
$ deno test --allow-env --allow-read --allow-net supabase/functions/_shared/ingest.ts
ok | 12 passed | 0 failed (26ms)
$ deno test --allow-env --allow-read --allow-net supabase/functions/_shared/calle.ts
ok | 33 passed | 0 failed (34ms)
$ deno test --allow-env --allow-read --allow-net supabase/functions/_shared/dispatch-mode.ts
ok | 8 passed | 0 failed (4ms)
$ bash supabase/migrations/test-ingest.sh                                             EXIT=0 in 27.62s
T2.7 a busy port shifted the port band.
T2.7 completed fixture produced its rows.
T2.7 re-ingest changed no row.
T2.7 invalid fixture left a transcript and a disposition.
T2.7 unoffset retirement was rejected.
T2.7 concurrent ingest waited on the run lock.
T2.7 every boundary value ingest.ts accepts committed in PostgreSQL.
T2.7 all 18 values ingest.ts rejects raise in PostgreSQL.
T2.7 unmodified completed fixture rows: run_items=1 user_items=5 mentions=1 offsets=44 commitments=0 retired=1
T2.7 a call that recovered after a give-up cleared the give-up marker.
T2.7 the ingested row carried the repair list beside the counts of the rows.
T2.7 a failed call kept CALL-E's failure: poll 'failed not_answered null call_failed calling task status=NO ANSWER (Hangup by: bot) true' keep 'failed not_answered null busy CALL-E reported a busy line true' webhook 'failed not_answered null call_failed calling task status=NO ANSWER (Hangup by: bot) true'.
T2.7 a replay reported the stored proposal: summary 'yes' stored 'yes true 1'.
T2.7 ingestion acceptance passed.
```

The Deno suite keeps twelve tests, and two of them are new. The scrub table inside it grew from
nineteen spellings to twenty seven. The `calle.ts` graph imports this module, so its count rose
from 31 to 33 with the same two tests. The script keeps every earlier case and every earlier
reading, and seven of its cases now read a named value beside their counts.

## Findings

| Finding | Remediation | Pin | Mutation covered |
| --- | --- | --- | --- |
| **L. The timestamp lift spared any match of a date shape without checking it is a date.** A hyphen grouped number lost its first eight digits to the sentinel, and the remainder never reached seven digits, so the number left the thrown cause whole. The comment claimed only the uuid and timestamp shapes were lifted. | I removed the lift instead of gating it, because a real date and a phone grouping are the same eight digits. The masker now finds every phone shaped run itself and keeps inside it only a value this module would store. A uuid always keeps its digits, because thirty two hex digits run longer than any phone. A timestamp keeps them only while every other fragment of its run holds seven or more digits, because a date is also the front of a phone. A date shaped run that is not a date masks whole, and so does a number whose first eight digits spell a real date. The comment states that rule, and both the suite and the live probe measure it. | The seven new rows of the Deno scrub test, namely `4155-12-0127`, `2026-09-1241`, `2026-09-12 41`, `+4155-12-0127`, `4155-12-01-27`, `4155-55-01-27`, and the two rows that keep a real date. The live probe's 84 rows cover the same rule through real PostgREST. | Reverting the masker to the shipped lift in `/tmp/t27r6/trees/revert-l1` fails the scrub test with `####-##-#### was not masked: ... type uuid: ####-##-#### for call run <uuid> ...`, and the same probe reads `leaks=16` on the pre-fix module against `leaks=0` on the shipped one. |
| **L. The parts were scrubbed alone and then joined, so digits from two fields re-formed one run in what the function returned.** | `failureCause` now joins the parts first and scrubs the join, because the join is the string the caller reads. Every run inside a part is also a run inside the join, so the per part pass is subsumed and gone. The status is ours, three digits that no response changes, so the module appends it after the masker has run. The comment says that in four sentences. | The new Deno test `a number split across two response fields still masks`, and the split probe's eight rows under both trees. | Restoring the per part scrub and join in `/tmp/t27r6/trees/revert-l2` fails that test with `a number split across two fields reached the cause: ... type uuid: ###-### ####" for call run <uuid>`, which is the reviewer's own string. The split probe reads four failures on the pre-fix module and none on the shipped one. |
| **L. Cases one, six, ten and twelve read counts only.** No case read a stored `due`, and none read `item_mentions.offset_seconds` for a retirement or a commitment row. | A `named_values` helper reads the stored due, the evidence offset on the commitment row, the mention offsets of the one retired item, and the commitment row count for a run. Case one asserts them. Case two asserts a replay changes none of them. Case ten asserts them on the repaired commitment. Case twelve now carries a commitment with a due, so the replay's stored values are measured and not just the proposal. Case six reads all nine boundary dues back beside the value PostgreSQL makes of the same literal. Case five reads the locked mention's offset, and case three reads the stored `structured is null` flag. The rest of the script was audited, and cases four, seven, eight, nine and eleven already read a value or a rollback. | Case one of `test-ingest.sh`, whose new line reads `due=2026-09-12T03:00:00Z retired_mentions=62,80 commitment_offset=80 commitments=1`. | Nulling the due after the cast in `/tmp/t27r6/trees/revert-l3-null-due` exits 1 with `the committed values read 'due=none retired_mentions=62,80 commitment_offset=80 commitments=1'`. Zeroing the retirement and commitment mention inserts in `/tmp/t27r6/trees/revert-l3-zero-offsets` exits 1 with `... retired_mentions=0,0 ...`. Each mutation leaves the round-5 gate at `EXIT=0` with `T2.7 ingestion acceptance passed.` |
| **L. The comment claimed more and less than the code did.** It named a long integer and a comma joined list of single digits as the only over-matches. A version, a clock time with a fraction, decimal money and a plain fraction also mask, and a date the lift did not recognise was swallowed with the number beside it. | I wrote the truth rather than narrowing the masker, because narrowing reopens the leak. A version `1.2.3.4.5.6.7` cannot be told from a dot joined number, and money cannot be told from a three group number, so any narrowing rule leaks. The comment now names every shape the masker eats, in the order the code reads them. They are an integer of seven digits or more, a version, a clock time with a fraction, decimal money, a plain fraction, a comma joined list of single digits, and a date shaped run this module does not store as a timestamp. It also states the other direction, which is that every other digit of a run masks one fragment at a time. | The new Deno test `every shape the masker over-matches masks, and a stored value keeps its digits`, which measures nine over-mask shapes and two keep directions. The live probe measures twelve over-mask rows and six rows that keep a value. | Reverting the masker the comment documents fails that test with `an over-matched shape kept its digits: ... due ####-##-## and nothing else`. The restored sentence is also false for four of the twelve rows the probe measures, namely `deno #.#.#.#.#.#.#`, `at ##:##:##.######`, `paid #,###,###.##` and `pi is #.##########`. |

## The scrub, measured live

`/tmp/t27r6/probe-live.ts` drives the real `ingestTerminalRun` against
`ORMA_API_URL=http://127.0.0.1:57031`. The run id is a uuid with no digit in it, a space, then one
candidate spelling, so the PostgREST cast error echoes that spelling and nothing else. The thrown
cause is the measurement. The probe replaces every digit with `#` before it writes a line, and
`PROBE_FORMAT=markdown` emits the table below.

Three trees were measured with the same 84 rows:

| Tree | Leaks | Rows that lost benign digits |
| --- | --- | --- |
| the shipped module, `/home/nryn/work/orma/.../ingest.ts` | 0 | 12, every one documented |
| the round-5 module, `/tmp/orma-review-t27-r5/.../ingest.ts` | 16 | 0, and the 12 below never mask |
| the reviewer's own remedy, a lift gated on `isStorableTimestamp` | 10 | 0 |
The third row matters. The reviewer proposed gating the lift on `isStorableTimestamp`. That is not
enough, because eight digits in the 4 2 2 shape spell a real date for several of these rows, so a
gated lift still spares eight digits of a phone and still leaks the number. I built that tree and
measured it rather than guessing. It leaks ten of the 84 rows, namely the two real date 4 2 4
groupings, the real date 4 2 2 2 grouping, the 4 2 4 4 2 4 grouping, the plus 4 2 4 grouping, a
real date followed by two, three or four digits, a 4 2 2 grouping written as a date, a space and
two digits, and a stored timestamp followed by a short tail. That is why the fix removes the lift
and decides from the run instead of patching the line the review named.

Leaks and false masks are counted separately. A leak is a row whose phone shaped digits survive in
the cause. A false mask is a row whose benign digits the masker eats. Both rows of the shipped
module read `leaks=0` and `falseMasks=0`. Twelve rows lose benign digits, and every one of them is
a shape the comment now names. The accepted trade is stated rather than hidden. The masker cannot
tell a number from a count, so a long integer, a version, a time with a fraction, money, a plain
fraction, a comma joined count list, a compact stamp and a date shaped run that is not a date all
lose their digits. A masked benign digit run costs less than a leaked number.

The 84 rows, read live from the shipped module:

| Row | Spelling, digits masked | Kind | Shipped module | Verdict |
| --- | --- | --- | --- | --- |
| control, no digits | `no digits here` | plain | untouched | kept, as documented |
| plain | `##########` | phone | masks | masked, as documented |
| space | `### ### ####` | phone | masks | masked, as documented |
| hyphen | `###-###-####` | phone | masks | masked, as documented |
| dot | `###.###.####` | phone | masks | masked, as documented |
| slash | `###/###/####` | phone | masks | masked, as documented |
| comma | `###,###,####` | phone | masks | masked, as documented |
| comma space | `###, ###, ####` | phone | masks | masked, as documented |
| underscore | `###_###_####` | phone | masks | masked, as documented |
| parenthesised | `(###) ###-####` | phone | masks | masked, as documented |
| plus | `+###########` | phone | masks | masked, as documented |
| plus space | `+# ### ### ####` | phone | masks | masked, as documented |
| plus hyphen | `+#-###-###-####` | phone | masks | masked, as documented |
| non breaking space | `###\u00a0###\u00a0####` | phone | masks | masked, as documented |
| narrow no break space | `###\u202f###\u202f####` | phone | masks | masked, as documented |
| tab | `###\t###\t####` | phone | masks | masked, as documented |
| newline | `###\n###\n####` | phone | masks | masked, as documented |
| grouped 4 2 4, space | `#### ## ####` | phone | masks | masked, as documented |
| grouped 4 2 4, hyphen | `####-##-####` | phone | masks | masked, as documented |
| grouped 4 2 4, real date 4155 12 01 | `####-##-####` | phone | masks | masked, as documented |
| grouped 4 2 4, real date 2026 09 12 | `####-##-####` | phone | masks | masked, as documented |
| grouped 4 2 2 2, real date | `####-##-##-##` | phone | masks | masked, as documented |
| grouped 4 2 2 2, impossible month | `####-##-##-##` | phone | masks | masked, as documented |
| grouped 4 2 4 4 2 4 | `####-##-####-####-##-####` | phone | masks | masked, as documented |
| plus grouped 4 2 4 | `+####-##-####` | phone | masks | masked, as documented |
| date, space, two digits | `####-##-## ##` | phone | masks | masked, as documented |
| date, comma, three digits | `####-##-##, ###` | phone | masks | masked, as documented |
| date, space, four digits | `####-##-## ####` | phone | masks | masked, as documented |
| extension | `### ### #### ext ##` | phone | masks | masked, as documented |
| en dash | `###\u2013###\u2013####` | phone | masks | masked, as documented |
| em dash | `###\u2014###\u2014####` | phone | masks | masked, as documented |
| zero width space | `###\u200b###\u200b####` | phone | masks | masked, as documented |
| soft hyphen | `###\u00ad###\u00ad####` | phone | masks | masked, as documented |
| apostrophe | `###'###'####` | phone | masks | masked, as documented |
| curly apostrophe | `###\u2019###\u2019####` | phone | masks | masked, as documented |
| backslash | `###\###\####` | phone | masks | masked, as documented |
| colon space | `###: ###: ####` | phone | masks | masked, as documented |
| middle dot | `###\u00b7###\u00b7####` | phone | masks | masked, as documented |
| fullwidth digits | `\uff14\uff11\uff15\uff15\uff15\uff15\uff10\uff11\uff12\uff17` | phone | masks | masked, as documented |
| fullwidth parens | `\uff08\uff14\uff11\uff15\uff09\uff15\uff15\uff15\uff10\uff11\uff12\uff17` | phone | masks | masked, as documented |
| tel prefix | `tel:+###########` | phone | masks | masked, as documented |
| trailing space | `### ### #### ` | phone | masks | masked, as documented |
| lone closing paren | `###) ###-####` | phone | masks | masked, as documented |
| grouped 3 4 3 | `### #### ###` | phone | masks | masked, as documented |
| country code, hyphens | `#-###-###-####` | phone | masks | masked, as documented |
| digit comma list | `#,#,#,#,#,#,#,#,#,#` | phone | masks | masked, as documented |
| date, space, number | `####-##-## ##########` | phone | masks | masked, as documented |
| date, hyphen, number | `####-##-##-###-###-####` | phone | masks | masked, as documented |
| date, space, grouped 4 2 4 | `####-##-## ####-##-####` | phone | masks | masked, as documented |
| timestamp glued to number | `####-##-##T##:##:############` | phone | masks | masked, as documented |
| uuid then number | `########-####-####-####-############ ##########` | phone | masks | masked, as documented |
| uuid glued to number | `########-####-####-####-######################` | phone | masks | masked, as documented |
| date alone | `####-##-##` | kept | untouched | kept, as documented |
| timestamp alone | `####-##-##T##:##:##` | kept | untouched | kept, as documented |
| timestamp with a space | `####-##-## ##:##:##` | kept | untouched | kept, as documented |
| timestamp with a fraction and a zone | `####-##-##T##:##:##.#########Z` | kept | untouched | kept, as documented |
| timestamp with an offset | `key ####-##-##T##:##:##+##:##` | kept | untouched | kept, as documented |
| timestamp beside a number | `due ####-##-##T##:##:## and ### ### ####` | kept | masks | masked, as documented |
| long integer | `value ########## is out of range for type integer` | over | masks | masked, as documented |
| version | `deno #.#.#.#.#.#.#` | over | masks | masked, as documented |
| clock time with a fraction | `at ##:##:##.###### the run started` | over | masks | masked, as documented |
| decimal money | `paid #,###,###.## today` | over | masks | masked, as documented |
| fraction | `pi is #.########## exactly` | over | masks | masked, as documented |
| comma list of single digits | `scores #, #, #, #, #, #, # today` | over | masks | masked, as documented |
| compact fourteen digit stamp | `the id ############## is stored` | over | masks | masked, as documented |
| date shaped run that is not a date | `due ####-##-## and nothing else` | over | masks | masked, as documented |
| impossible calendar date beside a number | `due ####-#-## with ### ### ####` | over | masks | masked, as documented |
| unpadded date beside a number | `due ####-#-## with ###-###-####` | over | masks | masked, as documented |
| small counts | `item # of # mentions # times` | plain | untouched | kept, as documented |
| short version | `deno #.#.#` | plain | untouched | kept, as documented |
| small integer | `value ## is out of range` | plain | untouched | kept, as documented |
| non decimal breaks | `x\u00b2 and \u216b break the run` | plain | untouched | kept, as documented |
| letters only | `four hundred and fifteen` | plain | untouched | kept, as documented |
| short space group | `at ## ## ##` | plain | untouched | kept, as documented |
| fullwidth plus | `\uff0b###########` | phone | masks | masked, as documented |
| plain twenty digits | `####################` | phone | masks | masked, as documented |
| hyphen then space 4 2 2 | `####-##-## ##` | phone | masks | masked, as documented |
| slash 4 2 4 with a real date | `####/##/####` | phone | masks | masked, as documented |
| dot 4 2 4 with a real date | `####.##.####` | phone | masks | masked, as documented |
| space 4 2 4 with a real date | `#### ## ####` | phone | masks | masked, as documented |
| real date then letter then digits | `####-##-##x##` | plain | untouched | kept, as documented |
| stored timestamp then short tail | `####-##-##T##:##:## ##` | phone | masks | masked, as documented |
| year zero | `due ####-##-## and nothing else` | over | masks | masked, as documented |
| ten digit fraction | `due ####-##-##T##:##:##.##########` | over | masks | masked, as documented |

Three rows deserve a word. `real date then letter then digits` keeps every digit, because a letter
breaks a run and the comment states that. `stored timestamp then short tail` keeps the date part
and masks the time part, because the rule reads one run at a time. `fullwidth plus` masks in full,
because the pattern takes a fullwidth plus as the leading sign.

## The cross-field join, measured

`/tmp/t27r6/probe-split.ts` hands the real module the body shapes PostgREST returns, with one
number split across two fields. The shipped module masks all eight rows and the pre-fix module
fails four.

```
shipped: rows=8 failures=0
pre-fix: rows=8 failures=4
```

| Row | Shipped module | Pre-fix module |
| --- | --- | --- |
| message ends `###-###`, details starts `####` | `type uuid: [masked number]" for call run <uuid>` | `type uuid: ###-### ####" for call run <uuid>`, leak |
| message ends `###`, details starts `#######` | `type uuid: [masked number]" for call run <uuid>` | `type uuid: ### [masked number]" for call run <uuid>`, first group leaks |
| message holds `### ###`, hint holds `####` | `cast failed for [masked number] belongs to no uuid` | `cast failed for ### ### #### belongs to no uuid`, leak |
| details holds one whole number | `the value [masked number] is not a uuid` | the same, masked |
| a comma split across the fields | `type uuid: [masked number] for call run <uuid>` | `type uuid: ###,### ,#### for call run <uuid>`, leak |
| the key redaction still holds on the join | `[redacted key] [masked number]` | the same |
| a non JSON body holding the number | `type uuid: [masked number]` | the same |
| a benign two field body | `P#### call run does not exist run <uuid>` | the same |

The live probe's own status line shows the same thing from the wire, which is that PostgREST puts
the value it could not cast in one field:

```
call result ingestion failed: HTTP ### ##P## invalid input syntax for type uuid: "aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa no digits here"
```

## Reverted-fix runs

Each revert lives in its own tree under `/tmp/t27r6/trees`, built from the final three files plus
the rest of the tree, and each one reverts a single construct. The two script reverts ran the whole
`test-ingest.sh` there, in its own throwaway Supabase project.

| The fix reverted | Pin that failed | Observed |
| --- | --- | --- |
| the masker back to the shipped lift, `/tmp/t27r6/trees/revert-l1` | the Deno scrub test | `error: Error: ####-##-#### was not masked: ... type uuid: ####-##-#### for call run <uuid> key [redacted key] due 2026-09-12T03:00:00`, then `FAILED \| 10 passed \| 2 failed`, EXIT=1 |
| the same revert | the Deno over-match test | `error: Error: an over-matched shape kept its digits: ... due ####-##-## and nothing else`, same run |
| the join back to the per part scrub, `/tmp/t27r6/trees/revert-l2` | the new split test | `error: Error: a number split across two fields reached the cause: ... type uuid: ###-### ####" for call run <uuid>`, `FAILED \| 11 passed \| 1 failed`, EXIT=1 |
| the due stored as null after the cast, `/tmp/t27r6/trees/revert-l3-null-due` | case one | `the committed values read 'due=none retired_mentions=62,80 commitment_offset=80 commitments=1', not the values the result named`, EXIT=1 |
| the retirement and commitment mention offsets zeroed, `/tmp/t27r6/trees/revert-l3-zero-offsets` | case one | `the committed values read 'due=2026-09-12T03:00:00Z retired_mentions=0,0 commitment_offset=80 commitments=1', not the values the result named`, EXIT=1 |
| the same two mutations, under the round-5 gate | the round-5 gate itself | `EXIT=0` and `T2.7 ingestion acceptance passed.` in `/tmp/t27r6/trees/prefix-gate-null-due` and `/tmp/t27r6/trees/prefix-gate-zero-offsets` |
| the reviewer's remedy, a lift gated on `isStorableTimestamp`, `/tmp/t27r6/trees/reviewer-remedy` | the live probe | `leaks=10` of 84 rows, against `leaks=0` on the shipped module |

The last two rows are the interesting ones. The round-5 gate cannot see either SQL mutation, and
The remedy the review proposed still leaks ten spellings through a real PostgREST body.

Finding four is documentation, so restoring the old comment alone cannot fail a test. The pin
measures the behaviour the repaired comment names, and the row above fails that behaviour. The
same measurement also refutes the old sentence, because the shipped module masks four shapes it
did not mention at all.

## Residue against rounds 1, 2, 3 and 4

Zero. The migration file is byte-identical to the round-5 hash, so every SQL fix the earlier
rounds reviewed is untouched. The script exits 0 with all thirteen earlier lines green, so the
completed fixture, the re-ingest, the invalid fixture, the unoffset retirement, the run lock, both
boundary tables, all eighteen rejection values, the fixture counts, the give-up clear, the repair
list, the payload failure and the stored proposal all still hold.

The Deno suite exits 0 with twelve tests, so the sanitisation, the repair table, the boundary dues,
the re-ingest case, the turn shapes and the key redaction all still hold. The exported surface is
unchanged, namely `TerminalCallSource`, `IngestDeps`, `IngestResult`, `depsFromEnv` and
`ingestTerminalRun`, so `calle.ts` and `dispatch-mode.ts` check and test green without a change.

The scrub is the only behaviour that moved, and it moved in one direction. Comparing the two
markdown tables row by row, no row the round-5 module masked is untouched in the shipped module,
and sixteen rows that leaked there now mask. The new work masks more, never less.

## The commands and the probe output I read

```
# the shipped acceptance
deno check supabase/functions/_shared/ingest.ts
deno test --allow-env --allow-read --allow-net supabase/functions/_shared/ingest.ts
bash supabase/migrations/test-ingest.sh

# the live masking table, on the shipped module and on the pre-fix copy
set -a && source /tmp/orma-p2/env.sh && set +a
deno run --allow-env --allow-read --allow-net /tmp/t27r6/probe-live.ts
PROBE_FORMAT=markdown deno run --allow-env --allow-read --allow-net /tmp/t27r6/probe-live.ts
INGEST_MODULE=/tmp/orma-review-t27-r5/supabase/functions/_shared/ingest.ts \
  deno run --allow-env --allow-read --allow-net /tmp/t27r6/probe-live.ts
INGEST_MODULE=/tmp/t27r6/trees/reviewer-remedy/supabase/functions/_shared/ingest.ts \
  deno run --allow-env --allow-read --allow-net /tmp/t27r6/probe-live.ts

# the cross-field join table
deno run --allow-env --allow-read --allow-net /tmp/t27r6/probe-split.ts
INGEST_MODULE=/tmp/orma-review-t27-r5/supabase/functions/_shared/ingest.ts \
  deno run --allow-env --allow-read --allow-net /tmp/t27r6/probe-split.ts

# the reverts
deno test --allow-env --allow-read --allow-net /tmp/t27r6/trees/revert-l1/supabase/functions/_shared/ingest.ts
deno test --allow-env --allow-read --allow-net /tmp/t27r6/trees/revert-l2/supabase/functions/_shared/ingest.ts
for d in revert-l3-null-due revert-l3-zero-offsets prefix-gate-null-due prefix-gate-zero-offsets; do
  bash /tmp/t27r6/trees/$d/supabase/migrations/test-ingest.sh
done
```

The only SQL I read this round is the stack's own guard, run as
`docker exec -i supabase_db_stack psql -qAt -U postgres`:

```sql
select 'rpc_args=' || pronargs
  || ' anon=' || has_function_privilege('anon', oid, 'execute')::text
  || ' auth=' || has_function_privilege('authenticated', oid, 'execute')::text
  || ' service=' || has_function_privilege('service_role', oid, 'execute')::text
from pg_proc where proname = 'ingest_call_result';
-- rpc_args=12 anon=false auth=false service=true

select 'probe rows: profiles=' || (select count(*) from public.profiles where id='bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb')
  || ' runs=' || (select count(*) from public.call_runs where user_id='bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb')
  || ' events=' || (select count(*) from public.call_events where detail->>'calle_call_id' in ('call_probe','call_split'));
-- probe rows: profiles=0 runs=0 events=0
```

## Cleanup

The shared stack holds no row from this round. The live probe's cast fails before the RPC runs, and
SQL read zero profiles, zero runs and zero events for its ids. The probes never drove `tick`, never
ran `/tmp/orma-p2/up.sh` and never placed a call. `CALLE_API_BASE` stayed the stub host. The
throwaway project of each script run stopped itself, and no container named `orma-t2-7` remains.
The scratch trees under `/tmp/t27r6/trees`, the probes under `/tmp/t27r6` and the round-5 freeze at
`/tmp/orma-review-t27-r5` sit outside the repository, and the only file I wrote inside it is this
remediation.

Every phone spelling in this file is the probe's synthetic number with its digits replaced by `#`.
No secret was echoed.
