# Adversarial review: T10.1, round 1

- Reviewer: gpt-5.6-sol
- Date: 2026-09-14
- Subject: uncommitted T10.1 work on `main` at `80f3f9f`.
- Specification: `dev-diary/PHASE-10-number-confirmation.md`, T10.1.
- Verdict: **APPROVE**

## Counts

| Severity | Count |
| --- | --- |
| C | 0 |
| H | 0 |
| M | 0 |
| L | 0 |

## Pin results

1. **Scope and contract.** T10.1 changes its claimed status, the owned migration, and the owned onboarding request and check. The concurrent P9 status-line change is unrelated. The generated database types are unchanged because the migration changes enforcement, not the schema shape. No implementation path lies outside the task's owned seam.

2. **Authenticated database writes.** I applied the new migration inside a transaction against `supabase_db_orma-p6-local`, created an authenticated fixture user, and rolled the transaction back. An insert carrying a non-null confirmation timestamp stored null. A same-number update could not set the field. An update that changed the E.164 number also left it null. This independently pins the trigger's insert, unchanged-number update, and changed-number update paths.

3. **Privileged database write.** In the same rollback-only local transaction, the privileged update set `phone_confirmed_at` to the requested timestamp. The trigger therefore preserves the service-role path required by T10.2.

4. **Onboarding request.** The onboarding harness captures the profile POST and fails when `phone_confirmed_at` is present. It passed with the field absent. The upsert fallback PATCH also builds its body from only display name, number, and timezone. Consent and slot requests remain unchanged.

5. **Dispatcher refusal.** An independent Deno probe called `dispatchClaimedRun` with a profile containing an E.164 number and a null confirmation timestamp. It returned `refused`, persisted `profile has no confirmed E.164 number`, and issued no request to `/v1/calls`.

6. **Build and regression checks.** `npm run check` in `web/` passed, including the onboarding capture check and Svelte diagnostics with zero errors and warnings. `deno test --allow-read --allow-env supabase/functions/_shared/calle.ts` passed all 50 dispatcher and fixture tests. `git diff --check` passed.

## Findings

None. The round carries zero findings across all severities.

## Zero-residue statement

This is the first review round for T10.1, so there is no prior-round residue. The local trigger measurements, onboarding capture, dispatcher refusal probe, and project checks leave no unaddressed T10.1 requirement.
