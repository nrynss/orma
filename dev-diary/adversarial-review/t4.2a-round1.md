# T4.2a round 1 review

## Verdict

**REMEDIATE**

| C | H | M | L |
| --- | --- | --- | --- |
| 0 | 0 | 0 | 1 |

## Findings

| Where | What | Pin | Mutation |
| --- | --- | --- | --- |
| `dev-diary/PHASE-4-mcp.md`, T4.2a `owns` line | The owns line names three paths. The task also writes `supabase/functions/mcp/tools/double.ts`, four sanctioned fixture lines inside T4.2's still claimed seam. The board hides that excursion. The T4.2a block text never mentions the reconciliation either. | `git status --porcelain` lists ` M supabase/functions/mcp/tools/double.ts`. The T4.2a owns line names only `supabase/functions/mcp/index.ts`, `proxy/src/index.ts` and `supabase/config.toml`. T4.2 still owns `supabase/functions/mcp/tools/` and reads `claimed:orma-impl`. | Leave the record as is and the next T4.2 remediation on `tools/` works against a board that hides the fixture edit. It can collide with or drop the four lines, which re-breaks the type check the suites depend on. |

This finding lives in dev-diary markdown alone. The orchestrator may close it directly under the AGENTS.md shortcut. All four shortcut conditions hold. Name and file are recorded above.

## Independent pins

1. Suites. Deno 2.9.6 on PATH. `deno test --allow-net --allow-env --allow-read supabase/functions/mcp/index.ts` reports 14 passed, 0 failed. The same command on `supabase/functions/mcp/tools/tools_tests.ts` reports 9 passed, 0 failed. `deno check` on both entry points is clean.

2. E2E and mutation. The new colocated test drives `createMcpHandler` itself, not a helper seam. Initialize pins serverInfo `orma` and protocol `2025-03-26`. `tools/list` pins exactly the six TOOL_NAMES. `tools/call list_items` round-trips `item-a1` under the owner-scoped fixture client with no user B row. Mutation in a /tmp copy of the tree: replaced `registerOrmaTools` inside `createOrmaMcpServer` with the old not-implemented stub loop. The suite failed 2 of 14, the cross-user isolation test and the e2e, and passed 12. Reverting the wiring breaks the suite.

3. Proxy mapping. Own Bun 1.3.14 harness imports the worker with a stubbed fetch and reads the outgoing URL and host. Nine cases pass. `/mcp` and `/mcp/` land on `/functions/v1/mcp`. `/mcp/extra`, `/mcp/extra/deeper`, `/rest/v1/items`, `/auth/v1/user`, `/functions/v1/tick`, `/` and `/mcpx` pass through untouched. The host is rewritten to `SUPABASE_HOST` in all nine. Mutation: removed the rewrite block in a /tmp copy. Exactly the two rewrite cases failed and the harness exited 1.

4. Gateway auth off. Own Deno script against `createMcpHandler` through the real `auth.getUser` path. A missing bearer returns 401 with jsonrpc 2.0, error code -32001, id null and `www-authenticate: Bearer error="invalid_token"`. An expired bearer returns the same protocol shape. Reading the handler confirms token extraction and `authenticateBearer` run before client creation, server creation and `transport.handleRequest`. `verify_jwt = false` therefore opens no anonymous access.

5. Fixture reconciliation. Read `20260913120000_terminal_writer.sql` and `20260913130000_finalise_attempts.sql`, plus the `poll.ts` and `dispatch-mode.ts` writers. The fixture row is `completed`, `answered_extracted`, `dry_run false`, `billable true`. The poll stamps `terminal_writer = poll` in the same statement as the state. A dry dispatch stamps `dry_run` only when `dry_run` is true. So `poll` truthfully names the writer for this row. `finalise_attempts 0` matches the column default. `finalise_after null` matches the comment "no attempt has failed". `finalise_error null` agrees. Types check under `deno check`. Extra measurement: reconstructing HEAD `double.ts` without the four lines fails `deno check` and `deno test` with TS2739 missing exactly those four Row properties. The four lines repair a real type break against the phase-2 migrations. `get_last_call` selects an explicit column list that excludes the four, so the tool payload is unchanged. `git status` shows `tools/` touched only in `double.ts` and only on those four lines. Zero semantic drift against the T4.2 approved handlers.

6. Ownership. `git diff --stat` lists exactly five modified files. Three are on the T4.2a owns line. The fourth is the sanctioned four-line fixture fix. The fifth is the diary hunk that adds the T4.2a block, updates T4.3 `requires` and flips no status line. No untracked paths.

7. TOOL_NAMES. One definition survives, at `tools/types.ts` line 21. `tools/mod.ts` re-exports it. `index.ts` imports it from `./tools/mod.ts` and re-exports the name and the `ToolName` type. The six names match the T4.2 list. `notImplementedResult` and `listItemsForUser` are gone with no references left anywhere.

8. Doc style. Every added comment and diary sentence scanned. No semicolons. No em dashes. Active voice. Longest added sentence runs 22 words, in the proxy comment.

## Done-when

A clean client flow initialises, lists the six tools and round-trips
`list_items` through `index.ts` under a fixture client. An anonymous request
fails as a -32001 protocol error. Both function suites are green and
`deno check` is clean.

T4.2a status stays `claimed:orma-impl-2a`. This review does not mark the task done.

## Orchestrator closure

The single L finding is closed directly under the AGENTS.md documentation
shortcut, on 2026-09-13. `dev-diary/PHASE-4-mcp.md` now names
`supabase/functions/mcp/tools/double.ts` on the T4.2a owns line and records the
four-line reconciliation in the block text. No code path, migration, policy,
configuration value or schema changed in this closure. Confirmation is by
reading, not running. Closed by the orchestrator in the T4.2a landing commit.
