# T2.2 contract change

The original file-only seam could not meet the requirement that every count and
age in a briefing is calculated in SQL. T2.2 now owns its dedicated RPC
migration.

| Where | What | Pin | Mutation |
| --- | --- | --- | --- |
| `dev-diary/PHASE-2-call-engine.md` T2.2 ownership | Add `supabase/migrations/20260912160000_briefing.sql` and `supabase/migrations/test-briefing.sh`. | The frozen P1 schema has tables only. It has no briefing RPC, view, or isolated database acceptance runner. | Removing the RPC moves count or age calculation into TypeScript. Removing the runner leaves the SQL facts unpinned. |
| T2.7 ingestion contract | Preserve the completed CALL-E response in `transcripts.raw`, including its top-level `summary`. | `call_runs.calle_confidence` stores only the score and label. `call_runs.briefing` stores prior input, not provider output. | Removing the durable provider response leaves the next call without its required `last_call_summary` substitution. |
| `dev-diary/PHASE-2-call-engine.md` T2.2 ownership | Add `supabase/migrations/test-briefing.sh` for an isolated SQL briefing pin. | Round-one H3 rejected mocked-only golden coverage. | Removing the runner leaves numerical briefing claims unproven against PostgreSQL. |
