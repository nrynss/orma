# T4.1 remediation round 1

This remediation addresses H, M, and L from `t4.1-round1.md`.
It does not change the verdict. T4.1 stays `claimed:orma-impl`.

| Finding | Remediation | Pin | Mutation covered |
| --- | --- | --- | --- |
| H `authenticateBearer` production branch | Added colocated tests that omit `deps.authenticate`. A fetch double answers `/auth/v1/user`. Success returns the fixture userId. Failure throws AuthError. The handler path is also pinned for 401 and 200. | `deno test --allow-net --allow-env --allow-read supabase/functions/mcp/index.ts`. Four new tests name getUser success, getUser failure, handler refuse, and handler accept. | Replacing the getUser body with `return { userId: "leaked-user" }` fails those four pins. The six older tests still pass. |
| M `depsFromEnv` and `apiBase` | Added tests that feed a `*.supabase.co` URL into `apiBase`, `depsFromEnv`, and `createUserScopedClient`. Each must throw a project host error. | Same Deno file. Three named reject tests. | Deleting both `includes("supabase.co")` throws fails all three. Happy path env tests stay green. |
| L colocated test tail | Expanded the minified one-liners after `import.meta.main` into readable blocks. Coverage of the original six names is unchanged. | Read the file from the `const testFn=` line. Named tests remain. | Reformat only. Behaviour pins stay green. |

## Deno result

`deno test --allow-net --allow-env --allow-read supabase/functions/mcp/index.ts`

13 passed, 0 failed.

## Owns note

Only `supabase/functions/mcp/index.ts` and this diary were changed.
No round 2 review. T4.1 is not marked done.
