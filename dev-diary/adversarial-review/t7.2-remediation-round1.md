# T7.2 remediation, round 1

Round 1 commit `59ccb13` returned REMEDIATE with C0 H0 M1 L0 against the
implementation at `4d8a70d`. This round pins every item text digit run behind
the presence of its complete source text in the prose.

## Findings

| where | what | pin | mutation |
| --- | --- | --- | --- |
| M `supabase/functions/analysis/prose.ts` `collectFactValue` text branch beside `tokenAllowed` and the new `textRunAllowed` | Item text digit runs no longer join the scalar `numbers` allowlist. They land in a separate `texts` set of normalized source strings. `textRunAllowed` accepts a prose token from a text run only while the normalized prose contains the complete normalized source text. Matching folds case and collapses whitespace. The digits stay in the comparison. Numeric scalars, period boundaries and slot local times keep their old behaviour. | `prose_tests.ts` pins `numbersOutsideFacts("You completed 12 calls this week.", fixtureFacts)` as non-empty while the fixture carries `completed_runs: 11`. The quoted sentence "She wants to renew 12 month pass this week." still passes. A folded double space variant passes. The 13 negative and every other older pin stay in place. | Reverting the text branch to the old `into.numbers.add` loop reintroduces the defect. The suite then prints `FAILED | 8 passed | 1 failed` because the fabricated count pin throws `a text digit run must fail while its source text is absent`. The quoted positive stays green under the mutation because the run re-enters the scalar allowlist and the quote stops mattering. |

## Proof

1. `deno test --allow-read --allow-env supabase/functions/analysis/prose.ts
   supabase/functions/analysis/prose_tests.ts` printed
   `ok | 9 passed | 0 failed (611ms)`.
2. `deno check supabase/functions/analysis/prose.ts` exited 0 with no output.
3. The mutation was a temporary working-tree edit on the text branch of
   `collectFactValue`. It restored the old allowlist join. The fabricated count
   pin failed as quoted in the mutation column.
4. `prose.ts` was restored by copy right after the run. Its sha256 returned to
   `54fb11db42f5554c0db1756a16d742db79d7b9c9be7f9855a5a4287e385984a3` and the
   suite passed 9 tests again.
