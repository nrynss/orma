# T10.2 remediation, round 3

Round 3 remains REMEDIATE with C0 H1 M0 L0. This remediation fixes H1 only.
It changes no verdict and does not mark the task complete.

## Findings

| where | what | pin | mutation |
| --- | --- | --- | --- |
| H1 `supabase/functions/calle-webhook/index.ts`, `supabase/migrations/20260914110000_phone_confirmations.sql` | A confirmation recovery PATCH now targets only recoverable states and a row whose expiry is still in the future. It cannot update an `expired` row. The phone-change trigger also expires `failed` rows, so a dispatch-record failure cannot leave an A to B to A confirmation available for later recovery. A completed callback still repairs a missing post-dispatch write from a live `requested` or `failed` row. | `deno test --allow-env --allow-net --allow-read supabase/functions/calle-webhook/index.ts` passed 39 tests. The new delayed-callback pin models an expired confirmation and applies the captured REST predicates as the database would. The callback returns 200 and the stored state remains `expired`. | I widened the completed-callback recovery predicate to include `expired`. The delayed-callback pin failed because it changed the stored state to `dialled`. The existing lost-write pin also failed because its exact recovery predicate changed. I restored the live-state predicate. |

## Proof

1. `deno check supabase/functions/calle-webhook/index.ts` exited 0.
2. `deno test --allow-env --allow-net --allow-read supabase/functions/calle-webhook/index.ts` passed 39 tests after restoration.
3. `git diff --check` exited 0.
