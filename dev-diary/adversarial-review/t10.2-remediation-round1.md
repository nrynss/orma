# T10.2 remediation, round 1

Round 1 remains REMEDIATE with C0 H1 M1 L0. This remediation fixes H1 and
M1 only. It changes no verdict and does not mark the task complete.

## Findings

| where | what | pin | mutation |
| --- | --- | --- | --- |
| H1 `supabase/migrations/20260914110000_phone_confirmations.sql`, `expire_phone_confirmations_on_phone_change` | Every `phone_e164` change expires each requested or dialled confirmation owned by that profile. Returning from B to A cannot restore A's old code. The verifier therefore finds no live A row. | Apply the migration on the local stack. Create a requested A row, update the profile A to B and B to A through the authenticated profile route, then call `verify_phone_code` with A's valid code. The independent query must show `state = 'expired'`, `confirmed_at is null`, and `profiles.phone_confirmed_at is null`. The local-stack command could not run in this sandbox because Supabase CLI telemetry tried to write `/home/nryn/.supabase` on a read-only mount. | Remove the `after update of phone_e164` trigger or its state update. The A to B to A database pin again finds the original requested row and confirms it. |
| M1 `supabase/functions/confirm-phone/index.ts` and `supabase/functions/_shared/calle.ts`, `maskedConfirmationRequestBody` | A dry run now persists one `phone_confirmations.dry_run_request` JSON value during the dispatch state patch. The generic phone and webhook masking runs first, then the digit-by-digit code text is replaced. The owner column grant excludes this private audit value. | `deno test --allow-env --allow-net --allow-read supabase/functions/_shared/calle.ts` passes 51 tests, including `a confirmation dry-run record redacts its code, number, and webhook secret`. The pin builds the actual confirmation request and independently asserts that the raw code, digit sequence, phone, and webhook secret are absent. `deno check supabase/functions/confirm-phone/index.ts` exits 0. | Remove the confirmation task replacement or omit `dry_run_request` from the dry-run patch. The pin exposes the code again or a dry-run row has no durable request record. |

## Proof

1. `deno check supabase/functions/confirm-phone/index.ts` exited 0.
2. `deno check supabase/functions/_shared/calle.ts` exited 0.
3. `deno test --allow-env --allow-net --allow-read supabase/functions/_shared/calle.ts` passed 51 tests.
4. `git diff --check` exited 0.
5. The local database pin remains for the re-reviewer. The sandbox blocked the local CLI before it could inspect the stack.
