# T2.1 review, round 1

Verdict: REMEDIATE

| Severity | Count |
|---|---:|
| C | 1 |
| H | 0 |
| M | 0 |
| L | 0 |

| Severity | Where | What | Pin | Mutation |
|---|---|---|---|---|
| C | `supabase/functions/materialise/index.ts`, `supabase/migrations/` | The function exists, but no migration or other tracked configuration creates a `pg_cron` job. T2.1 requires an invocation at 00:10 UTC. Without it, no runs are materialised in production. | `rg -n "cron\\.schedule|net\\.http_post" supabase/migrations` returns no job definition. `deno test --allow-env --allow-net supabase/functions/materialise/index.ts` passes, but it only invokes the handler directly. | Reverting the scheduler configuration leaves materialisation dependent on a manual POST. The next 48 hours of visible calls never appear. |

Evidence checked independently:

- `deno test --allow-env --allow-net supabase/functions/materialise/index.ts` passed all five tests.
- The Kolkata 08:00 conversion, first repeated DST instant, skipped DST time, key-based duplicate suppression, and inactive-slot predicate are covered by that suite.
- The inactive-slot request is constrained to the same `slot_id`, `state=scheduled`, and `scheduled_for` strictly after the run time. It therefore preserves claimed, dispatched, terminal, and already-due runs.
- `git diff --check` passed.
