# T6.4 round-2 review: Patterns — APPROVE

Fresh re-reviewer. Nothing was fixed in this round. Every round-1 pin was
re-run against the rebuilt stack, plus the remediation pin for the live-run
briefing. Zero residue.

## Residue against round 1 (explicit)

Round 1 returned REMEDIATE with one M finding: the T6.4 harness edit had
deleted the pre-existing live-run briefing update in `insert_harness_rows`,
leaving run `4f000000-0000-4000-8000-0000000000c1` with `briefing` null, so
T6.1 Today could no longer exercise its briefing-order branch.

Re-measured state, 2026-09-14, rebuilt stack:

- `scripts/p6-local-stack.sh:211-214` carries the briefing update again, in
  its original spot between the `item_mentions` insert and the Patterns
  mood-runs block. The four lines are byte-identical to HEAD. The only
  byte difference in that hunk is a removed blank line before the comment.
- `bash -n scripts/p6-local-stack.sh` passes.
- Live run `4f00...0000c1`: `briefing is not null` is true, `jsonb_typeof`
  is `object`, its six top-level keys
  (`lead_line`, `user_name`, `open_count`, `open_items`, `slot_local_time`,
  `last_call_summary`) exactly match the key set of a fresh
  `public.assemble_briefing('00000000-0000-4000-8000-000000000001', '08:00', now())`
  call, and `briefing = assemble_briefing(...)` is true. Today live state
  reads again.
- Claim: zero residue. The M is closed.

## Pins (all re-measured 2026-09-14, rebuilt stack)

- Report row: 1 `pattern_reports` row for the seed account
  (`00000000-0000-4000-8000-000000000001`), period 2026-09-07 to 2026-09-13.
- Facts keys (14): ages, mentions, period_end, period_days, retirements,
  period_start, answered_runs, observed_days, required_days, completed_runs,
  longest_surviving, mood_distribution, sufficient_history, answer_rate_by_slot.
- Facts scalars: `period_days` 7, `completed_runs` 4, `answered_runs` 4,
  `mood_distribution` [{energised 1}, {low 1}, {ok 1}, {stressed 1}],
  `sufficient_history` true, `required_days` 3, `observed_days` 4.
- Prose: "Across 7 days you answered 4 calls. The calls heard you as
  energised 1 time, ok 1 time, low 1 time and stressed 1 time."
- In-period runs: 4 completed rows with `local_date` inside 2026-09-07 to
  2026-09-13, ordered by `scheduled_for`: stressed 09-10, low 09-11,
  ok 09-12, energised 09-13, all `answered_extracted`. Two further rows
  sit outside the window (canceled 09-14, scheduled 09-15).
- PostgREST as local@example.com: latest-report read returns 1 row with all
  14 facts keys; runs read returns 6 rows in `scheduled_for` order with the
  same 4 moods first; pattern deliveries read returns 1 row
  (telegram, `call_run_id` null, `sent_at` 2026-09-13T06:04:44Z, about 1 day
  old), so the sent-line predicate (sent, null or in-period run) holds.
- PostgREST as firstrun@example.com: 0 reports, 0 runs, and the seed report
  is invisible, so RLS isolation holds and the loader takes the not-yet
  branch (`report` null). Not-yet counts derive from rows:
  `completedSoFar` over an empty set is 0 and `requiredDays` with no facts
  falls back to 3 (both helpers executed against the real source file).
- Model helpers executed from `web/src/routes/app/patterns/model.ts`:
  `trendRuns` over the 6 measured rows returns exactly the 4 in-period
  moods in chronological order (stressed, low, ok, energised);
  `moodLane` maps energised, ok, low, stressed to 0, 1, 2, 3 and null or
  unknown to -1 (hollow); `periodLabel` gives "7 to 13 September".
- Dev server port 5200: `/` returns 200; `/app/patterns` signed-out returns
  303 to `/login`, identical to `/app`, `/app/items`, and `/app/settings`.
  No signed-out shell leaks. Server was started on the assigned port only
  and killed afterwards with its own bracketed pattern. No `npm run check`,
  build, or deploy ran.
- Harness diff: the only tracked change is one hunk in
  `scripts/p6-local-stack.sh` inside `insert_harness_rows` (28 additions,
  2 deletions). Every added date is `now()`-relative. Every insert uses a
  fixed harness UUID. Facts come from the read-only `compute_pattern_facts`.
  No production side effects.
- Scope: `git diff HEAD --name-only` shows only `scripts/p6-local-stack.sh`.
  `web/src/routes/app/patterns/` is new and untracked as expected. The
  other untracked paths (review docs, `history/` route) belong to sibling
  tasks and process files, not this one.
- Scratch rows: none were inserted. Row counts were read, never written.

## No-hallucination check (T7.2 rule)

Every integer in the prose appears in `facts`. 7 is `period_days`. 4 is
`completed_runs` and `answered_runs`. Each 1 is one `mood_distribution`
entry (energised 1, low 1, ok 1, stressed 1). All four mood names in the
prose are present in the distribution. Counts stated, zero unpinned numbers.

## Findings: 0 C, 0 H, 0 M, 0 L

No new findings. The table is empty by measurement, not by trust.

| # | Where | What | Pin | Mutation |
|---|-------|------|-----|----------|
| - | - | - | - | - |

## Done-when assessment

- "A fixture report renders with its facts visible": PASS. One report row
  with 14 facts keys reads as the owner over PostgREST, and the view renders
  the prose as plain paragraphs plus every facts key beside its value under
  the "Computed by SQL" heading.
- "The mood trend draws from real rows": PASS. The 4 in-period completed
  rows are exactly the trend input in chronological order; out-of-period
  rows are excluded and the loader sort plus `trendRuns` filter were both
  executed, not read.
- "A user with too little history sees an honest not-yet state": PASS.
  firstrun@example.com has 0 reports and 0 rows, hits the not-yet branch,
  and sees row-counted "0 calls" with the 3-day rule and no invented trend.

## Verdict

APPROVE. Zero findings across all severities. Round-1 M closed with zero
residue, every round-1 pin re-measured clean, no new defects found.
